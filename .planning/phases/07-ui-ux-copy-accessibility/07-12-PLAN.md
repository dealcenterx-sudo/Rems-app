---
phase: 07-ui-ux-copy-accessibility
plan: 12
type: execute
wave: 7
depends_on: ["07-08", "07-09", "07-10", "07-11"]
files_modified:
  - src/App.css
  - package.json
  - docs/SAAS_UPGRADE_CHANGELOG.md
autonomous: true
requirements: [UI-05, A11Y-01, A11Y-03]

must_haves:
  truths:
    - "Text contrast meets WCAG 2.2 AA (4.5:1): --text-faint bumped to #7f7f7f"
    - "The focus ring is brand-green everywhere at >=3:1 (unified --focus-ring rgba(0,255,136,0.45)); --shadow-focus deleted; no :focus-visible selector left without a visible indicator"
    - "plugin:jsx-a11y/recommended is enabled at error level and npm run lint is green (full src)"
    - "Pass 2 value changes are a separate commit from Pass 1 (D-17)"
  artifacts:
    - "App.css Pass-2 token value changes (--text-faint, unified --focus-ring, deleted --shadow-focus, .skeleton base reconcile)"
    - "package.json eslintConfig with plugin:jsx-a11y/recommended"
    - "docs/SAAS_UPGRADE_CHANGELOG.md Phase 7 entry"
  key_links:
    - "Every :focus-visible consumer of --shadow-focus repointed to --focus-ring"
    - "The atomic flip is green only because plans 08+09 cleared all 251 violations"
---

<objective>
Land the value-changing half of the token work and the accessibility gates: Token Pass 2 (A11Y-01,
D-18) fixes text contrast and the focus-ring bug at the token level so it propagates everywhere, and
the atomic jsx-a11y flip (A11Y-03, D-21) enables `plugin:jsx-a11y/recommended` at error level with a
green full-src lint. Pass 2 is a SEPARATE COMMIT from Pass 1 (D-17). Also record the Phase 7 changelog
entry (AUDIT-03 standing task).

Purpose: A11Y-01, A11Y-03, and the completion of UI-05 (value changes). This is the phase capstone;
after it, `main` is shippable with WCAG-AA text/focus and a green a11y lint gate.
Output: App.css Pass-2 changes, package.json rule flip, changelog entry.

## Artifacts this phase produces (this plan)
- App.css: --text-faint -> #7f7f7f; single unified --focus-ring (rgba(0,255,136,0.45)); --shadow-focus deleted; :focus-visible consumers repointed; .skeleton base reconciled to var(--surface-2) (RC-02)
- package.json eslintConfig: adds plugin:jsx-a11y/recommended
- docs/SAAS_UPGRADE_CHANGELOG.md: Phase 7 entry
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-ux-copy-accessibility/07-UI-SPEC.md
@.planning/phases/07-ui-ux-copy-accessibility/07-PATTERNS.md
@.planning/phases/07-ui-ux-copy-accessibility/07-RESEARCH.md
@src/App.css
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Token Pass 2 — contrast + focus-ring reconciliation (A11Y-01, D-18) — separate commit</name>
  <files>src/App.css</files>
  <read_first>07-PATTERNS.md "App.css :root (Pass 2)" + 07-RESEARCH.md "Focus-ring reconciliation targets" (exact lines L73, L4345/4347, L4351, L4494) + the caution about the second :root block; 07-UI-SPEC.md "Focus indicator reconciliation" + "Text contrast" tables; 07-CONTEXT.md RC-02.</read_first>
  <action>
    Value changes only (Pass 2), committed separately from Pass 1. (1) Contrast: change --text-faint
    from #757575 to #7f7f7f (~4.6:1 on --surface-3) so the minimum text tier meets WCAG 2.2 AA 4.5:1;
    verify other tiers still clear 4.5:1. (2) Focus ring: collapse to ONE --focus-ring definition (keep
    a single :root definition; delete the duplicate at ~L4347). DELETE --shadow-focus (~L4351) and
    repoint every :focus-visible rule that consumed it (the global block at ~L4494, and any other
    consumer) to var(--focus-ring), so the indicator is brand-green everywhere. Set the unified ring to
    rgba(0, 255, 136, 0.45) (>=3:1 non-text contrast). CRITICAL (security/a11y): no :focus-visible
    selector may be left with no visible indicator — every rule that used --shadow-focus must now render
    the green --focus-ring. CAUTION: the second :root block (~L4345) also legitimately defines
    --surface-3, --radius-lg, --radius-md, --shadow-soft — delete ONLY the duplicate --focus-ring line
    and the --shadow-focus line, not the block. (3) RC-02: reconcile the legacy .skeleton rule base from
    #0a0a0a to var(--surface-2) and its sweep to var(--skeleton-highlight) so it matches the Skeleton
    primitive. Confirm no remaining reference to --shadow-focus exists anywhere.
  </action>
  <verify>
    <automated>npm run build && npm run lint && test "$(grep -c -- '--shadow-focus' src/App.css)" -eq 0 && grep -q "0, 255, 136, 0.45" src/App.css</automated>
  </verify>
  <acceptance_criteria>
    --text-faint is #7f7f7f; a single --focus-ring at rgba(0,255,136,0.45) drives all :focus-visible;
    --shadow-focus is deleted with zero remaining references; no :focus-visible selector lacks a visible
    ring; .skeleton base reconciled to var(--surface-2); build + lint green. Committed separately from
    Pass 1.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Atomic jsx-a11y flip + full-src green lint (A11Y-03, D-21)</name>
  <files>package.json</files>
  <read_first>07-PATTERNS.md "package.json eslintConfig" section; 07-RESEARCH.md Pitfall 2 (flip must be atomic-green); confirm plans 08+09 SUMMARYs report zero cluster violations.</read_first>
  <action>
    Add "plugin:jsx-a11y/recommended" to the package.json eslintConfig extends array (after
    "react-app", "react-app/jest"). The plugin is already installed transitively — no npm install. This
    flip makes the 251 previously-cleared violations enforced errors; because plans 08+09 cleared them
    all, the full-src lint must be green. If lint surfaces any residual violation, fix that specific
    file here (it is a straggler plans 08/09 missed) so the flip lands green in one commit — never
    enable-then-fix across commits.
  </action>
  <verify>
    <automated>npm run lint && npm run build && grep -q "jsx-a11y/recommended" package.json</automated>
  </verify>
  <acceptance_criteria>
    package.json eslintConfig extends plugin:jsx-a11y/recommended; npm run lint is green across all of
    src (rule now enforced at error); build green. CI lint gate stays green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Phase 7 changelog entry (AUDIT-03) + full-suite gate</name>
  <files>docs/SAAS_UPGRADE_CHANGELOG.md</files>
  <read_first>docs/SAAS_UPGRADE_CHANGELOG.md existing format (what/why/files/commands/results/risks per phase).</read_first>
  <action>
    Append a Phase 7 entry following the existing changelog format: what changed (copy standard + error
    map, empty states, skeletons + SWR, pending + optimistic feedback, two-pass token migration, token-
    level contrast + brand-green focus ring, focus trap + keyboard/semantic/color-alone a11y, jsx-a11y
    lint gate), why, key files/artifacts, verification commands (npm run lint / test:ci / build + the
    manual UAT items from 07-VALIDATION.md), results, and residual risks / deferred items (admin-only
    copy sweep, exhaustive focus-trap coverage, decorative-color migration — per Deferred Ideas). Record
    the manual-only verifications (CLS, focus trap, focus-ring color, color-alone, optimistic revert)
    as pending phase-verify items. Run the full suite as the final phase gate.
  </action>
  <verify>
    <automated>npm run test:ci && npm run lint && npm run build && grep -qi "phase 7" docs/SAAS_UPGRADE_CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    Changelog has a Phase 7 entry (what/why/files/commands/results/risks); full suite (test:ci + lint +
    build) green; manual-only UAT items recorded for phase verify.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard user → focus indicator | The focus ring is the visible affordance telling users where they are |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-15 | Denial of Service (a11y / security affordance) | :focus-visible reconciliation | high | mitigate | --shadow-focus is deleted and EVERY consuming :focus-visible selector is repointed to the visible green --focus-ring; verify greps for zero remaining --shadow-focus references. No selector is left without a visible indicator (removing visible focus without replacement is both an a11y and a security-affordance regression). |
| T-07-16 | Tampering | App.css Pass-2 value changes | low | mitigate | Value changes are a separate commit from Pass 1 (D-17); build/lint gate + WCAG contrast targets bound the change; brand direction preserved. |

No package installs this phase (jsx-a11y is already present transitively; only enabled). No supply-chain checkpoint applies.
</threat_model>

<verification>
- `npm run lint` green WITH plugin:jsx-a11y/recommended enabled (atomic flip proof, full src)
- `npm run build` + `npm run test:ci` green (phase gate)
- `grep -c -- '--shadow-focus' src/App.css` == 0; unified --focus-ring at 0.45 opacity present
- Manual (07-VALIDATION.md): focus ring visible + brand-green everywhere; CLS; focus trap; color-alone; optimistic revert
</verification>

<success_criteria>
Text/focus meet WCAG 2.2 AA at the token level; the focus indicator is brand-green everywhere with no
missing-indicator selector; jsx-a11y/recommended is enforced and green; the phase changelog is
recorded; main is shippable.
</success_criteria>

<output>
Create `.planning/phases/07-ui-ux-copy-accessibility/07-12-SUMMARY.md` when done.
</output>
