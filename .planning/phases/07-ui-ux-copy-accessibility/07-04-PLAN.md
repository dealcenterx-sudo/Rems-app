---
phase: 07-ui-ux-copy-accessibility
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/components/HomePage.js
  - src/components/CRMLeadsPage.js
  - src/components/TasksPage.js
  - src/components/MyDealsPage.js
  - src/components/ContactsPage.js
  - src/components/PropertiesPage.js
  - src/components/DocumentsPage.js
  - src/components/ActiveDealsPage.js
autonomous: true
requirements: [UI-01, COPY-03]

must_haves:
  truths:
    - "Every list/dashboard surface (including the buyer/seller MyDealsPage shell) shows a designed empty state"
    - "The 6 primary lists distinguish first-use vs no-results (no-results offers a quiet clear-filters affordance)"
    - "Load-failure branches render a PageState error state sourced from errorMessages.js"
  artifacts:
    - "PageState first-use/no-results/error wiring across the listed pages"
  key_links:
    - "Empty-state copy follows docs/COPY-STANDARD.md"
    - "Error states source their message from errorMessages.js (no raw err.message)"
---

<objective>
Give every list/dashboard surface a designed empty state built on the existing `PageState`
primitive (UI-01, D-04) — with first-use vs no-results variants distinct on the 6 primary lists
(D-02/D-03) and an error-tone state for load failures sourced from the central error map (D-05).
Copy follows the standard authored in plan 01. `PageState` structure is unchanged (D-04); pages
that lack it import and add it, pages that have it gain the missing variants.

Purpose: UI-01. Scope is the empty/error branches only — skeleton wiring (loading branch) is
plan 06, so this plan does not touch the loading branch beyond leaving it intact.
Output: PageState wiring across HomePage, CRMLeadsPage, TasksPage, MyDealsPage (buyer/seller
shell), ContactsPage, PropertiesPage, DocumentsPage, ActiveDealsPage.

## Artifacts this phase produces (this plan)
- Consistent PageState empty/no-results/error wiring on the listed pages (no new files)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-ux-copy-accessibility/07-UI-SPEC.md
@.planning/phases/07-ui-ux-copy-accessibility/07-PATTERNS.md
@docs/COPY-STANDARD.md
@src/components/PageState.js
@src/components/ContactsPage.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: First-use + no-results variants on the primary lists (CRMLeadsPage, TasksPage, ContactsPage, PropertiesPage, DocumentsPage)</name>
  <files>src/components/CRMLeadsPage.js, src/components/TasksPage.js, src/components/ContactsPage.js, src/components/PropertiesPage.js, src/components/DocumentsPage.js</files>
  <read_first>07-PATTERNS.md "Page empty/loading branches" (ContactsPage L485-503 canonical two-variant wiring); 07-UI-SPEC.md "Empty states" table; docs/COPY-STANDARD.md.</read_first>
  <action>
    Replicate the canonical two-variant PageState wiring (ContactsPage L485-503) on each primary list.
    First-use branch (data length === 0): terse noun title (e.g. "No leads yet", "No tasks yet",
    "No contacts yet", "No properties yet", "No documents yet"), one factual message line stating
    what appears here + how to add the first item, and one understated single action (D-03). No-results
    branch (filtered/searched to zero): title "No matches", a factual line that nothing matches the
    current filters, and a quiet "Clear filters" affordance wired to the page's existing filter/search
    reset so it never reads identically to first-use (D-03). Pages that already import PageState
    (ContactsPage, PropertiesPage, DocumentsPage) extend to both variants; pages that lack it
    (CRMLeadsPage, TasksPage) import and add it. Copy per COPY-STANDARD.md; minimal/factual (D-01).
    Do not alter the loading branch (skeletons are plan 06). Use tokens for any new inline color;
    do not introduce raw hex.
  </action>
  <verify>
    <automated>npm run lint && npm run build && grep -l "PageState" src/components/CRMLeadsPage.js src/components/TasksPage.js</automated>
  </verify>
  <acceptance_criteria>
    Each of the five primary lists renders a first-use empty state and a distinct no-results state
    with a clear-filters affordance; copy follows the standard; lint + build green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Empty + error states on HomePage, MyDealsPage (client shell), ActiveDealsPage</name>
  <files>src/components/HomePage.js, src/components/MyDealsPage.js, src/components/ActiveDealsPage.js</files>
  <read_first>07-PATTERNS.md "Page empty/loading branches"; 07-RESEARCH.md per-page table (MyDealsPage = buyer/seller shell, explicitly in UI-01); 07-UI-SPEC.md error-state row.</read_first>
  <action>
    Add a designed empty state to HomePage (dashboard first-use) and MyDealsPage — the buyer/seller
    client shell, which UI-01 explicitly includes — using PageState with minimal/factual copy from
    the standard. On surfaces with a distinguishable filtered view (ActiveDealsPage), extend to the
    no-results variant per D-03. For each page, add a `PageState tone="error"` branch for load
    failures with title of the form "Couldn't load {noun}", message+recovery sourced from
    `errorMessages.js` (mapError), and a "Try again" action where a retry/reload handler already
    exists. Never interpolate raw `err.message` (security). Leave the loading branch for plan 06.
    Use tokens for any new inline color.
  </action>
  <verify>
    <automated>npm run lint && npm run build && grep -l "PageState" src/components/HomePage.js src/components/MyDealsPage.js</automated>
  </verify>
  <acceptance_criteria>
    HomePage, MyDealsPage, and ActiveDealsPage render designed empty states plus a PageState error
    state whose copy comes from errorMessages.js; lint + build green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| load error → PageState error copy | Firestore load failures surface as user-visible error states |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Information Disclosure | PageState error states | medium | mitigate | Error-state copy comes from errorMessages.js (curated), never raw err.message; reuses the leak-safe map from plan 01. |

No package installs this phase; no supply-chain checkpoint applies.
</threat_model>

<verification>
- `npm run lint` + `npm run build` green
- Manual (deferred to phase verify): each surface shows the correct empty/no-results/error variant
</verification>

<success_criteria>
Every listed surface (incl. the client shell) shows a designed empty state; primary lists
distinguish first-use vs no-results; error states are leak-safe and standard-compliant.
</success_criteria>

<output>
Create `.planning/phases/07-ui-ux-copy-accessibility/07-04-SUMMARY.md` when done.
</output>
