---
phase: 07-ui-ux-copy-accessibility
plan: 07
type: execute
wave: 4
depends_on: ["07-01", "07-05", "07-06"]
files_modified:
  - src/components/TasksPage.js
  - src/components/ActiveDealsPage.js
  - src/components/TasksPage.test.js
  - src/components/NewDealPage.js
autonomous: true
requirements: [UI-04, UI-06]

must_haves:
  truths:
    - "Task complete/incomplete and deal status toggles update the UI optimistically"
    - "On write failure the toggle silently reverts and an error toast (message+recovery) shows — no Retry"
    - "Primary submit/destructive buttons show a present-tense pending state while in flight"
  artifacts:
    - "TasksPage.handleToggleComplete converted to optimistic setState → updateDoc → revert-on-catch"
    - "ActiveDealsPage.updateDealStatus optimistic status with post-confirm side effects preserved"
    - "src/components/TasksPage.test.js optimistic-rollback unit test"
    - "LoadingButton pendingLabel adopted on key submit/destructive buttons"
  key_links:
    - "Rollback error toast sources copy from errorMessages.js (plan 01)"
    - "Pending buttons use LoadingButton pendingLabel (plan 05)"
---

<objective>
Make the two toggle surfaces feel instant (UI-06, D-12/D-13) and adopt the standardized pending
button (UI-04, D-14) on the primary submit/destructive controls. Optimistic scope is TOGGLES ONLY:
task complete/incomplete and task/deal status. On failure, silently revert + error toast
(message+recovery from the central map) with NO Retry. All other mutations keep normal
pending→confirm.

Purpose: UI-06 + the visible half of UI-04. Removes the anti-optimistic Firestore reload from the
task toggle hot path and adopts LoadingButton where users wait on a write.
Output: Optimistic toggles + rollback test; pending-button adoption on key buttons.

## Artifacts this phase produces (this plan)
- TasksPage.handleToggleComplete optimistic conversion (removes the loadData reload from the hot path)
- ActiveDealsPage.updateDealStatus optimistic status (post-confirm side effects preserved)
- src/components/TasksPage.test.js (optimistic rollback)
- pendingLabel adoption on NewDealPage submit + destructive confirms
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-ux-copy-accessibility/07-UI-SPEC.md
@.planning/phases/07-ui-ux-copy-accessibility/07-PATTERNS.md
@src/components/Loading.js
@src/utils/errorMessages.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Optimistic toggles with rollback + unit test (UI-06, D-12/D-13)</name>
  <files>src/components/TasksPage.js, src/components/ActiveDealsPage.js, src/components/TasksPage.test.js</files>
  <read_first>07-PATTERNS.md "Optimistic toggles" (TasksPage.handleToggleComplete L466-479 anti-pattern; ActiveDealsPage.updateDealStatus L102-139 rollback anchor); 07-CONTEXT.md D-12/D-13.</read_first>
  <behavior>
    - Toggling a task optimistically updates its status in local state immediately (before the write resolves).
    - When the Firestore write rejects, the task's status reverts to its prior value and an error toast is shown with the mapped message + recovery.
    - No Firestore reload occurs on the success hot path (the optimistic state is authoritative until next natural load).
  </behavior>
  <action>
    Convert `TasksPage.handleToggleComplete` from "await updateDoc → loadData()" to: capture prior
    status, optimistic `setState` to the new status, `await updateDoc`, and on catch revert `setState`
    to the prior value + `toast.error` composed from `errorMessages.mapError(e)` (message + recovery).
    REMOVE the `loadData(...)` reload from the hot path (it is the anti-optimistic reload). For
    `ActiveDealsPage.updateDealStatus`, use the already-captured `previousStatus` as the rollback
    anchor: reflect `status` optimistically, keep ALL existing post-confirm side effects (seller
    activelySelling, loadDeals, setSelectedDeal, logActivity, notifyUsers) AFTER the successful write,
    and on catch revert only the optimistic status + error toast from the central map. No inline
    per-control error, no Retry (D-13). Add `src/components/TasksPage.test.js` (RTL, mock the Firestore
    updateDoc to reject) asserting the three behaviors above.
  </action>
  <verify>
    <automated>npx react-scripts test --watchAll=false src/components/TasksPage.test.js && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    Both toggles update optimistically and revert with an error toast on write failure; the task
    toggle no longer reloads from Firestore on success; the rollback unit test passes; lint green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Adopt LoadingButton pending state on primary submit/destructive controls (UI-04, D-14)</name>
  <files>src/components/NewDealPage.js, src/components/ActiveDealsPage.js</files>
  <read_first>07-UI-SPEC.md "LoadingButton standardization"; 07-PATTERNS.md "Pending state" shared pattern.</read_first>
  <action>
    Convert the primary submit button on NewDealPage and the destructive/status-confirm controls on
    ActiveDealsPage to `LoadingButton` with `loading` bound to the in-flight state and a present-tense
    `pendingLabel` ("Saving…", "Creating deal…", "Deleting…", etc. per COPY-STANDARD.md). Where a
    destructive confirm flows through ConfirmModal, pass its new `loading`/`confirming` prop (plan 05)
    so the confirm button shows pending. Verify destructive copy matches actual handler behavior
    (D-07): deal "close" writes status (not delete); a true delete uses deleteDoc — copy must not
    claim an outcome the handler does not perform. Use tokens for any new inline color.
  </action>
  <verify>
    <automated>npm run lint && npm run build && grep -q "pendingLabel" src/components/NewDealPage.js</automated>
  </verify>
  <acceptance_criteria>
    NewDealPage submit and ActiveDealsPage destructive/status controls show a present-tense pending
    state while in flight; destructive copy matches handler behavior; lint + build green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| optimistic UI ↔ Firestore | UI reflects a write before confirmation; Firestore remains source of truth on rollback |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-09 | Tampering (state integrity) | optimistic toggles | low | mitigate | On write failure the UI reverts to prior state (Firestore authoritative); post-confirm side effects run only after a successful write. |
| T-07-10 | Information Disclosure | rollback error toast | medium | mitigate | Toast copy sourced from errorMessages.js (curated), never raw err.message. |

No package installs this phase; no supply-chain checkpoint applies.
</threat_model>

<verification>
- `npx react-scripts test --watchAll=false src/components/TasksPage.test.js` green
- `npm run lint` + `npm run build` green
- Manual (phase verify): induce a denied/offline write; confirm revert + error toast, no Retry
</verification>

<success_criteria>
Task and deal toggles are optimistic with leak-safe rollback; primary submit/destructive buttons
show present-tense pending states; destructive copy matches handlers.
</success_criteria>

<output>
Create `.planning/phases/07-ui-ux-copy-accessibility/07-07-SUMMARY.md` when done.
</output>
