---
phase: 07-ui-ux-copy-accessibility
plan: 09
type: execute
wave: 5
depends_on: ["07-07"]
files_modified:
  - src/components/CRMEmailInboxPage.js
  - src/components/DealEditModal.js
  - src/components/DealProgressTab.js
  - src/components/DocumentsPage.js
  - src/components/DealChatTab.js
  - src/components/DealFinancialsTab.js
  - src/components/DealPartiesTab.js
  - src/components/ActiveDealsPage.js
  - src/components/DealDocumentsTab.js
  - src/components/ConfirmModal.js
  - src/components/DealsDashboard.js
  - src/components/LeadDrawer.js
  - src/components/SettingsPage.js
autonomous: true
requirements: [A11Y-02]

must_haves:
  truths:
    - "All remaining jsx-a11y/recommended violations outside cluster A are fixed"
    - "Deal-portal tabs, modals, and secondary pages are keyboard-reachable with associated labels"
    - "Together with cluster A, the whole src/ tree is jsx-a11y clean (verified atomically in plan 12)"
  artifacts:
    - "jsx-a11y fixes across cluster-B files (~117 of 251 violations)"
  key_links:
    - "Zero violations here + in cluster A is the precondition for the atomic lint flip (plan 12)"
---

<objective>
Fix the remaining jsx-a11y/recommended violations — cluster B, every violating file NOT owned by
plan 08 (CRMEmailInboxPage, DealEditModal, DealProgressTab, DocumentsPage, DealChatTab,
DealFinancialsTab, DealPartiesTab, ActiveDealsPage, DealDocumentsTab, ConfirmModal, DealsDashboard,
LeadDrawer, SettingsPage, plus any small stragglers ≤3 the full scan reveals). The rule is not yet
enabled in package.json (that atomic flip is plan 12); this plan clears the rest of the backlog.

Purpose: A11Y-02. Together with plan 08 this drives the whole tree to zero jsx-a11y violations.
Output: Zero jsx-a11y violations in cluster-B files.

## Artifacts this phase produces (this plan)
- label/keyboard/semantic fixes across cluster-B files
- never-color-alone status cues on deal-portal status indicators where color-only
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-ux-copy-accessibility/07-RESEARCH.md
@.planning/phases/07-ui-ux-copy-accessibility/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deal-portal tabs + modals a11y (DealEditModal, Deal*Tab, LeadDrawer, ConfirmModal)</name>
  <files>src/components/DealEditModal.js, src/components/DealProgressTab.js, src/components/DealChatTab.js, src/components/DealFinancialsTab.js, src/components/DealPartiesTab.js, src/components/DealDocumentsTab.js, src/components/LeadDrawer.js, src/components/ConfirmModal.js</files>
  <read_first>07-RESEARCH.md a11y breakdown (per-file counts) + reproduce command.</read_first>
  <action>
    Apply the same fix families as plan 08 (label-has-associated-control via htmlFor/id or wrap;
    click-events-have-key-events + no-static-element-interactions via button-or-role+tabIndex+onKeyDown;
    no-autofocus removal) to these deal-portal tabs and modals. For ConfirmModal, this is a11y-attribute
    only — do NOT undo the plan-05 focus-trap/LoadingButton structure; just clear its remaining jsx-a11y
    violations. Where deal status is conveyed by color alone in these tabs, add a text/icon cue (D-20).
    No color/hex changes (token sweep is plans 10/11). Preserve behavior and appearance.
  </action>
  <verify>
    <automated>printf '{ "root": true, "extends": ["react-app", "plugin:jsx-a11y/recommended"] }' > .a11y-eslintrc.json; npx eslint --no-eslintrc --resolve-plugins-relative-to . -c .a11y-eslintrc.json --ext .js src/components/DealEditModal.js src/components/DealProgressTab.js src/components/DealChatTab.js src/components/DealFinancialsTab.js src/components/DealPartiesTab.js src/components/DealDocumentsTab.js src/components/LeadDrawer.js src/components/ConfirmModal.js; code=$?; rm -f .a11y-eslintrc.json; exit $code</automated>
  </verify>
  <acceptance_criteria>
    Zero jsx-a11y/recommended violations in these deal-portal/modal files; plan-05 ConfirmModal
    structure intact; behavior/appearance unchanged.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Secondary pages a11y + straggler sweep (CRMEmailInboxPage, DocumentsPage, ActiveDealsPage, DealsDashboard, SettingsPage, + rest)</name>
  <files>src/components/CRMEmailInboxPage.js, src/components/DocumentsPage.js, src/components/ActiveDealsPage.js, src/components/DealsDashboard.js, src/components/SettingsPage.js</files>
  <read_first>07-RESEARCH.md a11y breakdown ("plus 3-and-under across the rest") + reproduce command.</read_first>
  <action>
    Apply the same fix families to these pages. Then run the full-src a11y scan (reproduce command)
    and fix any remaining violating files NOT owned by plan 08 (the unnamed ≤3-violation stragglers) —
    add each such file to this plan's edits. For ActiveDealsPage, keep the plan-04/06/07 empty/skeleton/
    optimistic changes intact; a11y-attribute changes only. Apply never-color-alone cues (D-20) to any
    color-only status here. No color/hex changes (token sweep is plans 10/11).
  </action>
  <verify>
    <automated>printf '{ "root": true, "extends": ["react-app", "plugin:jsx-a11y/recommended"] }' > .a11y-eslintrc.json; npx eslint --no-eslintrc --resolve-plugins-relative-to . -c .a11y-eslintrc.json --ext .js src/components/CRMEmailInboxPage.js src/components/DocumentsPage.js src/components/ActiveDealsPage.js src/components/DealsDashboard.js src/components/SettingsPage.js; code=$?; rm -f .a11y-eslintrc.json; exit $code</automated>
  </verify>
  <acceptance_criteria>
    Zero jsx-a11y/recommended violations in these files and in any straggler files discovered by the
    full-src scan (outside cluster A); behavior/appearance unchanged.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard/AT users → UI | Semantic/keyboard fixes govern assistive-tech reach across the deal portal + secondary pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-12 | Denial of Service (a11y access) | cluster-B interactive elements | low | mitigate | Convert click-only handlers to keyboard-reachable controls; scoped a11y lint proves zero violations in these files. No visible-focus indicator removed without replacement. |

No package installs this phase; no supply-chain checkpoint applies.
</threat_model>

<verification>
- Scoped jsx-a11y run over cluster-B files (+ discovered stragglers): zero violations
- `npm run lint` + `npm run build` green (global rule enabled in plan 12)
- Manual (phase verify): keyboard-navigate deal-portal tabs + secondary pages
</verification>

<success_criteria>
All jsx-a11y violations outside cluster A are fixed; combined with plan 08 the tree is ready for the
atomic lint flip.
</success_criteria>

<output>
Create `.planning/phases/07-ui-ux-copy-accessibility/07-09-SUMMARY.md` when done.
</output>
