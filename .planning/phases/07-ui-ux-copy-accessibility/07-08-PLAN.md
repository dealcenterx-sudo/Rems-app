---
phase: 07-ui-ux-copy-accessibility
plan: 08
type: execute
wave: 5
depends_on: ["07-07"]
files_modified:
  - src/components/CRMLeadDetailPage.js
  - src/components/PropertiesPage.js
  - src/components/NewDealPage.js
  - src/components/TasksPage.js
  - src/components/CRMLeadsPage.js
  - src/components/ContactsPage.js
autonomous: true
requirements: [A11Y-02]

must_haves:
  truths:
    - "The heaviest a11y-violation files (CRM/Properties/Deals/Tasks core) have zero jsx-a11y/recommended violations"
    - "Interactive elements are keyboard-reachable with semantic HTML; labels are associated with controls"
    - "Status conveyed by color also carries a text or icon cue (never color alone)"
  artifacts:
    - "jsx-a11y fixes across cluster-A files (~134 of 251 violations)"
    - "Non-color status cues on status pills/badges in these surfaces"
  key_links:
    - "Zero violations here is a precondition for the atomic lint flip (plan 12)"
---

<objective>
Fix the jsx-a11y/recommended violations in cluster A — the six heaviest files
(CRMLeadDetailPage 39, PropertiesPage 30, NewDealPage 24, TasksPage 23, CRMLeadsPage 10,
ContactsPage 8 = ~134 of the 251 total) — and apply the never-color-alone remediation (D-20) to
the status cues on these status-heavy surfaces. The rule is NOT yet enabled in package.json (that
atomic flip is plan 12); this plan clears the backlog in these files so the flip lands green.

Purpose: A11Y-02 (keyboard/semantic/label + color-alone). Split from cluster B (plan 09) by
disjoint files so both run in the same wave.
Output: Zero jsx-a11y violations in cluster-A files; non-color status cues.

## Artifacts this phase produces (this plan)
- label-has-associated-control, click-events-have-key-events, no-static-element-interactions,
  no-autofocus fixes in cluster-A files
- text/icon cues paired with color-coded status in these surfaces
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-ux-copy-accessibility/07-RESEARCH.md
@.planning/phases/07-ui-ux-copy-accessibility/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Label associations + semantic/keyboard fixes (cluster A)</name>
  <files>src/components/CRMLeadDetailPage.js, src/components/PropertiesPage.js, src/components/NewDealPage.js, src/components/CRMLeadsPage.js, src/components/ContactsPage.js</files>
  <read_first>07-RESEARCH.md "jsx-a11y violation surface" (rule breakdown + interpretation) and its exact reproduce/verify command.</read_first>
  <action>
    Clear jsx-a11y/recommended violations in these files. For label-has-associated-control: tie each
    label to its control via htmlFor/id or wrap the control. For click-events-have-key-events plus
    no-static-element-interactions: convert onClick on a div/span to a button where it is genuinely a
    button, or add role, tabIndex 0, and an equivalent onKeyDown (Enter/Space) where the element must
    stay non-button. For no-autofocus: remove autoFocus and rely on managed focus instead. Preserve
    existing behavior and visual appearance — these are structural/attribute changes, not redesigns.
    Do NOT change colors/hex here (token sweep is plans 10/11). Verify against the scoped a11y config,
    expecting zero violations in these files.
  </action>
  <verify>
    <automated>printf '{ "root": true, "extends": ["react-app", "plugin:jsx-a11y/recommended"] }' > .a11y-eslintrc.json; npx eslint --no-eslintrc --resolve-plugins-relative-to . -c .a11y-eslintrc.json --ext .js src/components/CRMLeadDetailPage.js src/components/PropertiesPage.js src/components/NewDealPage.js src/components/CRMLeadsPage.js src/components/ContactsPage.js; code=$?; rm -f .a11y-eslintrc.json; exit $code</automated>
  </verify>
  <acceptance_criteria>
    Zero jsx-a11y/recommended violations in these five files; behavior and appearance unchanged.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: TasksPage a11y + never-color-alone status cues (D-20)</name>
  <files>src/components/TasksPage.js</files>
  <read_first>07-RESEARCH.md a11y breakdown (TasksPage 23); 07-UI-SPEC.md "Never color-alone" row + Color reserved-list note.</read_first>
  <action>
    Clear TasksPage's jsx-a11y/recommended violations using the same fixes as Task 1 (label
    association, button-or-role+keydown, remove autoFocus). Additionally apply the never-color-alone
    rule (D-20): any status conveyed only by color (task complete/active/overdue pills, priority
    badges) MUST also carry a text label or icon cue so meaning is not color-dependent. Keep the
    optimistic toggle behavior from plan 07 intact. No color/hex changes here (token sweep is plans
    10/11) — add text/icon cues, not new colors.
  </action>
  <verify>
    <automated>printf '{ "root": true, "extends": ["react-app", "plugin:jsx-a11y/recommended"] }' > .a11y-eslintrc.json; npx eslint --no-eslintrc --resolve-plugins-relative-to . -c .a11y-eslintrc.json --ext .js src/components/TasksPage.js; code=$?; rm -f .a11y-eslintrc.json; exit $code</automated>
  </verify>
  <acceptance_criteria>
    Zero jsx-a11y/recommended violations in TasksPage; status pills/badges carry a text or icon cue in
    addition to color; optimistic toggle behavior intact.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard/AT users → UI | Semantic/keyboard fixes govern how assistive tech reaches controls |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-11 | Denial of Service (a11y access) | cluster-A interactive elements | low | mitigate | Convert click-only handlers to keyboard-reachable controls; scoped a11y lint proves zero violations. No visible-focus indicator is removed without replacement (focus ring handled in plan 12). |

No package installs this phase; no supply-chain checkpoint applies.
</threat_model>

<verification>
- Scoped jsx-a11y run over cluster-A files: zero violations
- `npm run lint` + `npm run build` green (rule not yet enabled globally until plan 12)
- Manual (phase verify): keyboard-navigate these pages; status cues legible without color
</verification>

<success_criteria>
Cluster-A files are jsx-a11y clean and status is never conveyed by color alone.
</success_criteria>

<output>
Create `.planning/phases/07-ui-ux-copy-accessibility/07-08-SUMMARY.md` when done.
</output>
