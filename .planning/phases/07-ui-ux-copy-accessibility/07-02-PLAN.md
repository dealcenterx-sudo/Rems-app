---
phase: 07-ui-ux-copy-accessibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.css
autonomous: true
requirements: [UI-05]

must_haves:
  truths:
    - "Every byte-identical token the migration needs is defined in :root before any component hex is remapped"
    - "Adding the token definitions changes zero rendered pixels (tokens have no consumers yet)"
  artifacts:
    - "src/App.css :root additions: --white, --text-muted-2, --skeleton-highlight, gray-ramp tokens"
  key_links:
    - "--skeleton-highlight consumed by Skeleton.js (plan 03)"
    - "--white/--text-muted-2/gray-ramp consumed by the token sweep (plans 10/11)"
---

<objective>
Token migration Pass 1, step A: add the byte-identical `:root` tokens the component hex sweep
and the Skeleton primitive will consume — WITHOUT changing any consumer yet (RC-01 binding).
This is a pure additive, provably zero-visual-change step: defining unused CSS variables cannot
alter computed styles.

Purpose: RESEARCH proved the three highest-frequency component hex values (`#ffffff` ×207,
`#888888` ×180, `#1a1a1a` ×150 = 537) have NO byte-identical existing token. Pass 1 is
therefore add-tokens-first. Defining them up front lets plan 03 (Skeleton) and plans 10/11
(sweep) reference real tokens.
Output: New exact-match tokens in `src/App.css :root`.

## Artifacts this phase produces (this plan)
- ★ src/App.css :root: --white:#ffffff, --text-muted-2:#888888, --skeleton-highlight:#1a1a1a, plus gray-ramp tokens
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-ux-copy-accessibility/07-CONTEXT.md
@.planning/phases/07-ui-ux-copy-accessibility/07-RESEARCH.md
@.planning/phases/07-ui-ux-copy-accessibility/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add byte-identical exact-match tokens to :root (RC-01)</name>
  <files>src/App.css</files>
  <read_first>07-CONTEXT.md RC-01 (binding); 07-RESEARCH.md "Hex→token migration surface" value-frequency table; 07-PATTERNS.md "App.css :root (Pass 1)".</read_first>
  <action>
    In the primary `:root` block (near the existing token definitions, App.css L19-81), ADD these
    exact-match tokens — values MUST equal the hex they will later replace, so computed styles are
    unchanged by construction: `--white: #ffffff`, `--text-muted-2: #888888`,
    `--skeleton-highlight: #1a1a1a`. Also add gray-ramp tokens for the recurring grays that lack an
    exact token today (from RESEARCH: `#111`/`#111111`, `#333`/`#333333`, `#444`/`#444444`,
    `#555`/`#555555`, `#666666`, and the `#ff4444` danger variant) using clear names (e.g.
    `--gray-111: #111111` … `--gray-666: #666666`, `--danger-alt: #ff4444`) — exact hex values only.
    Do NOT change any EXISTING token's value (no consolidation, no contrast fix — those are Pass 2 /
    plan 12). Do NOT add any consumer / touch any rule that reads these vars. This step only defines
    variables. Leave truly one-off decorative/chart values (`#aa00ff`, `#ff6600`, `#ff8800`,
    `#e0e0e0`) unaddressed here (they stay inline + documented in the sweep, D-16).
  </action>
  <verify>
    <automated>npm run build && npm run lint && grep -q -- "--skeleton-highlight" src/App.css && grep -q -- "--text-muted-2" src/App.css</automated>
  </verify>
  <acceptance_criteria>
    New exact-match tokens present in :root with byte-identical values; no existing token value
    changed; build + lint green. Because the tokens have no consumers yet, rendered output is
    identical (zero visual change).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | CSS token definitions only; no runtime data crosses a boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03 | Tampering | App.css token values | low | accept | Additive-only, byte-identical; build/lint gate catches syntax errors. No behavior change. |

No package installs this phase; no supply-chain checkpoint applies.
</threat_model>

<verification>
- `npm run build` + `npm run lint` green
- New tokens present; existing token values untouched (diff review: only additions)
</verification>

<success_criteria>
All byte-identical tokens the sweep and Skeleton need exist in :root, with zero visual change.
</success_criteria>

<output>
Create `.planning/phases/07-ui-ux-copy-accessibility/07-02-SUMMARY.md` when done.
</output>
