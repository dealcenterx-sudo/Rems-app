---
phase: 07-ui-ux-copy-accessibility
plan: 11
type: execute
wave: 6
depends_on: ["07-02", "07-09"]
files_modified:
  - src/components/CRMDashboard.js
  - src/components/DocumentsPage.js
  - src/components/CRMReportsPage.js
  - src/components/DealFinancialsTab.js
  - src/components/CRMCampaignsPage.js
  - src/components/DealsDashboard.js
  - src/components/ActiveDealsPage.js
  - src/components/LoginPage.js
  - src/components/CRMLeadDetailPage.js
  - src/components/SettingsPage.js
  - src/components/CRMEmailInboxPage.js
  - src/components/DealEditModal.js
  - src/components/HomePage.js
  - src/components/ContactsPage.js
  - src/components/MyDealsPage.js
  - src/components/DealPortalPage.js
autonomous: true
requirements: [UI-05]

must_haves:
  truths:
    - "Every remaining hex file (not in plan 10 or plan 05) is byte-identically tokenized"
    - "After plans 10 + 11, no byte-identical-mappable raw hex remains in components (only documented decorative one-offs)"
    - "Build + lint green with a byte-identical diff — Pass 1"
  artifacts:
    - "Hex→token migration of the remaining component files (byte-identical, Pass 1)"
  key_links:
    - "Consumes tokens from plan 02; excludes Loading/ConfirmModal/Toast (migrated in plan 05) and App.css (plan 12)"
    - "No token VALUE changes here — those are plan 12 (Pass 2)"
---

<objective>
Token migration Pass 1, component sweep (cluster B) — the remaining hex files not covered by plan 10
or plan 05. Same byte-identical discipline (D-16/D-17/RC-01): replace semantic hex with byte-identical
tokens, leave documented decorative one-offs inline, no value changes. The executor also greps the tree
to catch any remaining hex file not explicitly listed (excluding plan-10 files and the three shared
components from plan 05) and migrates it here, so after plans 10+11 the component tree holds no
byte-identical-mappable raw hex.

Purpose: UI-05 Pass 1, completing the component sweep.
Output: Byte-identical tokenization of all remaining component files.

## Artifacts this phase produces (this plan)
- var(--token) replacements across the remaining component files
- inline-but-documented notes on decorative/chart one-offs
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-ux-copy-accessibility/07-RESEARCH.md
@.planning/phases/07-ui-ux-copy-accessibility/07-PATTERNS.md
@.planning/phases/07-ui-ux-copy-accessibility/07-CONTEXT.md
@src/App.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Byte-identical sweep — CRM + Deals dashboards/pages</name>
  <files>src/components/CRMDashboard.js, src/components/DocumentsPage.js, src/components/CRMReportsPage.js, src/components/DealFinancialsTab.js, src/components/CRMCampaignsPage.js, src/components/DealsDashboard.js, src/components/CRMLeadDetailPage.js, src/components/CRMEmailInboxPage.js</files>
  <read_first>07-RESEARCH.md "Value frequency vs existing tokens" table; 07-CONTEXT.md RC-01.</read_first>
  <action>
    Apply the byte-identical hex→token mapping (same as plan 10 Task 1) across these files:
    existing-token values and the plan-02 added tokens only; no consolidation, no value change;
    decorative/chart one-offs (common in CRMReportsPage/CRMDashboard) stay inline and documented;
    alpha-suffixed variants handled per case preserving exact rendered color. Color-only diff; keep
    the plan-08/09 a11y changes intact.
  </action>
  <verify>
    <automated>npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    These files use tokens for all byte-identical semantic hex; decorative one-offs documented; build +
    lint green; byte-identical diff.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Byte-identical sweep — ActiveDeals/DealEditModal/DealPortal/Settings + LoginPage</name>
  <files>src/components/ActiveDealsPage.js, src/components/DealEditModal.js, src/components/DealPortalPage.js, src/components/SettingsPage.js, src/components/LoginPage.js</files>
  <read_first>07-RESEARCH.md value-frequency table; 07-CONTEXT.md RC-01.</read_first>
  <action>
    Apply the byte-identical mapping across these files. LoginPage keeps its plan-01 errorMessages
    wiring; this is a color-only pass. ActiveDealsPage keeps its plan-04/06/07/09 changes. No value
    changes, decorative one-offs documented, alpha variants preserved exactly.
  </action>
  <verify>
    <automated>npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    These files are byte-identically tokenized; build + lint green; byte-identical diff.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Byte-identical sweep — HomePage/Contacts/MyDeals + straggler grep</name>
  <files>src/components/HomePage.js, src/components/ContactsPage.js, src/components/MyDealsPage.js</files>
  <read_first>07-RESEARCH.md value-frequency table; the hex-file count (38 files) — enumerate remaining via grep.</read_first>
  <action>
    Apply the byte-identical mapping across HomePage, ContactsPage, MyDealsPage (keeping their plan-04/06
    empty/skeleton/SWR changes intact). Then grep the src tree for remaining raw hex literals in inline
    styles and migrate any file NOT already covered by plan 10, plan 05 (Loading/ConfirmModal/Toast), or
    the earlier tasks here — add each such straggler file to this plan's edits and tokenize it
    byte-identically. After this task, the only raw hex remaining in components should be documented
    decorative/chart one-offs. Do not touch App.css (Pass 2 is plan 12).
  </action>
  <verify>
    <automated>npm run build && npm run lint && npm run test:ci</automated>
  </verify>
  <acceptance_criteria>
    HomePage/Contacts/MyDeals plus any discovered straggler files are byte-identically tokenized; no
    byte-identical-mappable raw hex remains in components (only documented decorative one-offs); build +
    lint + test:ci green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Mechanical CSS-color source substitution; no runtime data boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-14 | Tampering | cluster-B color migration | low | mitigate | Byte-identical mapping only; build/lint gate + human diff-review guarantee zero rendered-color change (D-17). No value changes. |

No package installs this phase; no supply-chain checkpoint applies.
</threat_model>

<verification>
- `npm run build` + `npm run lint` + `npm run test:ci` green
- Human diff-review: every replaced hex equals its token's :root value (byte-identical, D-17)
- Straggler grep confirms no byte-identical-mappable raw hex remains in components
</verification>

<success_criteria>
All remaining component hex is byte-identically tokenized with zero visual change; Pass 1 is complete
across the codebase (components); only documented decorative one-offs remain inline.
</success_criteria>

<output>
Create `.planning/phases/07-ui-ux-copy-accessibility/07-11-SUMMARY.md` when done.
</output>
