---
phase: 07-ui-ux-copy-accessibility
plan: 06
type: execute
wave: 3
depends_on: ["07-03", "07-04"]
files_modified:
  - src/components/HomePage.js
  - src/components/ActiveDealsPage.js
  - src/components/CRMLeadsPage.js
  - src/components/ContactsPage.js
  - src/components/PropertiesPage.js
  - src/components/TasksPage.js
  - src/components/DocumentsPage.js
autonomous: true
requirements: [UI-02, UI-03]

must_haves:
  truths:
    - "Loads over ~400ms show skeletons that mirror the final layout with no layout shift"
    - "Sub-threshold loads go straight to content (no skeleton flash)"
    - "Home KPIs render instantly from cache and refresh silently in the background (SWR)"
  artifacts:
    - "Delayed <Skeleton> wiring in each skeleton page's loading branch"
    - "HomePage SWR: render-stale-then-refetch cache behavior"
  key_links:
    - "Skeleton + useDelayedFlag (plan 03) consumed by page loading branches"
    - "HomePage localStorage KPI cache extended in place (no new store)"
---

<objective>
Wire the delay-then-show skeleton (UI-02, D-09/D-10, 400ms) into each skeleton page's loading
branch so loads over the threshold mirror the final layout with no layout shift, and convert the
HomePage KPI cache to silent SWR (UI-03, D-11) — render cached KPIs instantly, refetch in the
background, swap in fresh numbers with no visible refresh indicator.

Purpose: Perceived-speed polish. Replaces inline spinners with layout-mirroring skeletons and
makes Home feel instant.
Output: Skeleton wiring on Home, Deals (ActiveDealsPage), CRM (CRMLeadsPage), Contacts,
Properties, Tasks, Documents; HomePage SWR cache behavior.

## Artifacts this phase produces (this plan)
- Per-page skeleton assemblies (composed from plan-03 shapes) reserving exact content dimensions
- HomePage render-stale-then-refetch SWR cache logic (extends existing localStorage cache)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-ux-copy-accessibility/07-UI-SPEC.md
@.planning/phases/07-ui-ux-copy-accessibility/07-PATTERNS.md
@src/components/Skeleton.js
@src/utils/useDelayedFlag.js
@src/components/HomePage.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delayed skeleton wiring on the list/dashboard pages (UI-02, D-09/D-10)</name>
  <files>src/components/ActiveDealsPage.js, src/components/CRMLeadsPage.js, src/components/ContactsPage.js, src/components/PropertiesPage.js, src/components/TasksPage.js, src/components/DocumentsPage.js</files>
  <read_first>07-PATTERNS.md "Skeleton.js" delay-then-show pattern + "Page empty/loading branches"; 07-UI-SPEC.md "<Skeleton>" + "Delay-then-show" (400ms).</read_first>
  <action>
    In each page's existing loading branch, replace the inline spinner with a delayed skeleton:
    gate with `useDelayedFlag(loading, 400)` so the skeleton mounts only after loading exceeds 400ms;
    sub-threshold loads render nothing then swap to content (no flash). Assemble the page's skeleton
    from the composed shapes (SkeletonText/Card/TableRow/KpiTile from plan 03) to mirror THAT page's
    real layout — each shape reserving the exact height/width of the content it stands in for so there
    is no layout shift on swap-in (D-10). Do not disturb the empty/error branches added in plan 04.
    Use tokens for any new inline style; no raw hex.
  </action>
  <verify>
    <automated>npm run lint && npm run build && grep -l "useDelayedFlag" src/components/TasksPage.js src/components/PropertiesPage.js</automated>
  </verify>
  <acceptance_criteria>
    Each listed page shows a layout-mirroring skeleton only after 400ms of loading; content swaps in
    without layout shift; lint + build green. (CLS is a manual-only phase-verify check.)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: HomePage silent SWR + cold-load KPI skeleton (UI-03, D-11)</name>
  <files>src/components/HomePage.js</files>
  <read_first>07-PATTERNS.md "HomePage.js — silent SWR" (readHomeKpiCache L14-24 discards stale); 07-RESEARCH.md HomePage KPI cache facts; 07-UI-SPEC.md "Home KPIs" note.</read_first>
  <action>
    Change the cache read so stale KPIs are RENDERED immediately rather than discarded (return the
    payload even when stale, or return `{ payload, isStale }`), then refetch in the background and swap
    in fresh numbers when ready — with NO visible refresh indicator and NO skeleton on a cached render
    (D-11). Show the KPI-tile skeleton (SkeletonKpiTile, delayed) ONLY on a true cold/no-cache first
    load. Keep the existing localStorage cache key, TTL, and scoping — do not add a new store (locked
    no-new-dependency). Preserve the cache payload shape `{stats,tasks}`.
  </action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    Home renders cached KPIs instantly (even when stale) and refreshes silently; cold load shows a
    delayed KPI skeleton; no new store added; lint + build green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage cache → UI | Cached KPI numbers render before refetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-08 | Information Disclosure | HomePage KPI cache | low | accept | Cache is per-scope (admin/uid) and unchanged in structure; SWR only alters discard timing, not scoping. No cross-user leakage introduced. |

No package installs this phase; no supply-chain checkpoint applies.
</threat_model>

<verification>
- `npm run lint` + `npm run build` green
- Manual (phase verify): throttled load shows skeleton >400ms with no CLS; Home KPIs instant + silent refresh
</verification>

<success_criteria>
Skeleton pages show layout-mirroring, delayed, no-CLS skeletons; Home KPIs are instant with silent
background refresh.
</success_criteria>

<output>
Create `.planning/phases/07-ui-ux-copy-accessibility/07-06-SUMMARY.md` when done.
</output>
