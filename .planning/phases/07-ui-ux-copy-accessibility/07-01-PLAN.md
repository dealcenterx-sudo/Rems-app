---
phase: 07-ui-ux-copy-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/COPY-STANDARD.md
  - src/utils/errorMessages.js
  - src/utils/errorMessages.test.js
  - src/components/LoginPage.js
autonomous: true
requirements: [COPY-01, COPY-02]

must_haves:
  truths:
    - "A one-page copy standard exists on disk BEFORE any copy sweep begins (Success Criterion 1)"
    - "A single central util maps Firebase/API error codes to { message, recovery } and never leaks raw SDK detail"
    - "LoginPage's auth error UX still works, now sourced from the central map"
  artifacts:
    - docs/COPY-STANDARD.md
    - src/utils/errorMessages.js
    - src/utils/errorMessages.test.js
  key_links:
    - "LoginPage.friendlyAuthError replaced by errorMessages.mapError — auth flow must keep working"
    - "errorMessages fallback returns a safe generic, not err.message (security gate)"
---

<objective>
Establish the two copy foundations the rest of Phase 7 consumes: (1) a one-page copy
standard (COPY-01) that MUST exist before any copy is rewritten, and (2) a single central
error map `src/utils/errorMessages.js` (COPY-02, per D-05) that generalizes the existing
`friendlyAuthError` into `code → { message, recovery }`, unit-tested, with a
security-safe fallback that never echoes raw SDK error detail.

Purpose: Downstream plans (empty/error states, optimistic-toggle failure toasts,
destructive confirmations) all depend on this standard + map. It must land first.
Output: Copy standard doc, error map util + test, LoginPage repointed to the map.

## Artifacts this phase produces (ownership: this plan creates the ones marked ★)
- ★ docs/COPY-STANDARD.md — one-page copy standard (COPY-01)
- ★ src/utils/errorMessages.js — central `code → { message, recovery }` map (COPY-02, D-05)
- ★ src/utils/errorMessages.test.js — unit test
- src/components/Skeleton.js (+ composed shapes) — plan 03
- src/utils/useDelayedFlag.js — plan 03
- src/utils/useFocusTrap.js (+ test) — plan 03
- LoadingButton pendingLabel prop, Toast error composition — plan 05
- new :root tokens (--white, --text-muted-2, --skeleton-highlight, gray ramp) — plan 02
- token Pass-2 contrast/focus-ring changes — plan 12
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-ux-copy-accessibility/07-UI-SPEC.md
@.planning/phases/07-ui-ux-copy-accessibility/07-PATTERNS.md
@.planning/phases/07-ui-ux-copy-accessibility/07-CONTEXT.md
@src/components/LoginPage.js
@src/utils/permissions.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the one-page copy standard (COPY-01)</name>
  <files>docs/COPY-STANDARD.md</files>
  <read_first>07-UI-SPEC.md "Copywriting Contract" section; 07-CONTEXT.md D-01/D-03/D-06.</read_first>
  <action>
    Write a single-page copy standard capturing the locked voice (D-01: minimal, factual,
    enterprise-austere — no marketing warmth). Codify: sentence case everywhere; buttons are
    verb+object (e.g. "New deal", "Add contact", "Save changes"); one term per concept (list the
    canonical terms: deal, lead, contact, property, task, document); the error-copy format
    ({plain problem sentence} + {one imperative recovery step}); empty-state variant contract
    (first-use vs no-results vs error, per the UI-SPEC table); destructive-confirmation rule
    (state object + real irreversible consequence, verified against handler behavior per D-07);
    and a short "no fluff" list of banned patterns. Keep it to one page. This is the reference
    every later copy task cites.
  </action>
  <verify>
    <automated>test -f docs/COPY-STANDARD.md && grep -qi "sentence case" docs/COPY-STANDARD.md && grep -qi "recovery" docs/COPY-STANDARD.md</automated>
  </verify>
  <acceptance_criteria>
    docs/COPY-STANDARD.md exists, is one page, and states voice, button rule, one-term-per-concept,
    error format, empty-state variants, and destructive-copy rule (D-07). No copy sweep has begun.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create central errorMessages.js map + test (COPY-02, D-05)</name>
  <files>src/utils/errorMessages.js, src/utils/errorMessages.test.js</files>
  <read_first>07-PATTERNS.md "errorMessages.js" section (generalizes friendlyAuthError, LoginPage L17-41); permissions.test.js test idiom; 07-RESEARCH.md "Security Domain" (V5/V7 no-leak rule).</read_first>
  <behavior>
    - Known auth code (e.g. auth/invalid-credential) returns an object with a non-empty human message and a non-empty recovery step.
    - Known Firestore/API code the live flows surface (permission-denied, unavailable, not-found, deadline-exceeded) returns a curated { message, recovery }.
    - An unknown/undefined code returns the safe generic { message, recovery } — and when the input error carries a distinctive raw message string, that raw string does NOT appear anywhere in the returned object (no leak of SDK detail, no codes, no stack).
  </behavior>
  <action>
    Create `src/utils/errorMessages.js` following the pure-util named-export convention
    (arrow fns, JSDoc, like permissions.js). Export a map plus a `mapError(err) => { message, recovery }`
    accessor (Claude's discretion on internal shape; may also export a `toToastString(err)` helper
    that composes message and recovery into one string for Toast call sites). Migrate the 7 auth/*
    codes currently in `friendlyAuthError`, plus the Firestore/API codes the live flows surface
    (permission-denied, unavailable, not-found, deadline-exceeded — cover what flows actually surface).
    CRITICAL security fix: the fallback for unknown codes MUST return a curated safe generic; it MUST
    NOT return the raw SDK message field the analog leaks. `message` = one plain sentence naming the
    problem (no codes, no stack, no security detail). `recovery` = one imperative next step.
    Write `errorMessages.test.js` mirroring the permissions.test.js idiom, asserting the three
    behaviors above (feed an error whose raw message is a distinctive sentinel string and assert
    it is absent from the mapped output).
  </action>
  <verify>
    <automated>npx react-scripts test --watchAll=false src/utils/errorMessages.test.js && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    errorMessages.js exports a working map/accessor returning { message, recovery }; the test passes
    including the unknown-code safe-fallback case proving the distinctive raw message is not echoed.
    Lint green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Repoint LoginPage to the central map (keep auth working)</name>
  <files>src/components/LoginPage.js</files>
  <read_first>LoginPage.js L16-41 (friendlyAuthError + its call sites).</read_first>
  <action>
    Replace the local `friendlyAuthError` usage with `errorMessages` (import and call `mapError`/
    `toToastString`). Preserve the exact user-facing behavior of the login and signup error paths —
    do NOT break Google sign-in or email/password auth (CLAUDE.md critical rule). If a single
    displayed string is expected, compose message and recovery at the call site. Remove the now-dead
    local `friendlyAuthError` only if fully replaced. Do not touch LoginPage's hardcoded hex here —
    token migration of this file happens in a later plan.
  </action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    LoginPage compiles, lints, and builds; auth error messages now come from errorMessages.js;
    no auth flow regressed (manual sign-in smoke deferred to phase verify).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK error → UI string | Firebase/API error objects cross into user-visible copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Information Disclosure | errorMessages.js fallback | high | mitigate | Fallback returns curated generic; never echoes raw SDK message/code/stack. Proven by the unknown-code test asserting a distinctive raw message is absent from output. |
| T-07-02 | Tampering | copy strings | low | accept | All copy is static JSX text (React auto-escapes); no dangerouslySetInnerHTML introduced. |

No package installs occur in this phase, so no supply-chain (T-07-SC) checkpoint applies here.
</threat_model>

<verification>
- `npx react-scripts test --watchAll=false src/utils/errorMessages.test.js` green
- `npm run lint` + `npm run build` green
- docs/COPY-STANDARD.md present before any downstream copy sweep
</verification>

<success_criteria>
Copy standard authored; central error map exists, is tested, and is leak-safe; LoginPage consumes
it with auth flows intact.
</success_criteria>

<output>
Create `.planning/phases/07-ui-ux-copy-accessibility/07-01-SUMMARY.md` when done.
</output>
