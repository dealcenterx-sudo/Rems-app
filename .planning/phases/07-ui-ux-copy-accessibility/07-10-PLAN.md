---
phase: 07-ui-ux-copy-accessibility
plan: 10
type: execute
wave: 6
depends_on: ["07-02", "07-08"]
files_modified:
  - src/components/PropertiesPage.js
  - src/components/AnalyticsDashboard.js
  - src/components/TasksPage.js
  - src/components/CRMLeadsPage.js
  - src/components/NewDealPage.js
  - src/components/LeadDrawer.js
  - src/components/DealDocumentsTab.js
  - src/components/DealProgressTab.js
  - src/components/DealPartiesTab.js
  - src/components/DealChatTab.js
autonomous: true
requirements: [UI-05]

must_haves:
  truths:
    - "Every semantic hex in the heavy cluster maps to a byte-identical token; computed styles are unchanged by construction"
    - "Truly one-off decorative/chart colors are left inline but documented (D-16)"
    - "Build + lint green with a byte-identical diff (no rendered-color change) — Pass 1"
  artifacts:
    - "Hex→token migration of the 10 heaviest hex files (byte-identical, Pass 1)"
  key_links:
    - "Consumes tokens defined in plan 02 (--white, --text-muted-2, --skeleton-highlight, gray ramp, existing tokens)"
    - "No token VALUE changes here — those are plan 12 (Pass 2)"
---

<objective>
Token migration Pass 1, component sweep (cluster A) — the 10 heaviest hex files. Replace each
semantic hex literal with a token whose value is BYTE-IDENTICAL, so computed styles are unchanged
by construction (D-16/D-17/RC-01). No token value changes; no consolidation; decorative/chart
one-offs stay inline but documented. This is a mechanical, diff-reviewable, zero-visual-change pass.

Purpose: UI-05 Pass 1 across the highest-volume files. Runs after feature + a11y edits so the sweep
sees final code and produces a pure color-only diff.
Output: Byte-identical tokenization of cluster-A files.

## Artifacts this phase produces (this plan)
- var(--token) replacements for byte-identical hex across the 10 listed files
- inline-but-documented notes on any decorative/chart one-offs left in place
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-ux-copy-accessibility/07-RESEARCH.md
@.planning/phases/07-ui-ux-copy-accessibility/07-PATTERNS.md
@.planning/phases/07-ui-ux-copy-accessibility/07-CONTEXT.md
@src/App.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Byte-identical hex→token sweep — Properties/Analytics/Tasks/CRMLeads/NewDeal</name>
  <files>src/components/PropertiesPage.js, src/components/AnalyticsDashboard.js, src/components/TasksPage.js, src/components/CRMLeadsPage.js, src/components/NewDealPage.js</files>
  <read_first>07-RESEARCH.md "Value frequency vs existing tokens" table (the byte-identical mapping); 07-CONTEXT.md RC-01; 07-PATTERNS.md "App.css :root (Pass 1)" mapping list.</read_first>
  <action>
    In each file, replace hex literals in inline styles with the byte-identical token per the RESEARCH
    mapping: existing-token values (#00ff88 to var(--accent), #0088ff to var(--info), #0f0f0f to
    var(--surface-2), #0a0a0a to var(--surface-1), #ffaa00 to var(--warning), #000000/#000 to
    var(--surface-0), #ff3333 to var(--danger), #2a2a2a to var(--border-strong), #8a8a8a to
    var(--text-muted)); and the plan-02 added tokens (#ffffff/#fff to var(--white), #888888/#888 to
    var(--text-muted-2), #1a1a1a to var(--skeleton-highlight), and the gray-ramp #111/#333/#444/#555/
    #666666 and #ff4444 to their new ramp tokens). Do NOT consolidate to a near-but-different token
    (no #ffffff to --text-primary — that is a Pass-2 value change). Leave truly one-off decorative/
    chart colors (#aa00ff, #ff6600, #ff8800, #e0e0e0, single chart-series colors — common in
    AnalyticsDashboard) inline but add a brief code comment marking them as intentional decorative
    values (D-16). Handle alpha-suffixed variants (e.g. `${color}20`, `#00ff8820`) per case: token +
    separate alpha or a soft token that preserves the exact rendered color; if a value cannot be made
    byte-identical, leave it and note it for Pass 2. The diff MUST be color-neutral (each replaced
    hex equals its token's :root value).
  </action>
  <verify>
    <automated>npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    These five files use tokens for all byte-identical semantic hex; decorative one-offs documented
    inline; build + lint green; diff is byte-identical (human diff-review confirms zero color change).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Byte-identical hex→token sweep — LeadDrawer + deal-portal tabs</name>
  <files>src/components/LeadDrawer.js, src/components/DealDocumentsTab.js, src/components/DealProgressTab.js, src/components/DealPartiesTab.js, src/components/DealChatTab.js</files>
  <read_first>07-RESEARCH.md "Value frequency vs existing tokens" table; 07-CONTEXT.md RC-01.</read_first>
  <action>
    Same byte-identical mapping as Task 1 across these five deal-portal files. Existing-token values
    and plan-02 added tokens only; no consolidation, no value change; decorative one-offs stay inline
    and documented; alpha-suffixed variants handled per case preserving exact rendered color. Keep the
    plan-09 a11y changes intact — this is a color-only diff.
  </action>
  <verify>
    <automated>npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    These five files use tokens for all byte-identical semantic hex; decorative one-offs documented;
    build + lint green; byte-identical diff.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Mechanical CSS-color source substitution; no runtime data boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-13 | Tampering | cluster-A color migration | low | mitigate | Byte-identical mapping only; build/lint gate + human diff-review guarantee zero rendered-color change (D-17). No value changes. |

No package installs this phase; no supply-chain checkpoint applies.
</threat_model>

<verification>
- `npm run build` + `npm run lint` green
- Human diff-review: every replaced hex equals its token's :root value (byte-identical, D-17)
</verification>

<success_criteria>
The 10 heaviest hex files are byte-identically tokenized with zero visual change; decorative one-offs
documented.
</success_criteria>

<output>
Create `.planning/phases/07-ui-ux-copy-accessibility/07-10-SUMMARY.md` when done.
</output>
