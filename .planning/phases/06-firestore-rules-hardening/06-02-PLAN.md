---
phase: 06-firestore-rules-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/TRUST_BOUNDARIES.md
autonomous: true
requirements: [SEC-05]
must_haves:
  truths:
    - "A reviewer can read a per-collection Firestore access matrix (Read | Create | Update | Delete | Why) in docs/TRUST_BOUNDARIES.md that matches the tested firestore.rules (SEC-05)"
    - "The matrix explicitly states activity_log is append-only against everyone INCLUDING admin (nobody can update/delete) — matching the SEC-04 catch-all removal"
    - "The extension is additive: the existing api/*.js rows and the two special auth postures are untouched, so tests/api/trust-boundaries-audit.test.mjs stays green"
  artifacts:
    - docs/TRUST_BOUNDARIES.md
  key_links:
    - "The matrix is derived from the tested rules (firestore.rules) — the doc and the emulator suite are the same access model expressed two ways"
    - "TRUST_BOUNDARIES.md is the established access-model doc (Phase 4 made its coverage machine-checked); extending the Firestore section keeps a single source of truth rather than a new standalone doc"
---

<objective>
Land SEC-05: extend the existing `## Firestore Rules` section of `docs/TRUST_BOUNDARIES.md` (today a thin 5-bullet summary at lines 37-45) with a per-collection access matrix — who can Read / Create / Update / Delete each collection, and why — that matches the tested `firestore.rules`. This is the "documented access-model assumptions matching the tested rules" deliverable.

Purpose: A reviewer must be able to read the access model and see it corresponds to what the emulator suite enforces. The matrix makes the append-only-against-admin guarantee (the SEC-04 fix) explicit in prose, and documents the assignment-array and canAccessDeal() inheritance model. Keeping it inside TRUST_BOUNDARIES.md (rather than a new file) preserves a single source of truth; the Phase-4 completeness audit test only pins the api/*.js rows, so this Firestore-section extension is additive and cannot break it.
Output: an extended `docs/TRUST_BOUNDARIES.md` Firestore Rules section containing the access matrix.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-firestore-rules-hardening/06-RESEARCH.md
@.planning/phases/06-firestore-rules-hardening/06-PATTERNS.md
@docs/TRUST_BOUNDARIES.md
@firestore.rules
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02 — the SEC-05 access-model doc)

- **`docs/TRUST_BOUNDARIES.md`** *(modified)* — the `## Firestore Rules` section (currently 5 bullets at L37-45) is extended with a per-collection access matrix (`Collection | Read | Create | Update | Delete | Why`) derived from the tested firestore.rules, including the explicit activity_log append-only-against-admin row. The existing 5 intro bullets and every `api/*.js` row/auth-posture line elsewhere in the doc are preserved unchanged.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Extend the TRUST_BOUNDARIES.md Firestore Rules section with the per-collection access matrix</name>
  <files>docs/TRUST_BOUNDARIES.md</files>
  <read_first>
    - .planning/phases/06-firestore-rules-hardening/06-PATTERNS.md (docs/TRUST_BOUNDARIES.md section — EXTEND the Firestore Rules section; keep the 5 bullets as intro then add the matrix; copy the pipe-table idiom from the serverless endpoint table at L26-33; the activity_log row wording at PATTERNS L136; the audit-test-awareness note L140 — do NOT rename/remove any api/<name>.js row)
    - .planning/phases/06-firestore-rules-hardening/06-RESEARCH.md (SEC-05 access-model matrix draft, lines 149-159 — the Read/Create/Update/Delete/Why rows per collection; the recommended home is expanding TRUST_BOUNDARIES.md)
    - docs/TRUST_BOUNDARIES.md lines 37-45 (current Firestore Rules section to extend) and lines 26-33 (the serverless table style to match)
    - firestore.rules (the source of truth the matrix must match — users L35-56, contacts/leads/documents/campaigns, deals L72-85, deal-* portal L105-136, properties L138-153, tasks L161-169, notifications L182-186, activity_log L191-195, companies L198-204)
  </read_first>
  <action>
    In docs/TRUST_BOUNDARIES.md, keep the existing five intro bullets under `## Firestore Rules` (L41-45) and append a per-collection access matrix as a markdown pipe table with header `Collection | Read | Create | Update | Delete | Why`, matching the table idiom already used by the serverless endpoint table (L26-33). Populate one row per collection group, derived from the tested firestore.rules and the RESEARCH draft (L149-159): users (self/admin; never self-elevate role); userId-scoped business records (contacts/leads/documents/campaigns); deals (owner/admin/assigned-read-only); the six deal-* portal collections (canAccessDeal inheritance; messages & lender-pushes update/delete admin-only); properties (assignedProperties grants read+update, not delete); tasks (assignee can act); notifications (any signed-in may create stamped as actor; recipient/admin read/clear); activity_log; and companies (membership-scoped). Make the activity_log row state Read=admin, Create=signed-in as self, Update=nobody (incl. admin), Delete=nobody (incl. admin), Why=append-only audit trail, tamper-evident after SEC-04 removed the admin catch-all. Keep prose declarative and secret-free — collection/field names only, no secret values, no admin email literal. Do NOT alter any `api/<name>.js` row or the two special auth-posture lines (L26-35) that the audit test pins.
  </action>
  <verify>
    <automated>grep -q '| Collection | Read | Create | Update | Delete | Why |' docs/TRUST_BOUNDARIES.md && grep -qi 'nobody (incl. admin)' docs/TRUST_BOUNDARIES.md && grep -q 'delete-media.js' docs/TRUST_BOUNDARIES.md && npm run test:api 2>&1 | tail -15 | grep -qiE 'pass' && echo DOC_MATRIX_OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/TRUST_BOUNDARIES.md contains a `Collection | Read | Create | Update | Delete | Why` table under the `## Firestore Rules` section
    - The activity_log row states update/delete is denied to everyone including admin (grep finds "nobody (incl. admin)")
    - Every collection group from firestore.rules appears as a row; the matrix matches the tested rules (userId scoping, assignment arrays, canAccessDeal inheritance, append-only activity_log)
    - `npm run test:api` still passes (the trust-boundaries-audit test is green — the api/*.js rows and auth-posture lines were not modified; `delete-media.js` row still present)
    - No secret values and no admin email literal appear in the doc
  </acceptance_criteria>
  <done>A reviewer can read a per-collection Firestore access matrix in TRUST_BOUNDARIES.md that matches the tested rules, and the existing serverless audit test still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → committed docs | Documentation must describe controls without leaking secret values or the admin email literal |
| documented model → tested rules | The doc is only trustworthy if it matches firestore.rules; drift would misrepresent the security posture |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06D-01 | Information Disclosure | secrets/admin-email in docs | medium | mitigate | Task 1 uses collection/field names only; acceptance criterion asserts no secret values and no admin email literal appear; matches the rest of the doc's secret-free convention |
| T-06D-02 | Repudiation | doc/rules drift misrepresenting access | medium | mitigate | The matrix is derived from the tested firestore.rules; `npm run test:api` (audit test) and the 06-01 rules suite keep doc and enforcement in lockstep |
| T-06D-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs NO packages (RESEARCH Package Legitimacy Audit: zero installs) — no supply-chain surface |
</threat_model>

<verification>
- Task 1: the access matrix exists under `## Firestore Rules`; the activity_log row states append-only-against-admin; every collection group is represented; `npm run test:api` stays green (api rows untouched)
- The matrix is cross-checked against firestore.rules — same access model expressed as prose and as tested rules
</verification>

<success_criteria>
docs/TRUST_BOUNDARIES.md documents the full per-collection Firestore access model matching the tested rules (SEC-05), additively — the existing serverless audit test remains green.
</success_criteria>

<output>
Create `.planning/phases/06-firestore-rules-hardening/06-02-SUMMARY.md` when done.
</output>
