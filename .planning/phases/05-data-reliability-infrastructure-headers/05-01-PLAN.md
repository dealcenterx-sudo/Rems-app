---
phase: 05-data-reliability-infrastructure-headers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/AnalyticsDashboard.test.js
autonomous: true
requirements: [DATA-01, DATA-02]
must_haves:
  truths:
    - "When AnalyticsDashboard's userId-scoped query throws a missing-index error, the fallback path calls captureError with feature 'analytics-index-fallback' (loud, not silent) — DATA-02"
    - "The fallback still returns data after reporting: the equality-only refetch runs and results render (graceful degradation preserved)"
    - "Every non-admin where('userId')+orderBy query site is covered by an existing composite def in firestore.indexes.json (code/doc half of DATA-01 confirmed complete — no new def needed)"
  artifacts:
    - src/components/AnalyticsDashboard.test.js
    - firestore.indexes.json
  key_links:
    - "AnalyticsDashboard.js:65 captureError(feature:'analytics-index-fallback') is imported from src/utils/observability.js — the same DSN-gated Sentry bridge Phase 3 verified live"
    - "The test mocks the LOCAL ../utils/observability module (never @sentry/react) — auto-mock breaks on the Sentry export shape"
---

<objective>
Close the DATA-02 test gap and reconcile the DATA-01 index-coverage claim — the locally-verifiable half of data reliability. The missing-index fallback in AnalyticsDashboard already reports to Sentry via captureError (shipped in dd6364a), but NO test asserts it. This plan adds the net-new unit test proving the fallback is loud, and confirms (verify-only, no edit) that the 15 shipped composite index defs in firestore.indexes.json cover every non-admin where('userId')+orderBy query site.

Purpose: Make DATA-02 machine-checked under `npm run test:ci`, and pin the DATA-01 code/doc half as complete so only the live Console "READY" state remains (Plan 03).
Output: `src/components/AnalyticsDashboard.test.js` (net-new); a confirmed index-coverage reconciliation recorded in the SUMMARY.

The index READY state in the Firebase Console and the non-admin production smoke are the LIVE half (Plan 03) — not observable here.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-data-reliability-infrastructure-headers/05-RESEARCH.md
@.planning/phases/05-data-reliability-infrastructure-headers/05-PATTERNS.md
@.planning/phases/05-data-reliability-infrastructure-headers/05-VALIDATION.md
@src/components/AnalyticsDashboard.js
@src/components/ErrorBoundary.test.js
@src/utils/observability.test.js
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01 — DATA local half)

- **`src/components/AnalyticsDashboard.test.js`** *(net-new)* — unit test asserting the missing-index fallback calls `captureError({ feature: 'analytics-index-fallback', collectionName: 'deals' })` and still returns data via the equality-only refetch (DATA-02).
- **`firestore.indexes.json`** *(verify-only — NO edit)* — reconciled: all 15 composite defs confirmed to cover every non-admin `where('userId')+orderBy` query site (DATA-01 code/doc half). Recorded in SUMMARY.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Net-new AnalyticsDashboard missing-index fallback test (DATA-02)</name>
  <files>src/components/AnalyticsDashboard.test.js</files>
  <read_first>
    - src/components/AnalyticsDashboard.js:46-86 (loadCollectionInRange — first getDocs at :60, catch guard at :63, captureError call at :65-71, equality-only refetch at :73) and :110-133 (loadAllData builds userFilter at :115 via isAdminUser, useEffect load trigger at :131-133)
    - src/components/ErrorBoundary.test.js:1-10,28-34 (mock-the-observability-bridge factory + assert-on-captureError.mock.calls pattern — copy verbatim shape)
    - src/utils/observability.test.js:1-12 (DO-NOT-auto-mock @sentry note — explicit factory only)
    - src/components/Toast.js (ToastProvider named export at :44 — component calls useToast() at AnalyticsDashboard.js:10, so render must be wrapped)
    - .planning/phases/05-data-reliability-infrastructure-headers/05-PATTERNS.md (Pattern Assignments — the exact mock list and assertions for this test)
  </read_first>
  <behavior>
    - When the first getDocs rejects with an error whose code is 'failed-precondition', captureError is called exactly once with a context object matching { feature: 'analytics-index-fallback', collectionName: 'deals' }
    - The userId-scoped branch is exercised (userScoped true): isAdminUser mocked to return false and auth.currentUser.uid present
    - After the fallback reports, the second (equality-only) getDocs is invoked and the component still resolves data (assert via waitFor, since load runs in useEffect)
  </behavior>
  <action>
    Create src/components/AnalyticsDashboard.test.js. Mock the LOCAL bridge with jest.mock('../utils/observability', () => ({ captureError: jest.fn() })) — never auto-mock @sentry/react (breaks on export shape). Additionally mock: firebase/firestore so collection/query/where/orderBy are identity/no-op stubs and getDocs is a jest.fn whose FIRST call rejects with an error carrying code 'failed-precondition' (or a message matching /index/i) and whose SECOND call resolves to { docs: [{ id: 'd1', data: () => ({ createdAt: new Date().toISOString(), status: 'active' }) }] }; ../firebase to export db (any object) and auth ({ currentUser: { uid: 'test-uid' } }); ../utils/helpers with isAdminUser: jest.fn(() => false) to force the userId-scoped fallback branch; optionally stub recharts chart components to () => null to keep render light. Render <ToastProvider><AnalyticsDashboard /></ToastProvider> from @testing-library/react. Use await waitFor to let the useEffect load settle, then assert captureError was called once and destructure captureError.mock.calls[0] to assert the context matches expect.objectContaining({ feature: 'analytics-index-fallback', collectionName: 'deals' }), and assert the second getDocs call occurred (fallback returned data). Follow the ErrorBoundary.test.js structure exactly.
  </action>
  <verify>
    <automated>CI=true npx react-scripts test --watchAll=false src/components/AnalyticsDashboard.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `CI=true npx react-scripts test --watchAll=false src/components/AnalyticsDashboard.test.js` exits 0 with at least one passing test
    - The test asserts captureError was called with expect.objectContaining({ feature: 'analytics-index-fallback', collectionName: 'deals' })
    - The test asserts the equality-only refetch ran (second getDocs invoked) and data still resolves
    - No auto-mock of @sentry/react is used; only ../utils/observability is factory-mocked
    - `npm run test:ci` still passes (no regression to the existing suite)
  </acceptance_criteria>
  <done>DATA-02 is machine-checked: the AnalyticsDashboard missing-index fallback is proven loud (calls captureError with the analytics-index-fallback tag) and still graceful (returns data via the equality-only refetch).</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the DATA-01 index-coverage claim (verify-only, no edit)</name>
  <files>firestore.indexes.json</files>
  <read_first>
    - firestore.indexes.json (the 15 composite defs)
    - .planning/phases/05-data-reliability-infrastructure-headers/05-RESEARCH.md (DATA-01 Query-Site → Index Coverage Cross-Reference — 13 index-relevant query sites, all covered; the "do NOT need a composite index" list)
    - docs/FIRESTORE_INDEXES.md (documents the composite indexes)
  </read_first>
  <action>
    Verify-only — do NOT edit firestore.indexes.json. Re-run the coverage cross-reference from RESEARCH.md: grep src/components for `where('userId'` and `orderBy(` construction sites, confirm each non-admin where+orderBy query maps to an existing def in firestore.indexes.json (deals/properties/contacts/documents/leads/tasks + deal-documents/deal-messages), and confirm the equality-only (no-orderBy) query sites correctly need no composite index. Record the reconciliation result in the SUMMARY: the DATA-01 code/doc half is COMPLETE (15 defs cover the full non-admin query surface; no def to add). Flag any newly-introduced query site not covered — none is expected. The remaining DATA-01 work (indexes READY in Console) is the LIVE half handled in Plan 03.
  </action>
  <verify>
    <automated>node -e "const j=require('./firestore.indexes.json'); if(!Array.isArray(j.indexes)||j.indexes.length<13){process.exit(1)} console.log('INDEX_DEFS',j.indexes.length)" && grep -q 'userId' firestore.indexes.json && echo INDEX_COVERAGE_RECONCILED</automated>
  </verify>
  <acceptance_criteria>
    - firestore.indexes.json parses and contains at least 13 composite index defs (15 shipped)
    - The SUMMARY records that every non-admin where('userId')+orderBy query site maps to an existing def (DATA-01 code/doc half COMPLETE)
    - No new index def is added and firestore.indexes.json is not edited
    - Any query site not covered by a def is flagged (none expected)
  </acceptance_criteria>
  <done>The DATA-01 code/doc half is confirmed complete: the shipped composite defs fully cover the current non-admin query surface, leaving only the live Console READY state for Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-admin client → Firestore query planner | A userId-scoped where+orderBy query crosses to Firestore; a missing composite index returns failed-precondition, triggering the client fallback |
| client fallback → Sentry (observability bridge) | captureError sends error context to Sentry; context must not leak secrets (only feature tag + collection name + boolean flags) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05A-01 | Information Disclosure | AnalyticsDashboard fallback → Sentry captureError | low | accept | Context is limited to feature tag, collectionName, and boolean bounds/scope flags — no userId value, no query data; the test asserts exactly this shape |
| T-05A-02 | Information Disclosure | missing-index fallback re-query scope | high | mitigate | The equality-only refetch reuses the SAME userId filter (userScoped preserved) and sorts client-side — scope is never broadened; verified by the test forcing isAdminUser=false |
| T-05A-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs NO packages (RESEARCH Package Legitimacy Audit: zero new installs) — no supply-chain surface |
</threat_model>

<verification>
- Task 1: `CI=true npx react-scripts test --watchAll=false src/components/AnalyticsDashboard.test.js` passes; `npm run test:ci` green (no regression)
- Task 2: firestore.indexes.json parses with ≥13 defs; coverage reconciliation recorded in SUMMARY (no edit)
- `npm run lint` clean
</verification>

<success_criteria>
DATA-02 is proven by a passing unit test (fallback calls captureError with the analytics-index-fallback tag and still returns data), and the DATA-01 code/doc half is confirmed complete (15 defs cover the full non-admin query surface). The live index-READY state and non-admin smoke remain for Plan 03.
</success_criteria>

<output>
Create `.planning/phases/05-data-reliability-infrastructure-headers/05-01-SUMMARY.md` when done.
</output>