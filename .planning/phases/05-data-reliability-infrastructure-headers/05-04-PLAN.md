---
phase: 05-data-reliability-infrastructure-headers
plan: 04
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03]
files_modified:
  - docs/SAAS_UPGRADE_CHANGELOG.md
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, INFRA-02, INFRA-03]
must_haves:
  truths:
    - "docs/SAAS_UPGRADE_CHANGELOG.md carries a Phase 5 entry recording the net-new AnalyticsDashboard.test.js, the package.json build-script edit (INLINE_RUNTIME_CHUNK=false), and the vercel.json CSP allowlist additions"
    - "The entry records the automatable results (DATA-02 test green, INFRA-02/03 config landed, DATA-01 coverage reconciled) and the live-half statuses from Plan 03 (indexes READY, non-admin smoke, CSP reports — approved or deferred)"
    - "Env vars are referenced by NAME only (SENTRY_DSN, REACT_APP_SENTRY_DSN); no secret values appear"
  artifacts:
    - docs/SAAS_UPGRADE_CHANGELOG.md
  key_links:
    - "The changelog is the AUDIT-03 standing task maintained by every phase; Plan 04 records the full Phase 5 outcome after Plans 01-03 have run"
    - "Records the INFRA-01 groundwork status: CSP is Report-Only this phase; enforcement completes in Phase 8 after a clean soak"
---

<objective>
Record the Phase 5 outcome in the running changelog — the AUDIT-03 standing task, mirroring the Phase 4 entry. This closes the phase by capturing the automatable halves (DATA-02 test, INFRA-03 build-script + CSP edits, INFRA-02 + DATA-01 reconciles) and the live-half statuses from Plan 03 (indexes READY, non-admin smoke, CSP report collection — approved or deferred-post-deploy).

Purpose: Leave a reviewer-readable record of what Phase 5 changed, how it was verified, and which live halves are deferred — with env vars by NAME only.
Output: a Phase 5 entry appended to docs/SAAS_UPGRADE_CHANGELOG.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-data-reliability-infrastructure-headers/05-VALIDATION.md
@docs/SAAS_UPGRADE_CHANGELOG.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 04 — changelog)

- **`docs/SAAS_UPGRADE_CHANGELOG.md`** *(modified)* — a Phase 5 (Data Reliability & Infrastructure Headers) entry: what/why/files/commands/results/risks, the standing AUDIT-03 task. Records the net-new test, the two INFRA-03 edits, the reconciles, and the Plan 03 live-half statuses. Env vars by NAME only.

No production code changes.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Record the Phase 5 outcome in the changelog (AUDIT-03)</name>
  <files>docs/SAAS_UPGRADE_CHANGELOG.md</files>
  <read_first>
    - docs/SAAS_UPGRADE_CHANGELOG.md (existing per-phase entry format — what/why/files/commands/results/risks; the Phase 4 entry as the closest structural template)
    - .planning/phases/05-data-reliability-infrastructure-headers/05-01-SUMMARY.md (DATA-02 test + DATA-01 coverage reconcile outcome, once 05-01 has run)
    - .planning/phases/05-data-reliability-infrastructure-headers/05-02-SUMMARY.md (INFRA-03 build-script + CSP allowlist edits + INFRA-02 reconcile, once 05-02 has run)
    - .planning/phases/05-data-reliability-infrastructure-headers/05-03-SUMMARY.md (the three live-half statuses — indexes READY, non-admin smoke, CSP reports — approved or deferred, once 05-03 has run)
  </read_first>
  <action>
    Append a Phase 5 (Data Reliability & Infrastructure Headers) entry to docs/SAAS_UPGRADE_CHANGELOG.md following the existing per-phase format. Record: the net-new artifact src/components/AnalyticsDashboard.test.js (DATA-02 fallback -> captureError proven loud); the package.json build-script change to INLINE_RUNTIME_CHUNK=false react-scripts build (INFRA-03, external runtime chunk closes the inline-script CSP hole); the vercel.json CSP Report-Only allowlist additions (apis.google.com in script-src; *.ingest.us.sentry.io and *.firebaseapp.com in connect-src; *.firebaseapp.com in frame-src) with the note that CSP stays Report-Only this phase (INFRA-01 enforcement completes in Phase 8); the verify-only reconciles (DATA-01 index coverage complete across the 15 defs; INFRA-02 cache headers immutable/no-cache intact); the gate commands (npm run check:constants, npm run lint, npm run test:ci, npm run test:api, npm run build); and the Plan 03 live-half statuses (DATA-01 indexes READY, DATA-03 non-admin smoke, INFRA-02/03 curl + CSP report collection — approved or deferred-post-deploy). Reference env var NAMES only — SENTRY_DSN, REACT_APP_SENTRY_DSN — never values. Do not edit any production source.
  </action>
  <verify>
    <automated>grep -qi 'Phase 5\|Data Reliability\|Infrastructure Headers' docs/SAAS_UPGRADE_CHANGELOG.md && grep -q 'AnalyticsDashboard.test.js' docs/SAAS_UPGRADE_CHANGELOG.md && grep -q 'INLINE_RUNTIME_CHUNK' docs/SAAS_UPGRADE_CHANGELOG.md && grep -qi 'Report-Only' docs/SAAS_UPGRADE_CHANGELOG.md && echo CHANGELOG_UPDATED</automated>
  </verify>
  <acceptance_criteria>
    - docs/SAAS_UPGRADE_CHANGELOG.md has a Phase 5 entry naming AnalyticsDashboard.test.js, the INLINE_RUNTIME_CHUNK build-script edit, and the CSP Report-Only allowlist additions
    - The entry records the DATA-01/INFRA-02 reconciles and the phase gate commands
    - The entry records the three Plan 03 live-half statuses (approved or deferred-post-deploy)
    - Env vars referenced by NAME only; no secret values appear
    - No production source is modified
  </acceptance_criteria>
  <done>The Phase 5 outcome — DATA-02 test green, INFRA-03 edits landed, DATA-01/INFRA-02 reconciled, live halves verified-or-deferred — is captured in the running changelog (AUDIT-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| changelog doc → public repo | The changelog is committed to a public repo; the risk is leaking a secret value (DSN/key) into documentation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05D-01 | Information Disclosure | env var handling in changelog | high | mitigate | The entry references SENTRY_DSN / REACT_APP_SENTRY_DSN by NAME only; the verify grep + review confirm no secret value is written |
| T-05D-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs NO packages (RESEARCH Package Legitimacy Audit: zero new installs) — no supply-chain surface |
</threat_model>

<verification>
- Task 1: `grep` confirms the Phase 5 changelog entry names AnalyticsDashboard.test.js, INLINE_RUNTIME_CHUNK, and Report-Only; env vars by NAME only
- `npm run lint` unaffected (docs-only change)
</verification>

<success_criteria>
The running changelog records the complete Phase 5 outcome (net-new DATA-02 test, INFRA-03 edits, DATA-01/INFRA-02 reconciles, and the three live-half statuses) with env vars by NAME only — satisfying the AUDIT-03 standing task and leaving main shippable.
</success_criteria>

<output>
Create `.planning/phases/05-data-reliability-infrastructure-headers/05-04-SUMMARY.md` when done.
</output>
