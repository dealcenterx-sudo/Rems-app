---
phase: 04-serverless-hardening
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - docs/SAAS_UPGRADE_CHANGELOG.md
autonomous: false
requirements: [SEC-01, SEC-02, SEC-03]
user_setup:
  - service: cloudinary
    why: "Verifying a REAL asset is removed from Cloudinary requires live Admin credentials + a real uploaded asset; the two secrets live in Vercel runtime, not local/git"
    env_vars:
      - name: CLOUDINARY_API_KEY
        source: "Cloudinary Dashboard -> Settings -> API Keys -> set as a Vercel RUNTIME env var for serverless functions"
      - name: CLOUDINARY_API_SECRET
        source: "Cloudinary Dashboard -> Settings -> API Keys -> set as a Vercel RUNTIME env var for serverless functions"
      - name: CLOUDINARY_CLOUD_NAME
        source: "Cloudinary Dashboard (cloud name; has public fallback dcirl3j3v in api/_lib/config.js) -> optionally set as a Vercel env var"
  - service: sentry
    why: "The 'log-then-enforce with Sentry watching' validation soak needs the Phase-3-deferred Sentry DSN + a production soak window; no DSN is configured yet"
    env_vars:
      - name: SENTRY_DSN
        source: "Sentry project settings (Client Keys / DSN) -> Vercel RUNTIME env var for serverless functions (Phase 3 deferral)"
must_haves:
  truths:
    - "With live Cloudinary Admin credentials set in Vercel and a real uploaded asset, deleting media in the deployed app removes the asset from Cloudinary and returns 200 result:'ok' (SEC-03 external half) — or is explicitly deferred post-deploy"
    - "A second delete of the same (now-absent) asset returns 200 idempotently (result:'not found'), confirming the documented idempotent behavior in production"
    - "The 'log-then-enforce validation soak with Sentry watching' is confirmed DEFERRED: the code is enforce-only (no log-mode toggle by decision) and Sentry has no DSN (Phase 3 deferral) — the accept-path tests (04-01) already prove live-client payloads pass (SEC-01 external half)"
    - "The Phase 4 outcome — automatable SEC-01/02/03 halves green, external halves verified-or-deferred — is recorded in docs/SAAS_UPGRADE_CHANGELOG.md with env vars by NAME only (AUDIT-03 standing task)"
  artifacts:
    - docs/SAAS_UPGRADE_CHANGELOG.md
  key_links:
    - "These checkpoints depend on the automatable coverage proven in 04-01 (accept-path + delete-media deltas) and 04-02 (trust-boundary doc + audit test); they close the external-resource halves that cannot be observed locally"
    - "The Cloudinary live-delete mirrors Phase 2's Java-21 and Phase 3's DSN deferral precedent; the Sentry-watched soak is blocked on the SAME Phase-3-deferred DSN"
---

<objective>
Close the two external-resource halves of Phase 4 that cannot be observed locally — exactly following the Phase 2 (Java-21 emulator delegated) and Phase 3 (Sentry event-landing deferred) precedent. Per the locked decisions, each is a `checkpoint:human-verify` (autonomous: false), NOT a blocking code task: (1) SEC-03 real Cloudinary asset deletion needs live Admin credentials + a real asset; (2) SEC-01 "log-then-enforce validation soak with Sentry watching" is a DEFERRED item — the code is enforce-only (no log-mode toggle, by decision) and Sentry has no DSN (Phase 3 deferral). A final auto task records the Phase 4 outcome in the running changelog.

Purpose: Make SEC-01/SEC-03 fully verifiable without pretending an external event can be observed locally. The automatable halves are green after 04-01/04-02; these checkpoints give the operator an exact repro after they provision Cloudinary credentials (and, for the soak, the Sentry DSN) and deploy. Env vars are referenced by NAME only.
Output: two human-verify confirmations (or documented deferral) + a Phase 4 changelog entry.

If credentials/DSN are not provisioned yet, these checkpoints defer post-deploy exactly like Phase 1/2/3 Manual-Only items — the phase's automatable gate (04-01 + 04-02) still passes independently. Expected terminal state: human_needed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-serverless-hardening/04-RESEARCH.md
@.planning/phases/04-serverless-hardening/04-VALIDATION.md
@docs/ENVIRONMENT.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03 — external-half verification + changelog)

- Two human-verify confirmations recorded in the SUMMARY, each as approved or explicitly deferred-post-deploy:
  - SEC-03: real Cloudinary asset deletion (200 result:'ok' + asset gone from dashboard + idempotent 200 on re-delete)
  - SEC-01: "log-then-enforce with Sentry watching" soak — confirmed deferred (enforce-only code + no Sentry DSN)
- **`docs/SAAS_UPGRADE_CHANGELOG.md`** *(modified)* — a Phase 4 entry (what/why/files/commands/results/risks), the standing AUDIT-03 task

No production code changes. Env vars referenced by NAME only (CLOUDINARY_API_KEY, CLOUDINARY_API_SECRET, CLOUDINARY_CLOUD_NAME, SENTRY_DSN).
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify a real media delete removes the asset from Cloudinary (SEC-03 external half)</name>
  <read_first>
    - .planning/phases/04-serverless-hardening/04-VALIDATION.md (Manual-Only table — "Real media deletion removes the asset from Cloudinary" row)
    - .planning/phases/04-serverless-hardening/04-RESEARCH.md (SEC-03 characterization — signed cloudinary.uploader.destroy; Pitfall 3 — do not attempt a real destroy locally; idempotent 200-on-not-found is intended)
    - docs/ENVIRONMENT.md (CLOUDINARY_API_KEY / CLOUDINARY_API_SECRET / CLOUDINARY_CLOUD_NAME entries)
    - docs/TRUST_BOUNDARIES.md (Cloudinary Media section lines 44-50 — server-side signed delete, records without publicId cannot be deleted)
  </read_first>
  <what-built>delete-media's auth gate, signed Admin destroy (api_secret → cloudinary.config → uploader.destroy), payload validation, 3-flow client wiring, and the 200/502/503 branches are code-verified with mocks in 04-01 and the prior suite. This checkpoint confirms a REAL asset actually leaves Cloudinary in the deployed app.</what-built>
  <action>Human verification only — no code change. The operator confirms CLOUDINARY_API_KEY and CLOUDINARY_API_SECRET are set as Vercel runtime env vars, uploads a throwaway asset in the deployed (or preview) app, deletes it through one of the three media-delete flows, and confirms removal (see how-to-verify). If credentials are not provisioned, mark DEFERRED post-deploy. Do not modify any handler.</action>
  <how-to-verify>
    1. Confirm `CLOUDINARY_API_KEY` and `CLOUDINARY_API_SECRET` are set as Vercel RUNTIME env vars for the functions (CLOUDINARY_CLOUD_NAME has the public fallback dcirl3j3v), then deploy.
    2. In the deployed app, upload a throwaway asset (a document via DocumentsPage, a property image via PropertiesPage, or a deal document via DealDocumentsTab), then delete it through that flow.
    3. Confirm the app's delete succeeds (network response 200 with `result: 'ok'`) AND the asset is gone from the Cloudinary Media Library dashboard.
    4. Delete the same (now-absent) record/asset a second time (or re-trigger) and confirm it returns 200 idempotently (`result: 'not found'`) — the documented intended behavior.
    Expected: the asset is removed from Cloudinary and the re-delete is a clean idempotent 200. If credentials are not provisioned yet, mark DEFERRED post-deploy (do not block the automatable gate from 04-01/04-02).
  </how-to-verify>
  <verify><human-check>A real uploaded Cloudinary asset is confirmed removed from the dashboard after an in-app delete (200 result:'ok'), and a second delete returns 200 result:'not found' idempotently — or explicitly deferred post-deploy if credentials are not set.</human-check></verify>
  <acceptance_criteria>
    - Operator confirms the asset is gone from the Cloudinary dashboard after an in-app delete, OR explicitly marks the item DEFERRED post-deploy pending credentials
    - The re-delete idempotency (200 result:'not found') is confirmed or noted as part of the deferral
    - No handler is modified; env vars referenced by NAME only
  </acceptance_criteria>
  <done>The SEC-03 external half — a real asset is actually deleted from Cloudinary via the signed Admin call — is confirmed in the deployed app, or explicitly deferred post-deploy pending Cloudinary credentials.</done>
  <resume-signal>Type "approved" (asset removed + idempotent re-delete confirmed) or "deferred" (no credentials yet) or describe the discrepancy.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm the log-then-enforce Sentry-watched validation soak is deferred (SEC-01 external half)</name>
  <read_first>
    - .planning/phases/04-serverless-hardening/04-VALIDATION.md (Manual-Only table — "Log-then-enforce validation soak with Sentry watching" row)
    - .planning/phases/04-serverless-hardening/04-RESEARCH.md (SEC-01 "Enforce vs log mode" — code is enforce-only, no toggle; Open Question Q1 — do NOT add a log-mode flag; route live-traffic observation to human-verify like OBS-01/02/03)
    - .planning/phases/04-serverless-hardening/04-01-SUMMARY.md (accept-path tests proving live-client payload shapes pass — the automatable substitute for the soak, once 04-01 has run)
    - docs/ENVIRONMENT.md (SENTRY_DSN — Phase 3 deferral)
  </read_first>
  <what-built>04-01 added accept-path tests proving every EXACT live-client payload shape passes zod validation — the automatable evidence that enforce-mode will not break clients. The success-criterion phrase "log-then-enforce rollout with Sentry watching" additionally implies a production soak observed via Sentry.</what-built>
  <action>Human verification only — no code change. Per the locked decision, do NOT add a log-then-enforce mode toggle (scope creep) and do NOT wait to add a log-mode flag. The operator confirms understanding that: (a) the validation code is enforce-only by design; (b) the accept-path tests (04-01) already prove live-client payloads pass; and (c) the "Sentry-watched soak" is DEFERRED, blocked on the same Phase-3-deferred SENTRY_DSN. If/when a DSN is provisioned and a production soak is run, the operator watches for any 400s on real client traffic. Do not modify validate.js.</action>
  <how-to-verify>
    1. Confirm the accept-path suite from 04-01 is green (`npm run test:api`) — this is the automatable proof that live-client payload shapes pass validation.
    2. Acknowledge that the validation code is enforce-only (no log-mode toggle, by decision) and that a live "Sentry-watched soak" cannot run until SENTRY_DSN is provisioned (Phase 3 deferral).
    3. If a DSN is later provisioned and a deploy soak is run: watch serverless logs / Sentry for any 400 'Invalid request payload' responses on real client traffic during the soak window; confirm no live-client payload is rejected.
    Expected outcome now: DEFERRED post-deploy — the automatable accept-path evidence stands in for the soak until a DSN exists (identical to the OBS-01/02/03 event-landing deferral).
  </how-to-verify>
  <verify><human-check>The operator acknowledges the enforce-only design + accept-path test coverage and confirms the Sentry-watched soak is deferred pending the Phase-3 SENTRY_DSN (or reports a live soak result if a DSN is now available).</human-check></verify>
  <acceptance_criteria>
    - The operator confirms no log-then-enforce toggle was added (matches the locked decision) and that accept-path tests cover the "clients don't break" claim
    - The Sentry-watched soak is recorded as DEFERRED (blocked on SENTRY_DSN) or reported with a live soak result
    - validate.js is not modified; env vars referenced by NAME only
  </acceptance_criteria>
  <done>The SEC-01 external half is resolved as an explicit, documented deferral: enforce-only code is justified by the green accept-path tests, and the Sentry-watched soak is deferred to the same post-deploy DSN bucket as Phase 3.</done>
  <resume-signal>Type "approved" (acknowledged + deferred) or "soak-passed" (live soak clean with a DSN) or describe the discrepancy.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the Phase 4 serverless-hardening outcome in the changelog (AUDIT-03)</name>
  <files>docs/SAAS_UPGRADE_CHANGELOG.md</files>
  <read_first>
    - docs/SAAS_UPGRADE_CHANGELOG.md (existing per-phase entry format — what/why/files/commands/results/risks; the Phase 3 entry as the closest structural template)
    - .planning/phases/04-serverless-hardening/04-01-SUMMARY.md (accept-path + delete-media delta outcome, once 04-01 has run)
    - .planning/phases/04-serverless-hardening/04-02-SUMMARY.md (trust-boundary doc + audit test outcome, once 04-02 has run)
    - .planning/phases/04-serverless-hardening/04-VALIDATION.md (Manual-Only table — the two deferred external items to record)
  </read_first>
  <action>
    Append a Phase 4 (Serverless Hardening) entry to docs/SAAS_UPGRADE_CHANGELOG.md following the existing per-phase format. Record: the net-new artifacts (tests/api/validate-schemas.test.mjs, tests/api/trust-boundaries-audit.test.mjs, the delete-media delta cases in tests/api/api-handlers.test.mjs, and the docs/TRUST_BOUNDARIES.md csp-report row); the gate commands (`npm run lint`, `npm run test:ci`, `npm run test:api`, `npm run build`); the results (SEC-01 accept-path green, SEC-02 doc + audit test green, SEC-03 delete-media deltas green); the decision that NO log-then-enforce toggle was added (enforce-only by design); and the two external-half statuses from Tasks 1-2 (SEC-03 real Cloudinary delete + SEC-01 Sentry-watched soak — approved or deferred-post-deploy). Reference env var NAMES only — CLOUDINARY_API_KEY, CLOUDINARY_API_SECRET, CLOUDINARY_CLOUD_NAME, SENTRY_DSN — never values. Do not edit any production source.
  </action>
  <verify>
    <automated>grep -qi 'serverless hardening\|SEC-01\|SEC-03' docs/SAAS_UPGRADE_CHANGELOG.md && grep -q 'trust-boundaries-audit.test.mjs' docs/SAAS_UPGRADE_CHANGELOG.md && grep -q 'validate-schemas.test.mjs' docs/SAAS_UPGRADE_CHANGELOG.md && echo CHANGELOG_UPDATED</automated>
  </verify>
  <acceptance_criteria>
    - docs/SAAS_UPGRADE_CHANGELOG.md has a Phase 4 entry naming the net-new test files, the csp-report doc row, and the phase gate commands
    - The entry records the enforce-only decision (no log-then-enforce toggle added)
    - The entry records the two external-half statuses (SEC-03 Cloudinary delete, SEC-01 Sentry soak — approved or deferred)
    - Env vars referenced by NAME only; no secret values appear
    - No production source is modified
  </acceptance_criteria>
  <done>The Phase 4 outcome — automatable SEC-01/02/03 halves verified, external halves verified-or-deferred, enforce-only decision recorded — is captured in the running changelog.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Vercel/Cloudinary/Sentry config | Credentials + DSN are provisioned by a human in external dashboards; the risk is leaking secret values into repo/docs or destroying a real production asset during the verification |
| serverless function → Cloudinary Admin API | The real signed destroy actually removes an asset; verification must use a throwaway asset, never a production media record |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04C-01 | Information Disclosure | env var handling in docs/changelog | high | mitigate | Task 3 records env var NAMES only (CLOUDINARY_API_KEY/SECRET/CLOUD_NAME, SENTRY_DSN); grep-guarded; the api_secret never leaves the Vercel runtime + api/ server |
| T-04C-02 | Tampering | real Cloudinary delete during verification | medium | mitigate | Task 1 uses a THROWAWAY uploaded asset, never a production media record; the idempotent 200-on-not-found means an accidental re-delete is safe |
| T-04C-03 | Denial of Service | live client 400s during a Sentry-watched soak | high | mitigate | 04-01 accept-path tests prove live-client payloads pass BEFORE any soak; the soak (if run) only watches, does not change enforce behavior; deferred until a DSN exists |
| T-04C-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs NO packages (RESEARCH Package Legitimacy Audit: zero new installs) — no supply-chain surface; no [ASSUMED]/[SUS]/[SLOP] packages |
</threat_model>

<verification>
- Task 1: real Cloudinary asset deletion confirmed in the deployed app (200 result:'ok' + dashboard removal + idempotent re-delete) OR explicitly deferred post-deploy
- Task 2: the enforce-only decision + accept-path coverage acknowledged; the Sentry-watched soak recorded as deferred (blocked on SENTRY_DSN) OR reported with a live soak result
- Task 3: docs/SAAS_UPGRADE_CHANGELOG.md carries the Phase 4 entry with net-new artifacts, gate commands, enforce-only decision, and external-half statuses
- Env vars referenced by NAME only; no values recorded
</verification>

<success_criteria>
The external-resource halves of SEC-01 and SEC-03 are verified in the deployed environment (or explicitly deferred post-deploy exactly as Phases 1/2/3 handled credential/DSN/deploy-gated items), no log-then-enforce toggle was added, and the Phase 4 outcome is captured in the running changelog. Expected terminal state: human_needed.
</success_criteria>

<output>
Create `.planning/phases/04-serverless-hardening/04-03-SUMMARY.md` when done.
</output>
