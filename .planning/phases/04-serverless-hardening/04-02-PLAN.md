---
phase: 04-serverless-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/TRUST_BOUNDARIES.md
  - tests/api/trust-boundaries-audit.test.mjs
autonomous: true
requirements: [SEC-02]
must_haves:
  truths:
    - "A reviewer can read docs/TRUST_BOUNDARIES.md and find EVERY api/*.js endpoint documented, including api/csp-report.js — the previously-missing row"
    - "The csp-report row documents caller proof 'None by design — browsers post CSP violations credential-less', the logs/forwards operation, and 405 non-POST / 204 valid-report failure behavior"
    - "A rationale sentence explains WHY csp-report is intentionally unauthenticated (browser beacon, no business-data write) so an auditor does not flag it as a defect"
    - "An automated audit test asserts every handler file under api/ has a matching endpoint row in docs/TRUST_BOUNDARIES.md (the doc's auth-posture coverage matches the actual endpoint set)"
    - "Every mutating/privileged endpoint's auth posture (401 on missing/invalid token, or 401 on shared-secret mismatch) is already pinned by the existing tests/api suite — cross-referenced, not rebuilt"
  artifacts:
    - docs/TRUST_BOUNDARIES.md
    - tests/api/trust-boundaries-audit.test.mjs
  key_links:
    - "The new csp-report row is inserted after the api/health.js row (docs/TRUST_BOUNDARIES.md:32), styled after health.js as the other progressive/intentionally-open endpoint"
    - "The audit test globs api/*.js, strips _lib, and asserts each basename appears in the doc — a positive completeness check, so a future new endpoint that is undocumented fails the suite"
    - "lead-intake (shared secret) and csp-report (open beacon) are the only two non-Firebase-token endpoints; both rationales now live in the doc"
---

<objective>
Close the single SEC-02 documentation gap: `api/csp-report.js` — an intentionally-unauthenticated browser beacon — is the only endpoint missing from `docs/TRUST_BOUNDARIES.md`. Add its row plus a rationale sentence (styled after the existing `api/health.js` row), and add an automated auth-audit test that asserts the doc lists every `api/*.js` endpoint, so the "auth posture matches the doc" claim is machine-checked and stays true as endpoints are added.

Purpose: Per SEC-02, the auth-token audit is already complete in docs/TRUST_BOUNDARIES.md for every mutating endpoint (send-email, accept-invite, lead-intake, delete-media, health all have rows; their 401/403/shared-secret behavior is pinned by the existing tests/api suite). The ONE gap is that csp-report is absent — and an auditor seeing an unauthenticated endpoint could mistake it for a defect. Documenting it with an explicit rationale, plus a completeness test, makes the trust boundary reviewer-readable and regression-proof.
Output: the csp-report row + rationale in docs/TRUST_BOUNDARIES.md and a net-new completeness audit test; `npm run test:api` green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-serverless-hardening/04-RESEARCH.md
@.planning/phases/04-serverless-hardening/04-PATTERNS.md
@docs/TRUST_BOUNDARIES.md
@api/csp-report.js
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02 — SEC-02 doc + audit test)

- **`docs/TRUST_BOUNDARIES.md`** *(modified)* — new `api/csp-report.js` row in the Serverless API Functions table (inserted after the `api/health.js` row) + a rationale sentence explaining the intentional lack of authentication
- **`tests/api/trust-boundaries-audit.test.mjs`** *(new)* — vitest suite asserting every `api/*.js` handler basename appears as a documented endpoint in docs/TRUST_BOUNDARIES.md (auth-posture coverage completeness)

No production code changes. No new packages. Env vars/secret values never written to the doc.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add the csp-report.js row + rationale to TRUST_BOUNDARIES.md (SEC-02)</name>
  <files>docs/TRUST_BOUNDARIES.md</files>
  <read_first>
    - docs/TRUST_BOUNDARIES.md (Serverless API Functions table header lines 26-27; existing rows lines 28-32; the api/health.js progressive-endpoint row line 32 to mirror; the Cloudinary prose section lines 44-50 for rationale-sentence style; the lead-intake shared-secret row line 30 as the other non-Firebase-token precedent)
    - api/csp-report.js (method guard 405 non-POST line 29-30; normalizeReport logs/forwards line 33+; returns 204 for valid reports; no business-data write)
    - .planning/phases/04-serverless-hardening/04-PATTERNS.md ("NET-NEW: docs/TRUST_BOUNDARIES.md csp-report row" — the exact column format, the health.js analog row, the row to ADD, and the rationale sentence to add near the table)
    - .planning/phases/04-serverless-hardening/04-RESEARCH.md (SEC-02 section — csp-report is the only missing row; Pitfall 2 — unauthenticated-by-design is not a defect)
  </read_first>
  <action>
    Edit docs/TRUST_BOUNDARIES.md. Insert one new table row into the Serverless API Functions table immediately after the `api/health.js` row (line 32), using the existing four columns (Endpoint | Caller Proof | Privileged Operation | Failure Behavior). Content per 04-PATTERNS.md: Endpoint `api/csp-report.js`; Caller Proof states it is unauthenticated by design because browsers post CSP violation reports credential-less; Privileged Operation states it logs/forwards the violation report with no business-data mutation; Failure Behavior states non-POST returns 405 and valid reports return 204.

    Then add a short rationale sentence in prose near the table (matching the doc's existing prose style, e.g. after the table or in a brief note) explaining that csp-report is intentionally unauthenticated: browsers send CSP violation beacons without credentials and the endpoint only records the report (never writes business data), so requiring a token would silently drop all reports — a deliberate, low-risk open beacon mirroring the CSP spec. Reference that lead-intake (shared secret) and csp-report (open beacon) are the only two non-Firebase-token endpoints, both now documented. Do NOT write any secret or env-var value; reference env vars by NAME only if mentioned at all.
  </action>
  <verify>
    <automated>grep -q 'api/csp-report.js' docs/TRUST_BOUNDARIES.md && grep -qi 'credential' docs/TRUST_BOUNDARIES.md && grep -q '204' docs/TRUST_BOUNDARIES.md && echo TRUST_BOUNDARIES_UPDATED</automated>
  </verify>
  <acceptance_criteria>
    - docs/TRUST_BOUNDARIES.md contains a row for `api/csp-report.js` in the Serverless API Functions table
    - The row documents the unauthenticated-by-design caller proof, the logs/forwards operation, and the 405/204 failure behavior
    - A prose rationale explains why csp-report is intentionally unauthenticated (browser beacon, no business-data write)
    - `grep -q 'api/csp-report.js' docs/TRUST_BOUNDARIES.md` succeeds; no secret values appear anywhere in the doc
  </acceptance_criteria>
  <done>Every serverless endpoint — including the previously-undocumented csp-report browser beacon — is documented in TRUST_BOUNDARIES.md with an explicit rationale for its auth posture (SEC-02 doc gap closed).</done>
</task>

<task type="auto">
  <name>Task 2: Add the auth-audit completeness test asserting the doc lists every endpoint (SEC-02)</name>
  <files>tests/api/trust-boundaries-audit.test.mjs</files>
  <read_first>
    - docs/TRUST_BOUNDARIES.md (the Serverless API Functions table — the set of documented endpoint rows the test asserts against)
    - tests/api/api-handlers.test.mjs (lines 1-9 — createRequire/requireFromRoot pattern; note the existing per-endpoint auth tests: send-email/accept-invite/delete-media 401 cases and lead-intake 401 shared-secret case — cross-reference, do NOT rebuild)
    - .planning/phases/04-serverless-hardening/04-RESEARCH.md (SEC-02 audit table listing all six endpoints and their auth mechanisms — the source of truth the doc must match)
    - .planning/phases/04-serverless-hardening/04-PATTERNS.md (module-cache mock note — but this test needs no handler load; it is a filesystem+doc string check)
  </read_first>
  <behavior>
    - The test enumerates the handler files under api/ (top-level *.js, excluding the api/_lib helpers directory) and, for each, asserts its filename (e.g. 'api/csp-report.js') appears in the docs/TRUST_BOUNDARIES.md text
    - The test fails if any current or future api/ handler is not documented (positive completeness — the doc must cover the full endpoint set)
    - The test documents (via its assertions/comments-in-spec) that lead-intake uses a shared secret and csp-report is unauthenticated by design, matching the RESEARCH audit table
  </behavior>
  <action>
    Create `tests/api/trust-boundaries-audit.test.mjs` as a vitest suite. Use Node `fs` (`readdirSync`) to list top-level `.js` files in the `api/` directory (resolve the path relative to the test file via `new URL('../../api', import.meta.url)`), excluding the `_lib` subdirectory. Read `docs/TRUST_BOUNDARIES.md` as a string via `readFileSync`. For each handler basename, assert the doc string includes the `api/<name>.js` reference (`expect(doc).toContain('api/<name>.js')`), so every endpoint — send-email, accept-invite, lead-intake, delete-media, health, csp-report — is proven documented. Add a focused assertion that the doc mentions the two intentional non-Firebase-token postures (shared-secret for lead-intake, unauthenticated beacon for csp-report) so the audit reflects the RESEARCH SEC-02 table. Do NOT load or mock the handlers; this is a static doc-vs-filesystem completeness audit. Do NOT restate secret values.
  </action>
  <verify>
    <automated>npm run test:api</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:api` exits 0 with tests/api/trust-boundaries-audit.test.mjs included
    - The test enumerates api/*.js handlers (excluding _lib) and asserts each appears in docs/TRUST_BOUNDARIES.md
    - The test would FAIL if a handler (e.g. csp-report) were missing from the doc — proving the doc's auth-posture coverage matches the endpoint set
    - The test asserts the doc documents the shared-secret (lead-intake) and unauthenticated-beacon (csp-report) postures
  </acceptance_criteria>
  <done>The SEC-02 "auth posture matches the doc" claim is machine-verified: an automated test proves every api/ endpoint is documented in TRUST_BOUNDARIES.md, and it will catch any future undocumented endpoint. test:api is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → api/csp-report.js | Unauthenticated CSP violation beacons cross here by design; the endpoint records only, never mutates business data |
| reviewer/auditor → TRUST_BOUNDARIES.md | The doc is the human-readable trust-boundary artifact; an incomplete doc risks an auditor mis-flagging an intentional open beacon or missing an undocumented endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04B-01 | Repudiation | undocumented endpoint auth posture | high | mitigate | Task 1 documents csp-report; Task 2 adds a completeness test that fails if any api/ endpoint is undocumented, so the trust-boundary doc cannot silently drift from the deployed endpoint set |
| T-04B-02 | Denial of Service | csp-report open beacon abuse (spam) | low | accept | Endpoint only logs/forwards and returns 204, no DB write; documented as an accepted low-risk open beacon per the CSP spec (RESEARCH Security Domain) |
| T-04B-03 | Information Disclosure | secrets/PII in the trust-boundary doc | low | mitigate | Task 1/2 reference env vars by NAME only and write no secret values; grep-guarded |
| T-04B-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs NO packages (RESEARCH Package Legitimacy Audit: zero new installs) — no supply-chain surface; no [ASSUMED]/[SUS]/[SLOP] packages |
</threat_model>

<verification>
- `npm run test:api` exits 0 with the net-new audit test included
- docs/TRUST_BOUNDARIES.md documents all six endpoints including csp-report, with the intentional-open-beacon rationale
- The audit test fails if any api/ handler is undocumented (regression guard)
- No secret values in the doc; env vars by NAME only
- Full lane gate: `npm run lint && npm run test:ci && npm run test:api` green
</verification>

<success_criteria>
SEC-02 is fully satisfied on its automatable half: the client/server trust boundary is documented for every endpoint (csp-report gap closed with an explicit rationale), and an automated audit test proves the doc's auth-posture coverage matches the actual endpoint set and will catch future drift.
</success_criteria>

<output>
Create `.planning/phases/04-serverless-hardening/04-02-SUMMARY.md` when done.
</output>
