---
phase: 04-serverless-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/api/validate-schemas.test.mjs
  - tests/api/api-handlers.test.mjs
autonomous: true
requirements: [SEC-01, SEC-03]
must_haves:
  truths:
    - "Developer can run `npm run test:api` and the net-new accept-path suite proves the EXACT payload shapes the live client sends still pass zod validation (SEC-01 accept-path half)"
    - "sendEmailSchema.safeParse of the emailService.js body {to,subject,text,html,cc,bcc} returns success===true (subject + one of text/html required; blank cc/bcc coerced to undefined, not a 400)"
    - "acceptInviteSchema.safeParse({inviteToken}) from InviteAcceptor.js returns success===true"
    - "deleteMediaSchema.safeParse of the cloudinary.js bodies {publicId,resourceType:'raw'} and {publicId,resourceType:'image'} returns success===true, and resourceType defaults to 'image' when omitted"
    - "leadIntakeSchema.safeParse of a single-identifying-field intake body returns success===true"
    - "delete-media returns 200 idempotently when Cloudinary reports 'not found' (SEC-03), and 502 with error 'Media provider rejected the delete' when Cloudinary rejects the delete"
    - "The happy-path delete asserts cloudinary.config receives api_secret (proving the signed Admin destroy) — already pinned; not regressed"
  artifacts:
    - tests/api/validate-schemas.test.mjs
    - tests/api/api-handlers.test.mjs
  key_links:
    - "validate-schemas.test.mjs imports the CommonJS api/_lib/validate.js via createRequire (requireFromRoot pattern, api-handlers.test.mjs:1,5) — direct schema unit tests, no handler, no mocks"
    - "delete-media delta cases extend the existing describe('api/delete-media') block (api-handlers.test.mjs:465) reusing invoke()/loadHandler/mockCloudinary and the auth fetch stub — no vi.mock"
    - "The uniform 400 reject path is ALREADY pinned (api-handlers.test.mjs:106-125, 217-237, 398-416, 477-494) — these accept-path tests are the complement, do not duplicate it"
---

<objective>
Add the two net-new automatable test surfaces RESEARCH Wave 0 identified as missing: (1) a direct-schema ACCEPT-PATH suite (`tests/api/validate-schemas.test.mjs`) proving every EXACT payload shape the live client sends still passes zod validation — the "live clients never break" half of SEC-01 — and (2) two delete-media handler deltas (`not found` idempotency 200 and 502 provider-rejection) extending the existing suite, closing the SEC-03 coverage gap.

Purpose: Per SEC-01, the reject/400 path is already covered by the Phase 2 suite and the code is ENFORCE-ONLY — do NOT add a log-then-enforce mode toggle (scope creep). The remaining automatable gap is proving the schemas are permissive enough that no live-client payload gets a 400. Per SEC-03, delete-media is fully wired and its 401/400/503/200-signature paths are pinned; only the idempotent-200-on-not-found and 502-provider-rejection branches are net-new. This is reconcile-and-verify — do NOT modify api/_lib/validate.js or api/delete-media.js; they are the subjects under test.
Output: one net-new test file + delta cases in the existing suite; `npm run test:api` green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-serverless-hardening/04-RESEARCH.md
@.planning/phases/04-serverless-hardening/04-PATTERNS.md
@.planning/phases/04-serverless-hardening/04-VALIDATION.md
@api/_lib/validate.js
@api/delete-media.js
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01 — net-new automatable tests)

- **`tests/api/validate-schemas.test.mjs`** *(new)* — vitest accept-path suite importing the four schemas from `api/_lib/validate.js`:
  - SEC-01: `sendEmailSchema` accepts `{to,subject,text}` and `{to,subject,html}` and blank `cc`/`bcc`; `acceptInviteSchema` accepts `{inviteToken}`; `deleteMediaSchema` accepts `{publicId,resourceType:'image'|'raw'}` and defaults `resourceType` to `'image'` when omitted; `leadIntakeSchema` accepts a single identifying field
- **`tests/api/api-handlers.test.mjs`** *(extended)* — two new `it()` cases in the `describe('api/delete-media')` block:
  - SEC-03: `not found` → 200 `{ ok: true, result: 'not found' }` (idempotent); provider `error` → 502 `{ error: 'Media provider rejected the delete' }`

No production code changes. No new packages (`zod`, `cloudinary`, `vitest`, `node-mocks-http` already installed).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add accept-path schema suite proving live-client payloads still pass (SEC-01)</name>
  <files>tests/api/validate-schemas.test.mjs</files>
  <read_first>
    - api/_lib/validate.js (full — formatIssues line 3; validateBody line 8; sendEmailSchema line 34 with subject + text-OR-html superRefine; acceptInviteSchema line 58; leadIntakeSchema line 62 with at-least-one superRefine; deleteMediaSchema line 84 with resourceType enum image|raw|video default image; module.exports lines 89-94)
    - tests/api/api-handlers.test.mjs (lines 1-9 — the createRequire/requireFromRoot import pattern to copy for loading the CommonJS validate.js)
    - src/utils/emailService.js (line 20 — the exact send-email body {to,subject,text,html,cc,bcc})
    - src/components/InviteAcceptor.js (line 29 — the exact accept-invite body {inviteToken})
    - src/utils/cloudinary.js (line 59 — the exact delete-media body {publicId,resourceType}; callers pass 'raw' at DocumentsPage.js:205 / DealDocumentsTab.js:136 and 'image' at PropertiesPage.js:309)
    - .planning/phases/04-serverless-hardening/04-PATTERNS.md ("NET-NEW: Accept-path (happy-path) schema tests" — import pattern, live-client payload shapes table, test body pattern; "Contrast — the reject-path is ALREADY pinned" so do not duplicate)
    - .planning/phases/04-serverless-hardening/04-RESEARCH.md (Pitfall 1 — reject-path tests are not full SEC-01 coverage; accept-path must be added)
  </read_first>
  <behavior>
    - sendEmailSchema.safeParse({ to, subject, text }) success===true (minimal required shape)
    - sendEmailSchema.safeParse({ to, subject, html }) success===true (html instead of text — text OR html superRefine)
    - sendEmailSchema.safeParse({ to, subject, text, cc: '', bcc: '' }) success===true (blank optional cc/bcc coerced to undefined, NOT a 400)
    - acceptInviteSchema.safeParse({ inviteToken: '<non-empty>' }) success===true
    - deleteMediaSchema.safeParse({ publicId, resourceType: 'image' }) and ({ publicId, resourceType: 'raw' }) success===true
    - deleteMediaSchema.safeParse({ publicId }) success===true and parsed data.resourceType === 'image' (enum default)
    - leadIntakeSchema.safeParse({ email: '<addr>', source: 'Website' }) success===true (single identifying field satisfies the at-least-one superRefine)
  </behavior>
  <action>
    Create `tests/api/validate-schemas.test.mjs` as a vitest suite. Import `describe`, `expect`, `it` from `vitest`, and load the CommonJS validators via `createRequire(new URL('../../package.json', import.meta.url))` — copy this exact requireFromRoot pattern from api-handlers.test.mjs lines 1-5 — then destructure `sendEmailSchema`, `acceptInviteSchema`, `leadIntakeSchema`, `deleteMediaSchema` from `requireFromRoot('./api/_lib/validate.js')`. Do NOT use vi.mock; these are pure schema unit tests with no handler, no req/res, no network.

    Assert each behavior above using `.safeParse(<body>).success` toBe true, sourcing the EXACT body shapes from the live client call sites (emailService.js:20, InviteAcceptor.js:29, cloudinary.js:59) — do not invent shapes. For the deleteMediaSchema default case, assert on the parsed value: `deleteMediaSchema.safeParse({ publicId: 'z' }).data.resourceType` equals `'image'`. Group the assertions in a single describe titled for the accept-path (live client shapes still pass). Do NOT re-test the reject/400 path — it is already pinned in api-handlers.test.mjs (lines 106-125, 217-237, 398-416, 477-494); this suite is strictly the accept-path complement. Do NOT modify api/_lib/validate.js.
  </action>
  <verify>
    <automated>npm run test:api</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:api` exits 0 with tests/api/validate-schemas.test.mjs included
    - A test asserts sendEmailSchema accepts {to,subject,text}, {to,subject,html}, and blank cc/bcc (all success===true)
    - A test asserts acceptInviteSchema accepts {inviteToken} and deleteMediaSchema accepts both 'image' and 'raw' shapes plus defaults resourceType to 'image' when omitted
    - A test asserts leadIntakeSchema accepts a single identifying field
    - No log-then-enforce mode toggle is introduced anywhere; api/_lib/validate.js is not modified
  </acceptance_criteria>
  <done>The accept-path (live-client payload) half of SEC-01 is automatically verified: every exact shape the client sends passes zod validation, proving enforce-mode will not break live clients. test:api is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add delete-media not-found-200 and provider-502 delta cases (SEC-03)</name>
  <files>tests/api/api-handlers.test.mjs</files>
  <read_first>
    - api/delete-media.js (lines 43-58 — cloudinary.config with api_secret line 43; uploader.destroy with resource_type + invalidate line 49; 200 on result 'ok' OR 'not found' line 54; 502 'Media provider rejected the delete' line 58)
    - tests/api/api-handlers.test.mjs (describe('api/delete-media') block starting line 465 — the 401/400/503 cases and the happy-path 'ok' 200 case with the config api_secret + destroy assertions; invoke() helper line 47; loadHandler line 42; mockCloudinary line 33; the auth fetch stub returning users:[{localId}]; env/cache reset beforeEach/afterEach lines ~75-91)
    - .planning/phases/04-serverless-hardening/04-PATTERNS.md ("NET-NEW: delete-media auth-gate + signature unit assertions" — Gaps to add: not-found idempotency 200 and 502 provider-rejection; Cloudinary module-cache mock + auth fetch mock + signature assertion patterns; harness invariants "reuse verbatim — do NOT introduce vi.mock")
  </read_first>
  <behavior>
    - With auth + Cloudinary creds mocked and destroy resolving { result: 'not found' }, the handler returns 200 with body { ok: true, result: 'not found' } (idempotent delete is intended behavior)
    - With auth + creds mocked and destroy resolving { result: 'error' } (or any non-ok/non-not-found), the handler returns 502 with body.error === 'Media provider rejected the delete'
    - Both cases run auth BEFORE validation BEFORE the privileged op (supply the valid auth fetch mock first, as the existing cases do)
  </behavior>
  <action>
    Extend the existing `describe('api/delete-media')` block in tests/api/api-handlers.test.mjs by adding two new `it()` cases AFTER the current happy-path 'ok' test. Reuse the established harness verbatim — set `process.env.CLOUDINARY_API_KEY`/`CLOUDINARY_API_SECRET`, build `const destroy = vi.fn(...)` and `const config = vi.fn()`, call `mockCloudinary({ config, uploader: { destroy } })`, stub the Identity Toolkit auth via `vi.stubGlobal('fetch', ...)` returning `{ users: [{ localId: 'user-1', email: 'agent@example.com' }] }`, `loadHandler('delete-media')`, and `invoke(handler, { headers: { authorization: 'Bearer valid-token' }, body: { publicId: 'properties/image-1', resourceType: 'image' } })` — mirroring the existing 200 case. Do NOT introduce vi.mock; use only the module-cache injection already in the file.

    Case 1 (idempotent not-found): make destroy resolve `{ result: 'not found' }`; assert `result.status` toBe 200 and `result.body` toEqual `{ ok: true, result: 'not found' }`. Case 2 (provider rejection): make destroy resolve `{ result: 'error' }`; assert `result.status` toBe 502 and `result.body.error` toBe `'Media provider rejected the delete'`. Rely on the file's existing beforeEach/afterEach to reset env + module cache between cases. Do NOT modify api/delete-media.js.
  </action>
  <verify>
    <automated>npm run test:api</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:api` exits 0 with the two new delete-media cases included
    - A test asserts destroy → { result: 'not found' } yields status 200 and body { ok: true, result: 'not found' }
    - A test asserts destroy → { result: 'error' } yields status 502 and body.error === 'Media provider rejected the delete'
    - Both new cases reuse invoke()/loadHandler/mockCloudinary and the auth fetch stub; no vi.mock is introduced
    - api/delete-media.js is not modified
  </acceptance_criteria>
  <done>The SEC-03 net-new coverage deltas are green: delete-media is idempotent (200 on not-found) and fails loud (502 on provider rejection), extending the already-passing 401/400/503/200-signature cases. test:api is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client → serverless API | Untrusted JSON payloads cross into `api/send-email`, `api/accept-invite`, `api/lead-intake`, `api/delete-media`; zod `validateBody` is the server-side gate |
| serverless function → Cloudinary Admin API | `delete-media` signs the destroy with `CLOUDINARY_API_SECRET`; the secret must never reach the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | zod schemas over-strict → live client 400s (availability) | high | mitigate | Task 1 accept-path suite pins the EXACT live-client payload shapes as success===true, proving enforce-mode does not reject real traffic; the reject path is already pinned by the Phase 2 suite |
| T-04-02 | Tampering | delete-media idempotency / failure signaling | medium | mitigate | Task 2 pins 200-on-not-found (idempotent, no false failure) and 502-on-provider-rejection (loud failure, Firestore record not silently orphaned) |
| T-04-03 | Information Disclosure | uniform 400 body leaking internals | low | accept | validateBody returns only `{error:'Invalid request payload', details:[{path,message}]}` (no stack/secret/PII); tests assert the shape; accepted as-is |
| T-04-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs NO packages (RESEARCH Package Legitimacy Audit: zero new installs; zod/cloudinary/vitest/node-mocks-http already present) — no supply-chain surface; no [ASSUMED]/[SUS]/[SLOP] packages |
</threat_model>

<verification>
- `npm run test:api` exits 0 with both net-new surfaces included
- validate-schemas.test.mjs proves all four schemas accept the exact live-client payload shapes (SEC-01 accept-path)
- api-handlers.test.mjs delete-media block gains idempotent-200 and provider-502 cases (SEC-03)
- No log-then-enforce toggle added; no production source (validate.js, delete-media.js) modified
- Full lane gate: `npm run lint && npm run test:ci && npm run test:api` green
</verification>

<success_criteria>
The automatable halves of SEC-01 (accept-path) and SEC-03 (idempotency + provider-rejection) are green under `npm run test:api`, proving enforce-mode validation will not break live clients and delete-media handles the not-found and provider-error branches correctly — without modifying any shipped handler.
</success_criteria>

<output>
Create `.planning/phases/04-serverless-hardening/04-01-SUMMARY.md` when done.
</output>
