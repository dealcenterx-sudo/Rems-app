---
phase: 03-observability
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - docs/SAAS_UPGRADE_CHANGELOG.md
autonomous: false
requirements: [OBS-01, OBS-02, OBS-03]
user_setup:
  - service: sentry
    why: "Event-landing verification of OBS-01/02/03 requires a live Sentry project + DSN; no DSN is configured in this environment and Vercel deploys production from main"
    env_vars:
      - name: REACT_APP_SENTRY_DSN
        source: "Sentry project settings (Client Keys / DSN) -> set as a Vercel BUILD-TIME env var (inlined by CRA at build; runtime-only will not enable the client)"
      - name: SENTRY_DSN
        source: "Sentry project settings (Client Keys / DSN) -> set as a Vercel RUNTIME env var for serverless functions"
    dashboard_config:
      - task: "Create (or select) a Sentry project and copy its DSN; confirm the deployed build carries REACT_APP_SENTRY_DSN and the functions carry SENTRY_DSN"
        location: "Sentry Dashboard -> Projects; Vercel -> Project -> Settings -> Environment Variables"
must_haves:
  truths:
    - "A deliberately triggered production CLIENT error appears as an Issue in the Sentry project (OBS-01 event-landing half)"
    - "A deliberately triggered UNCAUGHT serverless throw appears as an Issue in the Sentry project (OBS-02 event-landing half) — handled 500s are known NOT to appear (logged gap, Phase 5)"
    - "Web-vitals captureMessage events from the live production app appear in the Sentry project (OBS-03 event-landing half)"
    - "The required env var NAMES (REACT_APP_SENTRY_DSN build-time client, SENTRY_DSN runtime server) are confirmed present in Vercel — values never recorded"
    - "Phase 3 verification outcome is recorded in docs/SAAS_UPGRADE_CHANGELOG.md (standing AUDIT-03 task)"
  artifacts:
    - docs/SAAS_UPGRADE_CHANGELOG.md
  key_links:
    - "These checkpoints depend on the automatable code-wiring proven in 03-01 (client) and 03-02 (server); they verify the production event-landing half that cannot be observed without a live DSN + deploy"
    - "CSP already whitelists https://*.ingest.sentry.io in vercel.json connect-src, so ingest is not CSP-blocked once a DSN is configured"
---

<objective>
Verify the EVENT-LANDING half of OBS-01, OBS-02, and OBS-03 — the three "appears in Sentry" success criteria that require a live DSN + deployed production build and therefore cannot be observed in this environment. Per the locked decision, each is a `checkpoint:human-verify` (autonomous: false), NOT a blocking code task — mirroring how Phase 2 delegated the Java-21 emulator pass to CI/human. The code-wiring half is already proven automatically in 03-01 (client) and 03-02 (server); these checkpoints close the loop after deploy and record the outcome in the changelog.

Purpose: Make OBS-01/02/03 fully verifiable without pretending an event can land locally. The three checkpoints give the operator an exact repro after they provision a Sentry DSN in Vercel and deploy. OBS-02's checkpoint deliberately triggers an UNCAUGHT throw (a handled 500 would NOT appear — that blind spot is logged as a routed-forward finding in 03-02). Env vars are referenced by NAME only.
Output: three human-verify confirmations (or documented deferral) + a Phase 3 changelog entry.

If no Sentry DSN is provisioned yet, these checkpoints are deferred post-deploy exactly like Phase 1/2 Manual-Only items — the phase's automatable gate (03-01 + 03-02) still passes independently.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-observability/03-RESEARCH.md
@.planning/phases/03-observability/03-VALIDATION.md
@docs/ENVIRONMENT.md
</context>

<artifacts_produced>
## Artifacts this phase produces

- Three human-verify confirmations (OBS-01 client error, OBS-02 uncaught serverless throw, OBS-03 web-vitals) recorded in the SUMMARY, each as approved or explicitly deferred-post-deploy
- **`docs/SAAS_UPGRADE_CHANGELOG.md`** — a Phase 3 entry (what/why/files/commands/results/risks), the standing AUDIT-03 task

No production code changes. No new env vars (all six Sentry vars already documented in docs/ENVIRONMENT.md).
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify a production client error lands in Sentry (OBS-01 event-landing)</name>
  <read_first>
    - .planning/phases/03-observability/03-VALIDATION.md (Manual-Only table — OBS-01 event-landing row)
    - docs/ENVIRONMENT.md (REACT_APP_SENTRY_DSN — build-time client, line 18)
    - .planning/phases/03-observability/03-RESEARCH.md (Env behavior — REACT_APP_* inlined at build; client DSN must be a BUILD-TIME var)
  </read_first>
  <what-built>The client Sentry init (DSN-gated) and the ErrorBoundary→captureException bridge are code-verified in 03-01. This checkpoint confirms a real client error actually reaches the Sentry project in production.</what-built>
  <action>Human verification only — no code change. The operator provisions REACT_APP_SENTRY_DSN as a Vercel build-time env var, deploys, triggers a deliberate client error in production, and confirms the Issue in Sentry (see how-to-verify). If no DSN is provisioned, mark DEFERRED post-deploy.</action>
  <how-to-verify>
    1. Confirm `REACT_APP_SENTRY_DSN` is set as a Vercel BUILD-TIME env var (runtime-only will NOT enable the client — CRA inlines it at build), then deploy (merge to main).
    2. Open the deployed app https://rems-app.vercel.app/ and trigger a deliberate client error — e.g. force a render error in a component guarded by ErrorBoundary, or in the browser console evaluate a statement that throws inside app code.
    3. In the Sentry project Issues stream, confirm a new Issue appears for that error (with a componentStack for the ErrorBoundary path).
    Expected: the Issue is visible in Sentry within ~1 minute. If no DSN is provisioned yet, mark this DEFERRED post-deploy (do not block the phase's automatable gate).
  </how-to-verify>
  <verify><human-check>A new client-error Issue is visible in the Sentry project after the deliberate production trigger (or explicitly deferred post-deploy if no DSN yet).</human-check></verify>
  <done>A production client error appears as an Issue in Sentry, or the item is explicitly deferred post-deploy pending DSN provisioning.</done>
  <resume-signal>Type "approved" (Issue seen in Sentry) or "deferred" (no DSN yet) or describe the discrepancy.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify an UNCAUGHT serverless throw lands in Sentry (OBS-02 event-landing)</name>
  <read_first>
    - .planning/phases/03-observability/03-VALIDATION.md (Manual-Only table — OBS-02 event-landing row)
    - docs/ENVIRONMENT.md (SENTRY_DSN — runtime server, line 21)
    - .planning/phases/03-observability/03-RESEARCH.md (Gap #1 — only UNCAUGHT throws reach the wrapper; handled 500s do not)
    - docs/SAAS_READINESS_AUDIT.md (the OBS-02 gap finding logged in 03-02)
  </read_first>
  <what-built>The withSentry wrapper's capture-then-flush-before-respond on an uncaught throw is code-verified in 03-02. This checkpoint confirms a real UNCAUGHT serverless error reaches Sentry in production.</what-built>
  <action>Human verification only — no shipped code change. The operator provisions SENTRY_DSN as a Vercel runtime env var, deploys, triggers an UNCAUGHT throw in a wrapped handler, and confirms the Issue in Sentry (see how-to-verify). Any temporary throw used for the repro is removed afterward. If no DSN is provisioned, mark DEFERRED post-deploy.</action>
  <how-to-verify>
    1. Confirm `SENTRY_DSN` is set as a Vercel RUNTIME env var for the functions, then deploy.
    2. Trigger an UNCAUGHT throw in a wrapped handler (e.g. a temporary throw placed OUTSIDE the handler's own try/catch, or a route/input that forces an unexpected synchronous error before the handler's catch). A NORMAL handled error path (bad payload, provider 502) will NOT produce an event — that is the known, logged gap routed to Phase 5.
    3. In the Sentry project, confirm the Issue appears with the api_route tag and method extra.
    Expected: the Issue is visible in Sentry. Confirm you understand handled 500s are intentionally NOT reported in this phase (documented in docs/SAAS_READINESS_AUDIT.md, routed to Phase 5 / DATA-02). If no DSN is provisioned, mark DEFERRED post-deploy.
  </how-to-verify>
  <verify><human-check>An uncaught-throw serverless Issue (api_route tag, method extra) is visible in Sentry after the production trigger (or explicitly deferred post-deploy if no DSN yet).</human-check></verify>
  <done>An uncaught serverless throw appears as an Issue in Sentry, with the operator acknowledging handled 500s are the Phase 5 gap; or the item is explicitly deferred post-deploy.</done>
  <resume-signal>Type "approved" (uncaught-throw Issue seen in Sentry) or "deferred" (no DSN yet) or describe the discrepancy.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify web-vitals messages land in Sentry (OBS-03 event-landing)</name>
  <read_first>
    - .planning/phases/03-observability/03-VALIDATION.md (Manual-Only table — OBS-03 event-landing row)
    - .planning/phases/03-observability/03-RESEARCH.md (Gap #2 — vitals arrive as info-level captureMessage Issues, not native Performance metrics)
  </read_first>
  <what-built>reportWebVitals(captureWebVital) is wired in src/index.js and captureWebVital→Sentry.captureMessage is code-verified in 03-01. This checkpoint confirms web-vitals events reach Sentry from the live app.</what-built>
  <action>Human verification only — no code change. With REACT_APP_SENTRY_DSN set at build time and deployed, the operator loads/interacts with the production app and confirms the web-vitals captureMessage events in Sentry (see how-to-verify). If no DSN is provisioned, mark DEFERRED post-deploy.</action>
  <how-to-verify>
    1. With `REACT_APP_SENTRY_DSN` set at build time and deployed, load and interact with the production app so web-vitals (CLS/FID/FCP/LCP/TTFB) are measured and reported.
    2. In the Sentry project Issues stream, confirm info-level events titled `Web Vital: <name>` appear with web_vital tags/contexts.
    Expected: web-vitals messages are visible in Sentry (as Issues/messages, not the native Performance dashboard — that would require tracing, a documented v2 tradeoff). If no DSN is provisioned, mark DEFERRED post-deploy.
  </how-to-verify>
  <verify><human-check>Info-level `Web Vital: <name>` events with web_vital tags/contexts are visible in Sentry from the live app (or explicitly deferred post-deploy if no DSN yet).</human-check></verify>
  <done>Web-vitals captureMessage events appear in Sentry from the production app, or the item is explicitly deferred post-deploy.</done>
  <resume-signal>Type "approved" (web-vitals messages seen in Sentry) or "deferred" (no DSN yet) or describe the discrepancy.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Record the Phase 3 observability verification outcome in the changelog (AUDIT-03)</name>
  <files>docs/SAAS_UPGRADE_CHANGELOG.md</files>
  <read_first>
    - docs/SAAS_UPGRADE_CHANGELOG.md (existing per-phase entry format — what/why/files/commands/results/risks)
    - .planning/phases/03-observability/03-01-SUMMARY.md (client-test outcome, once 03-01 has run)
    - .planning/phases/03-observability/03-02-SUMMARY.md (wrapper-test + OBS-02 gap outcome, once 03-02 has run)
  </read_first>
  <action>
    Append a Phase 3 (Observability) entry to docs/SAAS_UPGRADE_CHANGELOG.md following the existing per-phase format. Record: the three net-new test files added (src/utils/observability.test.js, src/components/ErrorBoundary.test.js, tests/api/withSentry.test.mjs); the commands that gate the phase (`npm run test:ci`, `npm run test:api`, `npm run lint`, `npm run build`); the results (code-wiring half of OBS-01/02/03 verified automatically); the OBS-02 handled-500s gap logged and routed to Phase 5 / DATA-02; and the event-landing half status from Tasks 1-3 (approved or deferred-post-deploy). Note the required env var NAMES only — REACT_APP_SENTRY_DSN (build-time client) and SENTRY_DSN (runtime server) — never values. Do not restate secrets. Do not edit any production source.
  </action>
  <verify>
    <automated>grep -qi "observab\|sentry" docs/SAAS_UPGRADE_CHANGELOG.md && grep -q "withSentry.test.mjs" docs/SAAS_UPGRADE_CHANGELOG.md && echo CHANGELOG_UPDATED</automated>
  </verify>
  <acceptance_criteria>
    - docs/SAAS_UPGRADE_CHANGELOG.md has a Phase 3 entry naming the three net-new test files and the phase gate commands
    - The entry records the OBS-02 handled-500s gap routed to Phase 5 / DATA-02
    - The entry records the event-landing half status (approved or deferred) from Tasks 1-3
    - Env vars are referenced by NAME only; no secret values appear
    - No production source is modified
  </acceptance_criteria>
  <done>The Phase 3 observability outcome — automatable code-wiring verified, event-landing verified-or-deferred, OBS-02 gap routed forward — is recorded in the running changelog.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Vercel/Sentry config | The DSN and env vars are provisioned by a human in external dashboards; the risk is leaking secret values into repo/docs or misconfiguring the DSN scope (build vs runtime) |
| production app/function → Sentry SaaS | Real events now leave to `https://*.ingest.sentry.io`; must not carry PII/secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03V-01 | Information Disclosure | env var handling in docs/changelog | medium | mitigate | Task 4 records env var NAMES only; the DSN value is never written to repo/docs; DSN is a client-embeddable identifier but still lives in Vercel env |
| T-03V-02 | Information Disclosure | event payloads in Sentry | low | mitigate | Payload minimality already asserted in 03-01/03-02 (componentStack only client-side; api_route + method server-side); this plan does not widen capture |
| T-03V-03 | Tampering | temporary throw route for OBS-02 repro | low | mitigate | The uncaught-throw repro is a verification action, not a shipped change; the operator removes any temporary throw before the phase closes |
| T-03V-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs NO packages — no supply-chain surface |
</threat_model>

<verification>
- Tasks 1-3: each event-landing criterion (OBS-01 client error, OBS-02 uncaught throw, OBS-03 web-vitals) confirmed in Sentry OR explicitly deferred post-deploy
- Task 4: `docs/SAAS_UPGRADE_CHANGELOG.md` carries the Phase 3 entry with test files, gate commands, OBS-02 gap routing, and event-landing status
- Env vars referenced by name only; no values recorded
</verification>

<success_criteria>
The event-landing half of OBS-01/02/03 is verified in the live Sentry project (or explicitly deferred post-deploy exactly as Phase 1/2 handled DSN/deploy-gated items), OBS-02's uncaught-throw semantics and the handled-500s gap are understood and recorded, and the Phase 3 outcome is captured in the running changelog.
</success_criteria>

<output>
Create `.planning/phases/03-observability/03-03-SUMMARY.md` when done.
</output>
