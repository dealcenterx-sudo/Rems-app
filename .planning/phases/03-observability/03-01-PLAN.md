---
phase: 03-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/observability.test.js
  - src/components/ErrorBoundary.test.js
autonomous: true
requirements: [OBS-01, OBS-03]
must_haves:
  truths:
    - "Developer can run `npm run test:ci` and the new client observability suites pass (OBS-01, OBS-03 code-wiring half)"
    - "With REACT_APP_SENTRY_DSN unset, isSentryEnabled() is false, initObservability() does NOT call Sentry.init, captureError()/captureWebVital() are no-ops, and Sentry is never touched (the default env state)"
    - "With REACT_APP_SENTRY_DSN set, initObservability() calls Sentry.init exactly once and captureError() calls Sentry.captureException with the context under { extra } (OBS-01)"
    - "captureWebVital(metric) calls Sentry.captureMessage('Web Vital: <name>', { level:'info', tags:{web_vital,rating}, contexts }) when enabled, and no-ops when metric is falsy or DSN unset (OBS-03)"
    - "ErrorBoundary.componentDidCatch bridges a caught render error to captureError with { componentStack } and renders the 'Something went wrong' / 'Try again' fallback (OBS-01)"
    - "Adding jest.mock('@sentry/react') does not disturb CRA Jest isolation — the full `npm run test:ci` suite stays green"
  artifacts:
    - src/utils/observability.test.js
    - src/components/ErrorBoundary.test.js
  key_links:
    - "observability.test.js mocks '@sentry/react' with jest.mock and asserts on Sentry.init / captureException / captureMessage — no live DSN needed"
    - "ErrorBoundary.test.js mocks '../utils/observability' and asserts captureError is called; @testing-library/react render drives a throwing child"
    - "The module-level `initialized` flag in observability.js is reset between disabled- and enabled-init cases via jest.resetModules() / jest.isolateModules"
---

<objective>
Add the two net-new CLIENT-side observability unit suites that RESEARCH Gap #3 / Wave 0 identified as missing: `src/utils/observability.test.js` (OBS-01 init gating + captureError bridge; OBS-03 captureWebVital) and `src/components/ErrorBoundary.test.js` (OBS-01 componentDidCatch→captureError bridge). Both run under CRA Jest (`npm run test:ci`) with `@sentry/react` mocked, proving the code-wiring half of OBS-01 and OBS-03 without a live DSN.

Purpose: Satisfy the automatable (code-wiring) half of OBS-01 and OBS-03. The observability code shipped in commit dd6364a; this phase RECONCILES-AND-VERIFIES it and guards it against regression with tests that assert the DSN-gated no-op behavior (the current, intended env state) and the enabled-path wiring. This is NOT a build — do not rewrite the shipped `observability.js` or `ErrorBoundary.js`; they are the subjects under test.
Output: two net-new client test files; `npm run test:ci` green.

The event-landing half of OBS-01/OBS-03 (a real production error / web-vitals message appearing in Sentry) needs a live DSN + deployed build and is handled as a human-verify checkpoint in 03-03-PLAN.md — NOT here.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-observability/03-RESEARCH.md
@.planning/phases/03-observability/03-PATTERNS.md
@.planning/phases/03-observability/03-VALIDATION.md
@src/utils/observability.js
@src/components/ErrorBoundary.js
</context>

<artifacts_produced>
## Artifacts this phase produces (net-new client test files)

- **`src/utils/observability.test.js`** *(new)* — CRA Jest suite mocking `@sentry/react`:
  - OBS-01: `isSentryEnabled()` false / `initObservability()` no-op when DSN unset; `Sentry.init` called once when DSN set; `captureError` no-op when disabled, calls `Sentry.captureException(err, { extra: context })` when enabled
  - OBS-03: `captureWebVital` no-op when metric falsy or DSN unset; calls `Sentry.captureMessage('Web Vital: <name>', { level:'info', tags, contexts })` when enabled
- **`src/components/ErrorBoundary.test.js`** *(new)* — CRA Jest suite mocking `../utils/observability`:
  - OBS-01: a throwing child triggers `componentDidCatch`→`captureError(error, { componentStack })`; fallback UI shows "Something went wrong" + "Try again"

No production code changes. No new packages (`@sentry/react`, `@testing-library/react`, `@testing-library/jest-dom` all already installed).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add observability.test.js — DSN gating, captureError, captureWebVital (OBS-01, OBS-03)</name>
  <files>src/utils/observability.test.js</files>
  <read_first>
    - src/utils/observability.js (full — isSentryEnabled line 10; initObservability lines 12-21 with module-level `initialized` line 3; captureError lines 23-28; captureWebVital lines 30-48; parseSampleRate lines 5-8)
    - src/utils/notifications.test.js (lines 1-17 — the jest.mock + beforeEach reset pattern to clone, swapping firebase for @sentry/react)
    - src/utils/permissions.test.js (assertion style — describe/it + expect toBe/toEqual/toMatchObject)
    - .planning/phases/03-observability/03-PATTERNS.md ("Shipped Patterns to Assert Against" observability.js section; "Client tests → copy from notifications.test.js" harness anchor; the initialized-flag reset note)
    - .planning/phases/03-observability/03-RESEARCH.md (Local-vs-Production split table OBS-01/OBS-03 code-wiring rows; "Suggested Wave 0 test shape")
  </read_first>
  <behavior>
    - isSentryEnabled() returns false when process.env.REACT_APP_SENTRY_DSN is unset, true when set
    - initObservability() does NOT call Sentry.init when DSN unset (clean no-op)
    - initObservability() calls Sentry.init exactly once (with the DSN) when DSN set; a second call does not re-init (initialized guard)
    - captureError(err) is a no-op (Sentry.captureException NOT called) when DSN unset
    - captureError(err, ctx) calls Sentry.captureException(err, { extra: ctx }) when DSN set
    - captureWebVital(metric) is a no-op when DSN unset, and a no-op when metric is falsy even if DSN set
    - captureWebVital(metric) with DSN set calls Sentry.captureMessage with title 'Web Vital: <metric.name>', level 'info', tags { web_vital, rating }, and a web_vital context object carrying id/name/value/delta
  </behavior>
  <action>
    Create `src/utils/observability.test.js` as a CRA Jest suite. Mock the SDK with `jest.mock('@sentry/react')` (auto-mock turns `init`, `captureException`, `captureMessage` into jest.fn()) and `import * as Sentry from '@sentry/react'`. Manage `process.env.REACT_APP_SENTRY_DSN` inside `beforeEach`/`afterEach` — delete it by default (the no-DSN env state is the primary assertion), set it only in the enabled-path cases. Save and restore the original env value in afterEach so no test leaks DSN state (mirror the env-cleanup precedent in tests/api/api-handlers.test.mjs lines 75-91).

    Because `initialized` is module-scoped in observability.js, reset it between the disabled-init and enabled-init cases: import the module fresh inside `jest.isolateModules(() => { ... })` (or call `jest.resetModules()` then re-`require`) after setting the DSN, so the enabled path sees `initialized === false`. Assert `Sentry.init` was NOT called in the no-DSN case and called exactly once in the DSN-set case (use `expect(Sentry.init).toHaveBeenCalledTimes(1)` and a second invocation leaving the count at 1).

    Cover the behaviors listed above: gate no-ops (Sentry never touched with no DSN), captureError extra-context shape when enabled, and captureWebVital's captureMessage title/level/tags/contexts when enabled plus its two no-op guards (no DSN, falsy metric). Use a representative metric object with name/rating/id/value/delta. Assert captureMessage arguments with `expect(...).toHaveBeenCalledWith(...)` or `toMatchObject` on the mock call. Do NOT edit src/utils/observability.js — it is the subject under test.
  </action>
  <verify>
    <automated>CI=true npx react-scripts test --watchAll=false src/utils/observability.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `CI=true npx react-scripts test --watchAll=false src/utils/observability.test.js` exits 0
    - A test asserts `Sentry.init` is NOT called when REACT_APP_SENTRY_DSN is unset, and is called exactly once when it is set
    - A test asserts `captureError(err, ctx)` calls `Sentry.captureException` with the context passed under an `extra` key, and is a no-op with no DSN
    - A test asserts `captureWebVital(metric)` calls `Sentry.captureMessage` with a title starting `Web Vital: `, `level: 'info'`, and `tags.web_vital` equal to the metric name; and asserts the two no-op guards (no DSN, falsy metric)
    - The full `npm run test:ci` suite stays green (jest.mock('@sentry/react') does not break CRA Jest isolation)
    - src/utils/observability.js is not modified
  </acceptance_criteria>
  <done>observability.test.js proves the OBS-01 init/captureError wiring and the OBS-03 captureWebVital wiring, including the DSN-gated no-op that is the current env state; test:ci is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ErrorBoundary.test.js — componentDidCatch→captureError bridge + fallback UI (OBS-01)</name>
  <files>src/components/ErrorBoundary.test.js</files>
  <read_first>
    - src/components/ErrorBoundary.js (full — componentDidCatch lines 20-23 calling captureError(error, { componentStack }); getDerivedStateFromError line 16; fallback render lines 27-45 with title "Something went wrong" and "Try again" button)
    - src/utils/observability.js (captureError signature lines 23-28 — the mock target)
    - src/setupTests.js (imports '@testing-library/jest-dom' — matchers available)
    - src/components/roleNav.test.js (repo CRA Jest describe/it convention)
    - .planning/phases/03-observability/03-PATTERNS.md (ErrorBoundary.js SUBJECT section — mock ../utils/observability, console.error-spy precedent, fallback-text assertion)
  </read_first>
  <behavior>
    - When a child component throws during render, ErrorBoundary catches it (does not crash the test) and calls captureError with the thrown error and an object carrying componentStack
    - After catching, the fallback UI renders with the "Something went wrong" title and a "Try again" button
    - A non-throwing child renders normally and captureError is not called
  </behavior>
  <action>
    Create `src/components/ErrorBoundary.test.js` using `@testing-library/react` (`render`, `screen`) — already a project dependency. Mock the bridge with `jest.mock('../utils/observability', () => ({ captureError: jest.fn() }))` and import the mocked `captureError` to assert on it (this keeps `@sentry/react` out of the component test entirely). Define a small in-file `Boom` component that throws during render (e.g. throws a new Error with a fixed message). Render `<ErrorBoundary><Boom /></ErrorBoundary>`.

    React logs caught render errors to `console.error`; suppress the expected noise with a `jest.spyOn(console, 'error').mockImplementation(() => {})` in the throwing test and restore it afterward (mirrors the console-spy precedent noted in 03-PATTERNS.md). Assert `captureError` was called with the thrown error and an object whose `componentStack` is a string (use `expect.objectContaining({ componentStack: expect.any(String) })`). Assert the fallback UI is present via `screen.getByText('Something went wrong')` and the "Try again" button via `screen.getByRole('button', { name: /try again/i })`. Add a second test rendering a non-throwing child that asserts the child content renders and `captureError` was not called. Do NOT edit src/components/ErrorBoundary.js — it is the subject under test.
  </action>
  <verify>
    <automated>CI=true npx react-scripts test --watchAll=false src/components/ErrorBoundary.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `CI=true npx react-scripts test --watchAll=false src/components/ErrorBoundary.test.js` exits 0
    - A test renders a throwing child and asserts the mocked `captureError` was called with the error and an object containing a `componentStack` string
    - A test asserts the fallback UI shows "Something went wrong" and a "Try again" button after a caught error
    - A test asserts a non-throwing child renders and `captureError` is not called
    - The full `npm run test:ci` suite stays green
    - src/components/ErrorBoundary.js is not modified
  </acceptance_criteria>
  <done>ErrorBoundary.test.js proves the OBS-01 componentDidCatch→captureError bridge and the recovery fallback UI; test:ci is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client app → Sentry SaaS | Client error/vitals payloads leave the browser to `https://*.ingest.sentry.io`; must not carry secrets or excess PII |
| build env → client bundle | `REACT_APP_SENTRY_DSN` is inlined at CRA build; the DSN is a client-embeddable identifier (not a secret) but must live in Vercel env, never hardcoded |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | captureError context / captureWebVital contexts | medium | mitigate | Tests assert captureError sends only the caller-supplied context under `extra` (ErrorBoundary sends only `componentStack`) and captureWebVital sends only metric id/name/value/delta + rating — no request bodies, tokens, or user PII |
| T-03-02 | Information Disclosure | REACT_APP_SENTRY_DSN handling | low | mitigate | DSN read only from `process.env.REACT_APP_SENTRY_DSN`; tests never hardcode a real DSN (use a dummy value); no DSN literal in source |
| T-03-03 | Denial of Service | init gating regression | low | mitigate | Tests assert `Sentry.init` is a strict no-op when DSN unset, so the default (no-DSN) production/CI state never initializes the SDK |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs NO packages (RESEARCH Package Legitimacy Audit: no new rows; `@sentry/react` + testing-library already installed via dd6364a) — no supply-chain surface |
</threat_model>

<verification>
- `npm run test:ci` exits 0 with the two new client suites included
- observability.test.js asserts DSN-gated no-op (Sentry never called with no DSN), init-once when set, captureError extra-context, and captureWebVital captureMessage shape + no-op guards
- ErrorBoundary.test.js asserts the componentDidCatch→captureError bridge with componentStack and the fallback UI text
- No production source (observability.js, ErrorBoundary.js) modified
</verification>

<success_criteria>
The code-wiring half of OBS-01 and OBS-03 is automatically verified: client Sentry init is DSN-gated (clean no-op when unset, init-once when set), the ErrorBoundary bridges caught errors to captureException, and web-vitals forward to captureMessage — all under `npm run test:ci`. The event-landing half is carried to 03-03-PLAN.md as a human-verify checkpoint.
</success_criteria>

<output>
Create `.planning/phases/03-observability/03-01-SUMMARY.md` when done.
</output>
