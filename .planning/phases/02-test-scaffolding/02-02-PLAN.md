---
phase: 02-test-scaffolding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/rules/firestore.rules.test.js
autonomous: true
requirements: [TEST-01]
must_haves:
  truths:
    - "Developer can run `npm run test:rules` in a Java-21 environment (CI, or local JDK 21) and the emulator-backed suite passes (TEST-01)"
    - "The suite covers userId scoping, admin override, assignedProperties/assignedDeals access, deal-portal canAccessDeal() inheritance, and activity_log append-only behavior"
    - "The suite reads firestore.rules from disk (readFileSync) so it tests the current on-disk rules, keeping it honest across the Console-paste deploy channel"
    - "A users-block characterization pins role/assignment immutability (self cannot self-grant admin or change role/assignments; admin can) — the explicit safety net for Phase 6/SEC-04 (G-R2)"
    - "Each of the six deal-portal collections has at least a create-allowed / create-denied / read assertion exercising canAccessDeal(), not just deal-messages (G-R1)"
  artifacts:
    - tests/rules/firestore.rules.test.js
  key_links:
    - "initializeTestEnvironment reads readFileSync('firestore.rules','utf8') — the CI safety net for the manual Console-paste rules deploy"
    - "New assertions reuse the seeded fixtures admin-uid / agent-a / other-user / admin-email-agent and deals deal-assigned / deal-other"
    - "npm script test:rules wraps the vitest run in `firebase emulators:exec --only firestore --project rems-rules-test`; the projectId matches the test file"
---

<objective>
Reconcile and verify the shipped Firestore-rules characterization suite (`tests/rules/firestore.rules.test.js`, 7 `it` blocks) against the on-disk `firestore.rules`, then add the two coverage deepenings RESEARCH prioritized: G-R2 (users-block role/assignment immutability — the direct safety net for Phase 6/SEC-04's admin-email fallback removal) and G-R1 (the five deal-portal collections beyond deal-messages that share `canAccessDeal()`). This characterizes the real authorization layer before any later phase edits it.

Purpose: Satisfy TEST-01 — `npm run test:rules` covers userId scoping, admin override, assignment-based access, deal-portal `canAccessDeal()` inheritance, and activity_log append-only. The shipped suite already covers all five categories; the deepenings harden the exact rules Phase 6/SEC-04 will mutate. This is a reconcile-and-verify phase — the suite shipped in commit dd6364a; do NOT rebuild it. Per RESEARCH, the emulator requires Java 21, which this dev host lacks (Java 8); the authoritative pass for TEST-01 is a green CI run (or a local JDK 21), recorded as a Manual-Only verification in 02-VALIDATION.md.
Output: verified + deepened `tests/rules/firestore.rules.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-test-scaffolding/02-RESEARCH.md
@.planning/phases/02-test-scaffolding/02-PATTERNS.md
@firestore.rules
</context>

<artifacts_produced>
## Artifacts this phase produces (new test cases + seed docs in tests/rules/firestore.rules.test.js)

- **users-block role/assignment immutability** (G-R2) — a new `describe('users self-service rules'...)` with `it(...)` blocks asserting: self-create with `role:'admin'` fails; self-create with a non-admin role succeeds; self-update changing `role` fails; self-update changing `assignedProperties`/`assignedDeals` fails; a benign self-update (unchanged role/assignments) succeeds; admin update of another user's role succeeds
- **deal-portal per-collection canAccessDeal()** (G-R1) — a new `describe('deal-portal collections'...)` exercising deal-parties, deal-channels, deal-documents, deal-progress, and deal-lender-pushes (deal-messages already covered), each with read + create-allowed (deal-assigned) + create-denied (deal-other); the write-open collections (deal-parties/channels/documents/progress) additionally assert non-admin update/delete succeed, while deal-lender-pushes asserts non-admin update/delete fail (admin-only)
- **new seed docs** — one seeded doc per portal collection tied to `deal-assigned` (deal-parties/deal-channels/deal-documents/deal-progress/deal-lender-pushes) added to the existing `seed()`

No production code changes (firestore.rules is READ, never edited). No new files. No new packages.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile and verify the shipped rules suite against on-disk firestore.rules (TEST-01)</name>
  <files>tests/rules/firestore.rules.test.js</files>
  <read_first>
    - tests/rules/firestore.rules.test.js (full — the shipped 5-it suite, seed fixtures, emulator init)
    - firestore.rules (full — helpers, users block, portal collections, activity_log)
    - .planning/phases/02-test-scaffolding/02-RESEARCH.md (Characterization — Rules suite table; TEST-01 row; Pitfall 1 Java-21)
    - .planning/phases/02-test-scaffolding/02-PATTERNS.md (firestore.rules.test.js section — emulator-init, fixed-fixture seeding, allow/deny patterns)
    - .planning/phases/02-test-scaffolding/02-VALIDATION.md (Manual-Only table — test:rules requires Java 21)
  </read_first>
  <action>
    Confirm the shipped suite satisfies the TEST-01 contract by mapping each of its seven `it` blocks (measured: `grep -c "  it(" tests/rules/firestore.rules.test.js` → 7) to a success-criterion category: userId scoping (owner reads own / denied other / create-as-self / forged create denied), admin override (admin role doc reads+deletes another's record), admin-email-without-role denied (bonus, supports SEC-04), assignedProperties access (assigned agent reads+updates but cannot delete), assignedDeals + deal-portal `canAccessDeal()` via deal-messages, and activity_log append-only (append-own allowed / forged denied / admin read allowed / agent read denied / update+delete denied). Confirm `initializeTestEnvironment` reads `readFileSync('firestore.rules','utf8')` and the port matches firebase.json (127.0.0.1:8080).

    Attempt `npm run test:rules`. Per RESEARCH Pitfall 1, if it aborts with "firebase-tools no longer supports Java version before 21", that is an environment gap on this host (Java 8), NOT a code defect. Do NOT install Java as a task, do NOT edit the suite to avoid the emulator. Record the Java version observed. The authoritative TEST-01 pass is a green CI run (CI provisions temurin 21) or a local JDK 21 — this is the Manual-Only verification already recorded in 02-VALIDATION.md; note it in the SUMMARY.

    As the host-runnable automated gate (no Java needed), confirm the file parses and its test cases register with `npx vitest list tests/rules` (collection only — does not boot the emulator). If that aborts with a `@rolldown/binding-*` error, run `npm ci` once (RESEARCH Pitfall 2) then retry. Do NOT modify any passing test in this task.
  </action>
  <verify>
    <automated>npx vitest list tests/rules && grep -q "readFileSync('firestore.rules'" tests/rules/firestore.rules.test.js && echo RULES_SUITE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest list tests/rules` exits 0 and lists the suite's `it` blocks (collection succeeds without the emulator)
    - The suite reads rules via `readFileSync('firestore.rules', 'utf8')` (grep confirms) — never inlined rule text
    - Each TEST-01 category (userId scoping, admin override, assignment access, deal-portal canAccessDeal, activity_log append-only) maps to at least one existing `it` block (mapping recorded in SUMMARY)
    - `firestore.rules` is not modified by this task
    - The Java version observed on the host and the fact that the emulator pass is a CI/JDK-21 Manual-Only verification are recorded in the SUMMARY
  </acceptance_criteria>
  <done>The shipped rules suite is confirmed to cover all five TEST-01 categories against the on-disk rules; the emulator pass is delegated to CI/JDK-21 per the Manual-Only record.</done>
</task>

<task type="auto">
  <name>Task 2: Add users-block role/assignment immutability characterization (G-R2 — SEC-04 safety net)</name>
  <files>tests/rules/firestore.rules.test.js</files>
  <read_first>
    - tests/rules/firestore.rules.test.js (lines 25-99 seed fixtures; lines 22-23 authedDb; lines 122-135 allow/deny assertion style; lines 144-149 the admin-email-without-role block to mirror)
    - firestore.rules (lines 35-56 — the users match block: create forbids role:'admin' for self; update requires role, assignedProperties, and assignedDeals unchanged for self; admin bypasses)
    - .planning/phases/02-test-scaffolding/02-PATTERNS.md (users-block pattern to author for G-R2)
  </read_first>
  <action>
    Add a new `describe('users self-service rules', ...)` block using the existing fixtures (`agent-a` role agent with assignedProperties:['prop-assigned'], assignedDeals:['deal-assigned']; `admin-uid` role admin) and `authedDb(uid, claims)`. Follow the allow/deny assertion pattern (assertSucceeds / assertFails from @firebase/rules-unit-testing). Characterize the CURRENT on-disk rules behavior:

    - self-create-cannot-be-admin: `authedDb('self-new')` setDoc(users/self-new, {userId:'self-new', email:..., role:'admin', assignedProperties:[], assignedDeals:[]}) → assertFails.
    - self-create allowed for a non-admin role: `authedDb('self-new2')` setDoc(users/self-new2, {userId:'self-new2', role:'agent', assignedProperties:[], assignedDeals:[]}) → assertSucceeds.
    - self-update cannot change role: `authedDb('agent-a')` updateDoc(users/agent-a, {role:'admin'}) → assertFails.
    - self-update cannot change assignments: `authedDb('agent-a')` updateDoc(users/agent-a, {assignedProperties:['prop-x']}) → assertFails; updateDoc(users/agent-a, {assignedDeals:['deal-x']}) → assertFails.
    - benign self-update succeeds: `authedDb('agent-a')` updateDoc(users/agent-a, {email:'agent-new@example.com'}) → assertSucceeds (role and both assignment arrays unchanged, so the rule's equality guards pass).
    - admin can change another user's role/assignments: `authedDb('admin-uid')` updateDoc(users/agent-a, {role:'buyer', assignedProperties:['prop-assigned','prop-2']}) → assertSucceeds.

    Match the auth claims style of the existing blocks (pass `{ email: ... }` where the neighboring blocks do). Do not edit existing tests or the seed. This block only reads the on-disk rules; do not touch firestore.rules.
  </action>
  <verify>
    <automated>npx vitest list tests/rules && grep -q "users self-service rules" tests/rules/firestore.rules.test.js && echo USERS_BLOCK_ADDED</automated>
  </verify>
  <acceptance_criteria>
    - A `describe('users self-service rules', ...)` block exists in the file
    - It contains assertFails for self-create with role:'admin', for self-update changing role, and for self-update changing assignedProperties and assignedDeals
    - It contains assertSucceeds for a benign self-update (unchanged role/assignments) and for an admin updating another user's role
    - `npx vitest list tests/rules` exits 0 (the file still parses and collects)
    - firestore.rules is not modified
    - Emulator pass is confirmed via CI/JDK-21 (Manual-Only) and noted in SUMMARY
  </acceptance_criteria>
  <done>The users match block is characterized: self-service cannot escalate role or reassign; admin can — the explicit regression net for the Phase 6/SEC-04 admin-email fallback removal.</done>
</task>

<task type="auto">
  <name>Task 3: Broaden deal-portal coverage to all six collections via canAccessDeal() (G-R1)</name>
  <files>tests/rules/firestore.rules.test.js</files>
  <read_first>
    - tests/rules/firestore.rules.test.js (lines 89-92 the existing deal-messages seed doc; lines 164-181 the deal-messages canAccessDeal block to replicate; lines 25-99 seed structure)
    - firestore.rules (lines 91-136 — canAccessDeal + the six portal match blocks: deal-parties/channels/documents/progress allow read/update/delete via canAccessDeal; deal-messages/deal-lender-pushes restrict update/delete to admin)
    - .planning/phases/02-test-scaffolding/02-PATTERNS.md (portal canAccessDeal pattern to replicate for G-R1 — the update/delete branch distinction)
  </read_first>
  <action>
    Extend `seed()` with one document per untested portal collection tied to `deal-assigned`, mirroring the existing `deal-messages/message-assigned` seed doc (each with a `dealId: 'deal-assigned'` field): `deal-parties/party-assigned`, `deal-channels/channel-assigned`, `deal-documents/document-assigned`, `deal-progress/progress-assigned`, `deal-lender-pushes/lender-push-assigned`.

    Add a new `describe('deal-portal collections', ...)` block. For EACH of the five collections, as `authedDb('agent-a', { email:'agent@example.com' })` (assigned to deal-assigned), copy the three-assertion deal-messages shape: read the seeded doc → assertSucceeds; addDoc with `{ dealId:'deal-assigned', ... }` → assertSucceeds; addDoc with `{ dealId:'deal-other', ... }` → assertFails. Then add the update/delete branch that matches each collection's rule:
    - deal-parties, deal-channels, deal-documents, deal-progress (write-open via canAccessDeal): updateDoc the seeded doc → assertSucceeds; deleteDoc the seeded doc → assertSucceeds.
    - deal-lender-pushes (admin-only update/delete): updateDoc the seeded doc as agent-a → assertFails; deleteDoc as agent-a → assertFails; and as `authedDb('admin-uid', { email:'admin@example.com' })` updateDoc → assertSucceeds.

    Reuse the existing fixtures and the addDoc/updateDoc/deleteDoc imports already at the top of the file. Do not change the port, projectId, or existing tests. firestore.rules is READ only.
  </action>
  <verify>
    <automated>npx vitest list tests/rules && grep -q "deal-portal collections" tests/rules/firestore.rules.test.js && grep -q "party-assigned" tests/rules/firestore.rules.test.js && echo PORTAL_COVERAGE_ADDED</automated>
  </verify>
  <acceptance_criteria>
    - `seed()` creates a doc for each of deal-parties, deal-channels, deal-documents, deal-progress, and deal-lender-pushes, each with `dealId: 'deal-assigned'`
    - A `describe('deal-portal collections', ...)` block asserts, per collection, read-allowed + create-allowed for deal-assigned and create-denied for deal-other
    - deal-parties/channels/documents/progress assert non-admin update and delete succeed; deal-lender-pushes asserts non-admin update/delete fail and admin update succeeds
    - `npx vitest list tests/rules` exits 0
    - firestore.rules is not modified
    - Emulator pass confirmed via CI/JDK-21 (Manual-Only) and noted in SUMMARY
  </acceptance_criteria>
  <done>All six deal-portal collections exercise canAccessDeal() inheritance with the correct write-branch distinctions; TEST-01's "deal-portal canAccessDeal inheritance" criterion is covered breadth-wise, not just via deal-messages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client SDK → Firestore rules engine | Firestore rules are the real authorization layer (CLAUDE.md); every read/write crosses this boundary and is allowed/denied by firestore.rules |
| repo firestore.rules → published rules | Rules deploy by manual Console paste, invisible to CI; the emulator-backed suite reading the on-disk file is the only automated guard against drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02R-01 | Elevation of Privilege | users match block (role/assignment self-mutation) | high | mitigate | G-R2 tests assert self cannot grant admin or change role/assignments; this directly guards the Phase 6/SEC-04 admin-email-fallback removal from a silent escalation regression |
| T-02R-02 | Information Disclosure | canAccessDeal() portal inheritance | medium | mitigate | G-R1 tests assert create-denied for a deal the user is not assigned to, across all six portal collections — not just deal-messages |
| T-02R-03 | Repudiation | activity_log append-only (already covered) | low | accept | Shipped suite asserts update/delete denied for everyone incl. admin; no change this plan |
| T-02R-04 | Tampering | rules-suite drift from on-disk rules | medium | mitigate | Suite reads readFileSync('firestore.rules') so it always tests the current rules; verified in Task 1 |
| T-02R-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs NO packages (RESEARCH Package Legitimacy Audit: no new rows). `npm ci` (if needed) only reinstalls the locked tree. |
</threat_model>

<verification>
- `npx vitest list tests/rules` exits 0 (file parses, all blocks collect) — host-runnable without Java
- Suite reads firestore.rules from disk (readFileSync)
- users self-service block asserts role/assignment immutability for self and admin bypass (G-R2)
- deal-portal block covers all six collections with correct update/delete branches (G-R1)
- Manual/CI: `npm run test:rules` passes in a Java-21 environment (CI temurin 21 or local JDK 21) — the authoritative TEST-01 pass, recorded in 02-VALIDATION.md Manual-Only
- firestore.rules unmodified
</verification>

<success_criteria>
TEST-01 satisfied: `npm run test:rules` (in CI/JDK-21) passes emulator-backed tests covering userId scoping, admin override, assignedProperties/assignedDeals access, deal-portal canAccessDeal() inheritance across all six portal collections, and activity_log append-only — with the users-block role/assignment immutability characterized as the SEC-04 safety net.
</success_criteria>

<output>
Create `.planning/phases/02-test-scaffolding/02-02-SUMMARY.md` when done.
</output>
