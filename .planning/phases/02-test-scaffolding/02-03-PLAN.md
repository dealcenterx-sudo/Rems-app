---
phase: 02-test-scaffolding
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - docs/TESTING.md
autonomous: true
requirements: [TEST-03]
must_haves:
  truths:
    - "CI runs both new suites (test:api, test:rules) alongside the existing check:constants -> lint -> test:ci -> build pipeline (TEST-03)"
    - "`npm run test:ci` (CRA Jest) behaves exactly as before — it sweeps only src/ and never picks up tests/ (TEST-03)"
    - "A developer can find the documented prerequisites for running the suites locally: Java 21 for test:rules, and a clean `npm ci` on the host for the correct Vitest rolldown binding"
    - "The authoritative proof for the emulator-backed rules suite is a green CI run (CI provisions temurin 21)"
  artifacts:
    - docs/TESTING.md
  key_links:
    - ".github/workflows/ci.yml runs 'API tests' (npm run test:api) and 'Firestore rules tests' (npm run test:rules) after 'Test' (test:ci) and before 'Build'"
    - "CRA Jest roots are locked to <rootDir>/src; tests/ (the Vitest suites) is structurally outside the sweep"
    - "docs/TESTING.md documents the two run prerequisites and points to the two suites and their npm scripts"
---

<objective>
Verify TEST-03: CI runs the two new Vitest suites alongside the existing pipeline without disturbing CRA's Jest, and `npm run test:ci` behaves exactly as before (sweeps only `src/`). Then document the local-run prerequisites (Java 21 for `test:rules`; clean `npm ci` for the correct Vitest binding) in `docs/TESTING.md` so criterion 1 is reproducible off-CI.

Purpose: Satisfy TEST-03 — the CRA-Jest isolation (criterion 3) is the fragile invariant of this phase; this plan proves the isolation holds after the deepenings in plans 01 and 02, and captures the Java-21/`npm ci` prerequisites RESEARCH flagged so the next developer isn't blocked. This is a reconcile-and-verify phase; the CI wiring and scripts shipped in commit dd6364a — confirm the contract, do not rewrite it.
Output: verified CI/isolation contract; new `docs/TESTING.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-test-scaffolding/02-RESEARCH.md
@.planning/phases/02-test-scaffolding/02-PATTERNS.md
@.github/workflows/ci.yml
</context>

<artifacts_produced>
## Artifacts this phase produces

- **docs/TESTING.md** (NEW) — a testing note documenting: the three test lanes (CRA Jest `test:ci` over `src/`; Vitest `test:api` over `tests/api`; emulator-backed Vitest `test:rules` over `tests/rules`), the CRA-Jest isolation invariant (roots locked to `src/`), the Java 21 prerequisite for `test:rules` (CI provisions temurin 21; local dev needs JDK 21+), and the clean-`npm ci` fix for the Vitest rolldown native-binding arch artifact
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Verify CI wiring and CRA-Jest isolation (TEST-03)</name>
  <files>.github/workflows/ci.yml, package.json</files>
  <read_first>
    - .github/workflows/ci.yml (full — step order check:constants -> lint -> test:ci -> API tests -> Firestore rules tests -> build; the Setup Java temurin 21 step; the Build step's deliberate CI:false)
    - package.json (scripts block — test:ci is `react-scripts test --watchAll=false`; test:api is `vitest run tests/api`; test:rules wraps `firebase emulators:exec ... "vitest run tests/rules"`)
    - .planning/phases/02-test-scaffolding/02-RESEARCH.md (TEST-03 row; Pattern 1 CRA-Jest isolation via roots; Assumptions Log A1)
    - .planning/phases/02-test-scaffolding/02-PATTERNS.md (package.json scripts section — test:ci is frozen; ci.yml section)
  </read_first>
  <action>
    Confirm the CI pipeline order and that both new suites are wired: `.github/workflows/ci.yml` contains a step running `npm run test:api` and a step running `npm run test:rules`, both positioned after the `npm run test:ci` step and before the `npm run build` step, and a `Setup Java` step pinning temurin `java-version: 21` (required by firebase-tools 15.x for the emulator). Confirm the Build step keeps its deliberate `CI: false` parity comment intact — do NOT change it.

    Confirm the CRA-Jest isolation invariant is untouched: `package.json` `test:ci` is still exactly `react-scripts test --watchAll=false` (not renamed, not repointed at `tests/`). Run `CI=true npm run test:ci` and confirm it passes and sweeps only `src/` — the `tests/` Vitest files (`.mjs` API suite, `.js` rules suite) must NOT appear in the run. If the run does not print file paths, additionally confirm structurally that CRA's resolved Jest `roots` is `<rootDir>/src` (RESEARCH Pattern 1) so `tests/` is out of scope.

    Do NOT modify package.json scripts or ci.yml — this task verifies the shipped contract. If a genuine wiring gap is found (e.g. a suite step missing), record it in the SUMMARY and stop for the developer rather than silently rewriting CI.
  </action>
  <verify>
    <automated>CI=true npm run test:ci && grep -q "npm run test:api" .github/workflows/ci.yml && grep -q "npm run test:rules" .github/workflows/ci.yml && grep -q "java-version: 21" .github/workflows/ci.yml && node -e "const s=require('./package.json').scripts; process.exit(s['test:ci']==='react-scripts test --watchAll=false'?0:1)" && echo CI_ISOLATION_OK</automated>
  </verify>
  <acceptance_criteria>
    - `CI=true npm run test:ci` exits 0 and runs only `src/` suites (no `tests/` file swept)
    - `.github/workflows/ci.yml` contains steps running `npm run test:api` and `npm run test:rules`, both after the `test:ci` step and before the `build` step
    - `.github/workflows/ci.yml` contains a Setup Java step with `java-version: 21`
    - package.json `test:ci` is exactly `react-scripts test --watchAll=false` (frozen — unchanged)
    - Neither package.json nor ci.yml is modified by this task
  </acceptance_criteria>
  <done>CI runs both new suites alongside the existing lint -> test -> build pipeline, and CRA Jest (`test:ci`) still behaves exactly as before, sweeping only src/.</done>
</task>

<task type="auto">
  <name>Task 2: Document local-run prerequisites in docs/TESTING.md</name>
  <files>docs/TESTING.md</files>
  <read_first>
    - docs/ (list existing docs to match tone/format — e.g. SAAS_UPGRADE_PLAN.md, DATABASE_SCHEMA.md)
    - .planning/phases/02-test-scaffolding/02-RESEARCH.md (Environment Availability table; Pitfall 1 Java 21; Pitfall 2 rolldown binding; Open Questions 1)
    - .planning/phases/02-test-scaffolding/02-VALIDATION.md (Manual-Only table — the Java 21 / CI-proof rows)
    - package.json (the three test scripts to reference by name)
  </read_first>
  <action>
    Create docs/TESTING.md documenting how to run the test suites and their prerequisites. Include, in prose (no secrets, variable/tool names only):
    - The three test lanes and their commands: `npm run test:ci` (CRA Jest, unit tests under `src/`), `npm run test:api` (Vitest, in-process serverless-handler tests under `tests/api`), `npm run test:rules` (Vitest under `firebase emulators:exec`, emulator-backed Firestore rules tests under `tests/rules`).
    - The CRA-Jest isolation invariant: `test:ci` is frozen and sweeps only `src/`; new non-CRA tests must live under `tests/` (never `src/`) or they break the isolation. Do not rename or repoint `test:ci`.
    - Prerequisite 1 — Java 21: `test:rules` boots the Firestore emulator via firebase-tools 15.x, which requires JDK 21+. CI provisions temurin 21 automatically; local runs on an older JDK abort with a "no longer supports Java version before 21" error. State that a green CI run is the authoritative proof for the emulator-backed suite when a local JDK 21 is unavailable.
    - Prerequisite 2 — clean `npm ci`: Vitest 4.x pulls a platform-specific rolldown native binding; a `node_modules` tree populated for a different CPU architecture aborts with a `@rolldown/binding-*` module-not-found error. A clean `npm ci` (or `rm -rf node_modules && npm install`) on the actual host resolves the correct binding. Note this is an environment artifact, not a repo defect.
    - A short pointer to `.github/workflows/ci.yml` as the source of truth for CI step order.
    Match the heading/tone conventions of the existing docs/ files. Do NOT paste any API key, secret, or the admin email literal into this file (the check:constants gate forbids the admin email outside its three allowlisted paths).
  </action>
  <verify>
    <automated>test -f docs/TESTING.md && grep -q "test:rules" docs/TESTING.md && grep -q "test:api" docs/TESTING.md && grep -qi "java" docs/TESTING.md && grep -qi "npm ci" docs/TESTING.md && npm run check:constants && echo TESTING_DOC_OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/TESTING.md exists and references all three scripts (`test:ci`, `test:api`, `test:rules`) by name
    - It documents the Java 21 prerequisite for `test:rules` and states CI is the authoritative emulator-suite proof
    - It documents the clean-`npm ci` fix for the Vitest rolldown binding arch artifact
    - It states the CRA-Jest isolation invariant (test:ci sweeps only src/; new non-CRA tests go under tests/)
    - `npm run check:constants` exits 0 (docs/TESTING.md introduces no disallowed admin-email literal)
  </acceptance_criteria>
  <done>docs/TESTING.md documents the three test lanes, the CRA-Jest isolation invariant, and the Java 21 + clean-npm-ci run prerequisites; the constants gate stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → main | CI is the merge gate; if the new suites are not actually wired, a regression in rules/handlers could reach production undetected |
| CRA Jest ↔ Vitest | The two runners must stay isolated; a leak (a Vitest file swept by CRA Jest, or test:ci repointed) breaks the existing pipeline (criterion 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02C-01 | Tampering | CI pipeline wiring | medium | mitigate | Task 1 asserts both suite steps are present and ordered after test:ci and before build; a missing step is surfaced, not silently accepted |
| T-02C-02 | Denial of Service | CRA-Jest isolation regression | low | mitigate | Task 1 confirms test:ci is frozen and sweeps only src/; docs/TESTING.md records the invariant so future changes don't repoint it |
| T-02C-03 | Information Disclosure | docs/TESTING.md content | low | mitigate | Task 2 forbids secrets/admin-email in the doc; check:constants gate enforces the admin-email prohibition automatically |
| T-02C-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs NO packages (RESEARCH Package Legitimacy Audit: no new rows) — no supply-chain surface |
</threat_model>

<verification>
- `CI=true npm run test:ci` exits 0 and sweeps only src/ (tests/ not picked up)
- ci.yml runs npm run test:api and npm run test:rules after test:ci and before build; Setup Java pins temurin 21
- package.json test:ci is frozen (`react-scripts test --watchAll=false`)
- docs/TESTING.md documents the three lanes, the isolation invariant, and the Java 21 + npm ci prerequisites
- `npm run check:constants` green (no disallowed literal in the new doc)
</verification>

<success_criteria>
TEST-03 satisfied: CI runs the rules + API suites alongside the existing lint -> test -> build pipeline, `npm run test:ci` (CRA Jest) behaves exactly as before, and the local-run prerequisites (Java 21, clean npm ci) are documented so the emulator-backed suite is reproducible off-CI.
</success_criteria>

<output>
Create `.planning/phases/02-test-scaffolding/02-03-SUMMARY.md` when done.
</output>
