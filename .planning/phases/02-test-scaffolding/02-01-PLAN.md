---
phase: 02-test-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/api/api-handlers.test.mjs
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "Developer can run `npm run test:api` and the suite exits 0 (TEST-02)"
    - "send-email, accept-invite, and lead-intake each have assertions for auth validation, payload validation, and at least one error path"
    - "Every 400 payload assertion checks the zod contract: body.error == 'Invalid request payload' and body.details contains an object with the offending field path"
    - "send-email and accept-invite each have a 200 success assertion that locks the current response payload shape before Phase 4/SEC-01 rewrites validation (G-A1)"
    - "accept-invite covers the missing-token 401 branch and the 404 invite-not-found branch (G-A2)"
    - "Each of the three required handlers rejects a non-POST request with the current method-guard status (G-A3)"
  artifacts:
    - tests/api/api-handlers.test.mjs
  key_links:
    - "The suite loads the real handler modules via requireFromRoot.resolve('./api/<name>.js') and drives them through node-mocks-http, so tests track the on-disk handlers"
    - "New tests reuse the shipped mockFirebaseAdmin / mockCloudinary / loadHandler / invoke harness rather than a new mocking style"
---

<objective>
Reconcile and verify the shipped serverless-handler characterization suite (`tests/api/api-handlers.test.mjs`, 16 tests) against the live handlers, then deepen it with the low-cost characterizations RESEARCH flagged (G-A1 200-success payload shapes for send-email/accept-invite, G-A2 accept-invite missing-token 401 + 404 not-found, G-A3 non-POST method guards). This locks the current behavior of the two riskiest surfaces before Phase 4 (SEC-01) rewrites payload validation.

Purpose: Satisfy TEST-02 — `npm run test:api` covers auth validation, payload validation, and error paths for send-email, accept-invite, and lead-intake. The suite already covers the three categories; the deepenings pin the success payloads and untested branches so a later hardening phase cannot silently regress them. This is a reconcile-and-verify phase — the infrastructure shipped in commit dd6364a; do NOT rebuild it.
Output: verified + deepened `tests/api/api-handlers.test.mjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-test-scaffolding/02-RESEARCH.md
@.planning/phases/02-test-scaffolding/02-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (new test cases added to tests/api/api-handlers.test.mjs)

- **send-email 200 success** — new `it(...)` asserting status 200 and body shape `{ ok: true, id }` (G-A1)
- **accept-invite 200 success** — new `it(...)` asserting status 200 and body shape `{ ok: true, dealId, propertyAddress }` (G-A1)
- **accept-invite missing-token 401** — new `it(...)` asserting status 401, `body.error === 'Missing auth token'` (G-A2)
- **accept-invite invite-not-found 404** — new `it(...)` asserting status 404, `body.error === 'Invite not found or already used'` (G-A2)
- **method-guard 405** — new `it(...)` per handler (send-email, accept-invite, lead-intake) asserting a GET returns 405 `{ error: 'Method not allowed' }` (G-A3)

No production code changes. No new files. No new packages.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile and verify the shipped API suite against the live handlers (TEST-02)</name>
  <files>tests/api/api-handlers.test.mjs</files>
  <read_first>
    - tests/api/api-handlers.test.mjs (full — the shipped 16-test suite)
    - api/send-email.js (405/503/401/400/502/200 branches)
    - api/accept-invite.js (405/503/401/400/404/403/200 branches)
    - api/lead-intake.js (405/503/401/400/200 branches)
    - api/_lib/validate.js (the zod schemas + validateBody `details[].path` contract)
    - .planning/phases/02-test-scaffolding/02-RESEARCH.md (Characterization — API suite table; TEST-02 row)
    - .planning/phases/02-test-scaffolding/02-PATTERNS.md (api-handlers.test.mjs section — the four harness patterns)
  </read_first>
  <action>
    Confirm the shipped suite satisfies the TEST-02 contract: for each of send-email, accept-invite, and lead-intake there is at least one auth-validation assertion (401), one payload-validation assertion (400 asserting body.error 'Invalid request payload' and a body.details entry with the offending field path), and one error-path assertion (send-email 502, accept-invite 503+403, lead-intake 503+401). Confirm the module-cache mocking harness (mockFirebaseAdmin / mockCloudinary / loadHandler / invoke) and the fetch auth-token stub pattern match what the live handlers require.

    Run `npm run test:api`. Per RESEARCH Pitfall 2, if it aborts before running any test with a `Cannot find module '@rolldown/binding-*'` error, that is a cross-architecture node_modules artifact, NOT a code defect — run `npm ci` once to resolve the platform-correct binding, then re-run `npm run test:api`. Do NOT hand-pin a binding and do NOT edit the tests to work around it.

    Do NOT modify any passing test. This task only confirms the contract holds against the on-disk handlers; the deepening additions happen in Task 2. If a genuine contract mismatch is found (a handler branch a test asserts no longer exists), record it in the SUMMARY rather than silently rewriting the handler.
  </action>
  <verify>
    <automated>npm run test:api</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:api` exits 0
    - The suite contains `describe('api/send-email'...)`, `describe('api/accept-invite'...)`, and `describe('api/lead-intake'...)` blocks
    - Each of the three required handlers has a 401 auth assertion, a 400 payload assertion checking `body.error === 'Invalid request payload'` and a `body.details` entry with the offending field path, and at least one error-path status assertion
    - No production handler file (api/send-email.js, api/accept-invite.js, api/lead-intake.js) is modified by this task
  </acceptance_criteria>
  <done>The shipped API suite is confirmed green against the live handlers and the TEST-02 three-category contract holds for all three required handlers.</done>
</task>

<task type="auto">
  <name>Task 2: Deepen API characterization — 200 payloads, missing-token/404, method guards (G-A1/G-A2/G-A3)</name>
  <files>tests/api/api-handlers.test.mjs</files>
  <read_first>
    - tests/api/api-handlers.test.mjs (lines 47-59 `invoke` helper; lines 61-73 `makeCollection`; lines 106-125 the 400 payload shape; lines 127-147 the send-email 502 double-fetch stub; lines 167-206 accept-invite invalid-token + missing-payload blocks; lines 208-237 the 403 different-email block; lines 286-321 the ONLY existing 200 happy-path template)
    - api/send-email.js (200 returns `{ ok: true, id: data.id }`; auth via fetch lookup then a second fetch to resend)
    - api/accept-invite.js (missing token -> 401 'Missing auth token' at line 25; empty deal-parties -> 404 'Invite not found or already used' at line 49; 200 returns `{ ok: true, dealId, propertyAddress }` at lines 98-102; uses getAuthAdmin().verifyIdToken)
    - api/lead-intake.js (405 'Method not allowed' at lines 9-11)
    - .planning/phases/02-test-scaffolding/02-PATTERNS.md (deepening table G-A1/G-A2/G-A3 → analog line anchors; 200 happy-path pattern rule)
  </read_first>
  <action>
    Append new `it(...)` blocks to the existing `describe` blocks, copying the shipped harness verbatim (mockFirebaseAdmin / mockCloudinary / loadHandler / invoke, per-test env set inside the test body). Characterization means asserting the ACTUAL current status/body read from each handler — not a desired contract.

    G-A1 (200 success — copy the lead-intake 200 template at lines 286-321):
    - send-email: set RESEND_API_KEY, stub fetch to resolve twice — first the identitytoolkit lookup `{ ok:true, json: async () => ({ users:[{ email:'agent@example.com' }] }) }`, then the resend send `{ ok:true, json: async () => ({ id:'email-1' }) }`; invoke with a Bearer header and a full valid payload (to/subject/text); assert status 200 and body equals `{ ok: true, id: 'email-1' }`.
    - accept-invite: mockFirebaseAdmin with getAuthAdmin().verifyIdToken resolving `{ uid:'user-1', email:'buyer@example.com' }` and a getDb whose `deal-parties` query returns one party doc whose email matches `buyer@example.com` and has a dealId, and whose `deals` doc get returns `{ userId:'owner-1', propertyAddress:'123 Main St' }`; provide FieldValue.arrayUnion; invoke with Bearer header + `{ inviteToken:'invite-1' }`; assert status 200 and body equals `{ ok: true, dealId: <seeded dealId>, propertyAddress: '123 Main St' }`. Reuse `makeCollection` for the users/deals/notifications collections.

    G-A2 (accept-invite branches — copy the invalid-token block at lines 167-184):
    - missing-token 401: mockFirebaseAdmin with a working getDb/getAuthAdmin; invoke with NO authorization header and `{ inviteToken:'invite-1' }`; assert status 401 and `body.error === 'Missing auth token'`.
    - invite-not-found 404: verifyIdToken resolves a valid decoded token; getDb `deal-parties` query returns `{ empty:true, docs:[] }`; invoke with Bearer header + `{ inviteToken:'missing' }`; assert status 404 and `body.error === 'Invite not found or already used'`.

    G-A3 (method guards — pass `{ method: 'GET' }` to `invoke`):
    - For send-email, accept-invite, and lead-intake: load the handler, invoke with `{ method: 'GET' }`, assert status 405 and `body.error === 'Method not allowed'`. Set the minimal env each handler checks before the method guard is unnecessary — the method check is first in every handler (lines 9-11), so no env/mock setup is required.

    Do not renumber, reorder, or edit existing tests. Do not add a vitest config file. All additions stay inside `tests/api/api-handlers.test.mjs`.
  </action>
  <verify>
    <automated>npm run test:api && grep -c "it(" tests/api/api-handlers.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:api` exits 0 after the additions
    - A send-email `it(...)` asserts status 200 and body `{ ok: true, id: 'email-1' }`
    - An accept-invite `it(...)` asserts status 200 and a body containing `ok: true`, `dealId`, and `propertyAddress`
    - An accept-invite `it(...)` asserts status 401 with `body.error === 'Missing auth token'` when no authorization header is sent
    - An accept-invite `it(...)` asserts status 404 with `body.error === 'Invite not found or already used'` on an empty deal-parties query
    - send-email, accept-invite, and lead-intake each have an `it(...)` asserting a GET returns status 405 with `body.error === 'Method not allowed'`
    - `grep -c "it(" tests/api/api-handlers.test.mjs` returns a count strictly greater than the pre-task count (recorded in SUMMARY)
    - No production handler file is modified
  </acceptance_criteria>
  <done>The API suite now pins the send-email/accept-invite 200 payload shapes, the accept-invite missing-token and not-found branches, and the non-POST method guards; `npm run test:api` is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → serverless handler | Untrusted request bodies and auth tokens cross here; the handlers are the enforcement point for auth (Firebase ID token / x-api-key) and payload shape (zod) |
| test suite → live handler | The suite is a regression guard; if it drifts from the on-disk handlers it stops protecting the boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Spoofing | Handler auth checks (401 branches) | low | mitigate | Characterization tests assert 401 for missing/invalid tokens (send-email, accept-invite) and wrong x-api-key (lead-intake); locking these before SEC-01 prevents a silent auth regression |
| T-02-02 | Tampering | zod payload validation (400 branches) | low | mitigate | Tests assert the uniform `{ error:'Invalid request payload', details:[{path}] }` contract; the G-A1 200-shape tests pin the accepted-payload behavior so SEC-01's rewrite is diffable |
| T-02-03 | Information Disclosure | error-path bodies (502/503/500) | low | accept | Tests assert generic error strings that don't leak internals; no new surface introduced by this plan |
| T-02-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs NO packages (RESEARCH Package Legitimacy Audit: no new rows) — no supply-chain surface. `npm ci` in Task 1 only reinstalls the already-locked tree. |
</threat_model>

<verification>
- `npm run test:api` exits 0 (reconciled + deepened suite)
- All three required handlers have auth (401), payload (400 with details path), and error-path assertions
- send-email + accept-invite have 200-success payload-shape assertions; accept-invite has missing-token 401 and 404 not-found; all three have a 405 method-guard assertion
- No production handler file modified
</verification>

<success_criteria>
TEST-02 satisfied: `npm run test:api` passes, covering auth validation, payload validation, and error paths for send-email, accept-invite, and lead-intake, with the current success payloads and previously-untested branches now characterized.
</success_criteria>

<output>
Create `.planning/phases/02-test-scaffolding/02-01-SUMMARY.md` when done.
</output>
