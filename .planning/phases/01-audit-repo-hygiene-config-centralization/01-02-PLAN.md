---
phase: 01-audit-repo-hygiene-config-centralization
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - screenshot.js
  - package.json
  - .gitignore
  - api/health.js
autonomous: false
requirements: [HYG-01, HYG-02, HYG-03]
user_setup:
  - service: vercel
    why: "D-07 — verify whether external monitors hit /api/health before the gating change deploys"
    dashboard_config:
      - task: "Check Vercel dashboard logs for recent traffic to /api/health"
        location: "Vercel Dashboard → rems-app → Logs (filter path /api/health)"
must_haves:
  truths:
    - "The stray archive rems-project-source-2026-04-09 and its .zip are absent from the working tree (HYG-01)"
    - "screenshot.js is no longer tracked by git and is absent from the working tree (HYG-02)"
    - "An unauthenticated GET to /api/health returns exactly {\"status\":\"ok\"} and reveals nothing about env/infra (HYG-03)"
    - "A valid admin Firebase ID token unlocks the same five diagnostic flags as before the change (D-08)"
    - "A valid non-admin token receives the identical bare response — no 401/403 differential (D-06)"
    - "The user has checked Vercel logs for /api/health traffic and the result is captured for the audit (D-07)"
  artifacts:
    - api/health.js
  key_links:
    - "api/health.js imports ADMIN_EMAIL and FIREBASE_API_KEY from api/_lib/config.js (created in plan 01-01)"
    - "api/health.js verifies tokens via the Google identitytoolkit accounts:lookup pattern, not the Admin SDK"
    - ".gitignore lines 26-27 cover future rems-project-source-* exports"
---

<objective>
Remove the stale source archive (untracked — plain rm) and the obsolete screenshot.js (tracked — git rm, plus its orphaned puppeteer devDependency), then auth-gate api/health.js so unauthenticated and non-admin callers learn nothing about env/infra while a valid admin token still returns today's diagnostic flags. A mandatory human checkpoint (D-07) has the user check Vercel logs for /api/health traffic before the gating change deploys.

Purpose: Closes the repo-hygiene findings (HYG-01, HYG-02) and the env-schema-disclosure finding (HYG-03) with zero behavioral risk to auth flows. Honors D-06 (split response), D-08 (same flags behind the gate), D-07 (traffic-check pause), D-13 (screenshot.js deleted), D-14 (archive removed, no history rewrite).
Output: archive + screenshot.js removed; puppeteer devDep dropped; api/health.js gated; D-07 result captured for the audit/changelog (recorded in plan 01-03).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audit-repo-hygiene-config-centralization/01-CONTEXT.md
@.planning/phases/01-audit-repo-hygiene-config-centralization/01-RESEARCH.md
@.planning/phases/01-audit-repo-hygiene-config-centralization/01-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (new symbols — exclude from drift verification)

- **api/health.js** (MODIFY): split-response handler — bare `{ status: 'ok' }` for unauthenticated/non-admin callers; admin-gated diagnostics object (`resendConfigured`, `firebaseAdminConfigured`, `leadIntakeConfigured`, `adminInit`, `matchingKeyNames`)
- Deletions (not symbols): `screenshot.js`, `rems-project-source-2026-04-09/` + `.zip`, `puppeteer` devDependency
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Remove the stale archive, screenshot.js, and the orphaned puppeteer devDependency</name>
  <read_first>
    - .gitignore (lines 26-27 covering rems-project-source-* — verify coverage, no edit expected)
    - package.json (devDependencies — puppeteer)
    - .planning/phases/01-audit-repo-hygiene-config-centralization/01-RESEARCH.md (Pitfall 1 — the archive is untracked; git rm will FAIL; Hygiene commands section)
    - .planning/phases/01-audit-repo-hygiene-config-centralization/01-PATTERNS.md (Deletion Notes)
  </read_first>
  <action>
    HYG-01 / D-14 — the archive rems-project-source-2026-04-09/ and rems-project-source-2026-04-09.zip are UNTRACKED and gitignored (they were never committed — RESEARCH Pitfall 1). Remove them with plain `rm -rf` — do NOT use `git rm` (it errors "pathspec did not match") and NEVER `git add -f`. If already absent, treat as satisfied. Confirm .gitignore lines 26-27 already cover future `rems-project-source-*` exports (no edit expected). Note in the SUMMARY that this deletion is not git-recoverable (D-14 authorized no pre-removal copy).

    HYG-02 / D-13 — screenshot.js is TRACKED (commit 0b3cb88); its hardcoded /root/... paths can't run locally and Phase 8 uses fresh tooling. Remove with `git rm screenshot.js` (history keeps it recoverable). In the same change, drop the now-orphaned puppeteer devDependency (`npm uninstall puppeteer` — screenshot.js was its only consumer, RESEARCH Open Question 2) so package.json/package-lock.json no longer carry ~200MB of dead install weight. If either is already gone, treat as satisfied.
  </action>
  <verify>
    <automated>test ! -e rems-project-source-2026-04-09 && test ! -e rems-project-source-2026-04-09.zip && test ! -f screenshot.js && test "$(git ls-files screenshot.js | wc -l | tr -d ' ')" = "0" && node -e "const p=require('./package.json'); process.exit(p.devDependencies && p.devDependencies.puppeteer ? 1 : 0)" && echo HYGIENE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `rems-project-source-2026-04-09` and its `.zip` do not exist in the working tree
    - `git ls-files screenshot.js` returns nothing and screenshot.js does not exist on disk
    - package.json devDependencies has no puppeteer entry
    - .gitignore still matches `rems-project-source-*` (future exports stay ignored)
  </acceptance_criteria>
  <done>Archive and screenshot.js gone; puppeteer removed from devDependencies; .gitignore confirmed covering future exports.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: D-07 — verify /api/health external traffic before gating deploys</name>
  <action>Pause execution (D-07) and ask the user to check Vercel logs for /api/health traffic before the gating change in Task 2 deploys. Capture the reported result verbatim for the audit + changelog findings written in plan 01-03. Do not proceed to Task 2's deploy-facing acceptance until the user responds.</action>
  <what-built>The api/health.js gating change is ready but NOT yet deployed. Before it ships, D-07 requires confirming whether any external monitor consumes the current /api/health JSON detail, so the result can be recorded as an audit finding either way.</what-built>
  <how-to-verify>
    1. Open the Vercel dashboard → project rems-app → Logs.
    2. Filter/search for requests to path `/api/health` over the recent retention window.
    3. Note whether any recurring external caller (uptime monitor, status checker) hits it, and whether it appears to parse the JSON detail fields (vs a simple up/down HTTP 200 check).
    4. Report the finding: either "no external /api/health traffic observed" or a description of the caller(s). Note that the gated endpoint still returns HTTP 200 on the bare response, so simple up/down monitors keep working — only detail-parsing consumers would notice the change.
  </how-to-verify>
  <resume-signal>Reply with the observed /api/health traffic result (this becomes an audit + changelog finding in plan 01-03). Type "approved — no detail consumers" or describe the consumer(s) found.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Auth-gate api/health.js with a split response</name>
  <read_first>
    - api/health.js (current handler — verify contract)
    - api/send-email.js (lines 18-19 Bearer parsing, lines 25-41 identitytoolkit lookup pattern)
    - api/_lib/config.js (ADMIN_EMAIL, FIREBASE_API_KEY — required here)
    - api/_lib/firebaseAdmin.js (getDb — powers the adminInit probe only, never authentication)
    - .planning/phases/01-audit-repo-hygiene-config-centralization/01-RESEARCH.md (Pattern 1, Pitfalls 4 & 5, Code Examples: api/health.js gated shape)
    - .planning/phases/01-audit-repo-hygiene-config-centralization/01-PATTERNS.md (api/health.js section)
  </read_first>
  <action>
    Implement the split-response health endpoint (D-06, D-08, RESEARCH Pattern 1):
    - Parse the Bearer token using the api/send-email.js:18-19 pattern (`authHeader.startsWith('Bearer ') ? authHeader.slice(7) : null`). No token → return `res.status(200).json({ status: 'ok' })`.
    - Verify the token via the Google identitytoolkit `accounts:lookup` POST using FIREBASE_API_KEY from api/_lib/config.js — NOT `getAuthAdmin().verifyIdToken()`. Rationale (RESEARCH Pitfall 4): the endpoint's purpose is diagnosing a broken FIREBASE_SERVICE_ACCOUNT / admin-SDK init; using the Admin SDK for auth would depend on the exact thing being diagnosed (circular). Wrap the lookup in try/catch and fall through to the bare response on any failure.
    - Admin gate: only when `user.email === ADMIN_EMAIL && user.emailVerified` return the diagnostics object. For invalid tokens AND valid non-admin tokens, return the identical bare `{ status: 'ok' }` — never a 401 or 403 (D-06: no privilege-disclosure differential; RESEARCH threat "endpoint enumeration").
    - The admin-gated payload returns the SAME five flags as today (D-08, no expansion): `resendConfigured` (Boolean of RESEND_API_KEY), `firebaseAdminConfigured` (Boolean of FIREBASE_SERVICE_ACCOUNT), `leadIntakeConfigured` (Boolean of LEAD_INTAKE_KEY), `adminInit` (probe getDb() in try/catch — 'ok' or the error message), and `matchingKeyNames` (Object.keys(process.env) filtered by /RESEND|FIREBASE|LEAD|EMAIL/i). Keep `require('./_lib/firebaseAdmin')` for the adminInit probe only.
    - Preserve any existing observability wrapper on the export (e.g. withSentry) if present — do not strip it.
  </action>
  <verify>
    <automated>node -e "require('./api/health.js')" && grep -q "identitytoolkit.googleapis.com" api/health.js && grep -q "status: 'ok'" api/health.js && grep -q "ADMIN_EMAIL" api/health.js && test $(grep -c "status(401)\|status(403)" api/health.js) -eq 0 && echo HEALTH_GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - api/health.js loads under plain Node without throwing
    - The handler returns `{ status: 'ok' }` with no diagnostic fields when the Authorization header is missing or the token is invalid
    - The handler returns the five diagnostic flags ONLY when the looked-up user email equals ADMIN_EMAIL and emailVerified is true
    - A valid non-admin token receives the identical bare response (no 401/403 anywhere in the file for the auth path)
    - Token verification uses identitytoolkit accounts:lookup (grep finds the URL), not getAuthAdmin().verifyIdToken()
    - Production spot-check (human): `curl -s https://rems-app.vercel.app/api/health` returns exactly `{"status":"ok"}` after deploy
  </acceptance_criteria>
  <done>Unauthenticated and non-admin callers get a bare 200 status; an admin token unlocks the unchanged five-flag diagnostics; auth uses identitytoolkit, not the Admin SDK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated client → api/health.js | Untrusted callers must learn nothing about env/infra; only a Google-verified admin token crosses into diagnostics |
| repo working tree → git history | Deletions must not rewrite history (D-14 / Out of Scope) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-H1 | Information Disclosure | api/health.js env schema (matchingKeyNames) | high | mitigate | Split response (D-06): bare `{status:'ok'}` for all non-admin callers; diagnostics only behind a Google-verified admin-email token |
| T-01-H2 | Spoofing | Admin-email claim to unlock health detail | medium | mitigate | ID token verified server-side via Google identitytoolkit; Firebase enforces one-account-per-email and the admin account already exists (address unclaimable); `emailVerified` required as defense-in-depth |
| T-01-H3 | Information Disclosure | Privileged-endpoint enumeration | medium | mitigate | Non-admin and invalid tokens receive the identical bare 200 — no 401/403 differential reveals a richer response exists |
| T-01-H4 | Elevation of Privilege | REACT_APP_DEV_BYPASS build-time auth-gate skip | medium | mitigate | Documented in ENVIRONMENT.md + audit finding (plan 01-03); user confirms unset in Vercel production; Firestore rules still enforce data access regardless |
| T-01-H5 | Denial of Service | Unknown monitor breaks on new /api/health shape | low | mitigate | D-07 mandatory human log-check before deploy; bare response stays HTTP 200 so up/down monitors keep working |
| T-01-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs NO packages (it removes puppeteer); no supply-chain surface |
</threat_model>

<verification>
- Working tree free of the archive and screenshot.js; screenshot.js untracked; puppeteer absent from devDependencies
- `node -e "require('./api/health.js')"` loads clean; grep confirms identitytoolkit auth and no 401/403 in the auth path
- Post-deploy (human): unauthenticated `curl https://rems-app.vercel.app/api/health` → exactly `{"status":"ok"}`; admin Bearer token → full five-flag detail
- D-07 traffic-check result captured for plan 01-03
- `npm run lint && npm run test:ci && npm run build && npm run check:constants` green
</verification>

<success_criteria>
HYG-01, HYG-02, HYG-03 satisfied: stray archive and screenshot.js removed without history rewrite; api/health.js discloses nothing to unauthenticated/non-admin callers while preserving admin diagnostics; the D-07 traffic check completed and recorded.
</success_criteria>

<output>
Create `.planning/phases/01-audit-repo-hygiene-config-centralization/01-02-SUMMARY.md` when done.
</output>
