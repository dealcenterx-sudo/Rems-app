---
phase: 01-audit-repo-hygiene-config-centralization
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - docs/SAAS_READINESS_AUDIT.md
  - docs/ENVIRONMENT.md
  - docs/SAAS_UPGRADE_PLAN.md
  - docs/SAAS_UPGRADE_CHANGELOG.md
autonomous: false
requirements: [AUDIT-01, AUDIT-02, AUDIT-03, AUDIT-04]
must_haves:
  truths:
    - "A reviewer can read docs/SAAS_READINESS_AUDIT.md with all required sections in order, ending in a Definition of Done (AUDIT-01)"
    - "The audit's risk-ranked findings table carries a Status column (Open / In progress / Fixed in Phase N) and is ordered Critical→Low (D-02, D-03, D-04)"
    - "docs/SAAS_UPGRADE_PLAN.md maps the brief's 8 named phases onto the roadmap execution order with goal/files/tasks/risks/acceptance criteria/verification commands per phase (AUDIT-02)"
    - "docs/SAAS_UPGRADE_CHANGELOG.md has a Phase 1 entry with What/Why/Files/Commands/Results/Risks (AUDIT-03)"
    - "docs/ENVIRONMENT.md documents all 7 environment variables by name (never value) with purpose, consumer, scope, and required flag (AUDIT-04, D-10)"
    - "The D-07 /api/health traffic result and the Firestore backup posture are recorded as audit findings (D-05)"
  artifacts:
    - docs/SAAS_READINESS_AUDIT.md
    - docs/SAAS_UPGRADE_PLAN.md
    - docs/SAAS_UPGRADE_CHANGELOG.md
    - docs/ENVIRONMENT.md
  key_links:
    - "The audit and changelog link to docs/ENVIRONMENT.md for env-var documentation"
    - "The audit findings' Status column reflects Phase 1's own fixes as 'Fixed in Phase 1' and defers later findings to their phase"
    - "docs/SAAS_UPGRADE_PLAN.md phase names match the user-confirmed brief phase list"
---

<objective>
Write the four reviewer-facing documentation deliverables — the SaaS Readiness Audit, the 8-phase Upgrade Plan, the running Changelog (Phase 1 entry), and the Environment variable reference — recording the finished state of Phase 1's code work. The audit is a showcase artifact written with full candor: every finding named with file:line, severity, and remediation status.

Purpose: Delivers the audit foundation every later phase builds on and updates (AUDIT-01..04). Honors D-01 (showcase artifact), D-02 (full candor), D-03 (concise + evidence, ~8-12 pages), D-04 (living document with Status column), D-05 (include backup posture + D-07 result), D-10 (ENVIRONMENT.md, names only).
Output: docs/SAAS_READINESS_AUDIT.md, docs/SAAS_UPGRADE_PLAN.md, docs/SAAS_UPGRADE_CHANGELOG.md, docs/ENVIRONMENT.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-audit-repo-hygiene-config-centralization/01-CONTEXT.md
@.planning/phases/01-audit-repo-hygiene-config-centralization/01-RESEARCH.md
@.planning/phases/01-audit-repo-hygiene-config-centralization/01-PATTERNS.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/INTEGRATIONS.md
@docs/DATABASE_SCHEMA.md
</context>

<artifacts_produced>
## Artifacts this phase produces (new symbols — exclude from drift verification)

- **docs/SAAS_READINESS_AUDIT.md** (NEW/MODIFY): prose deliverable, living document with a Status column
- **docs/SAAS_UPGRADE_PLAN.md** (NEW/MODIFY): 8-phase mapping doc
- **docs/SAAS_UPGRADE_CHANGELOG.md** (NEW/MODIFY): running changelog, Phase 1 entry
- **docs/ENVIRONMENT.md** (NEW/MODIFY): env-var reference table
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: confirm the brief's 8 named phases for the upgrade plan</name>
  <action>Pause execution and present a reconstructed list of the brief's 8 phase names (drawn from PROJECT.md requirement areas + ROADMAP.md phases) for the user to confirm or correct. Use the confirmed names as the mapping keys when writing docs/SAAS_UPGRADE_PLAN.md in Task 2. Do not write the plan doc until the names are confirmed.</action>
  <what-built>docs/SAAS_UPGRADE_PLAN.md must map the brief's literal 8 named phases onto this roadmap's execution order (AUDIT-02). RESEARCH Open Question 1 found the brief's 8 phase titles are not preserved anywhere in .planning/ — only the roadmap side of the mapping exists on disk. A draft reconstruction has been prepared from PROJECT.md's requirement areas and ROADMAP.md for the user to confirm or correct.</what-built>
  <how-to-verify>
    1. Review the proposed 8 phase names (reconstructed from the brief's deliverable areas in PROJECT.md and the roadmap phases in ROADMAP.md).
    2. Confirm they match the brief's original phase titles, or paste the exact 8 names from the brief.
    3. This is the only external input the docs need — everything else is verifiable in-repo.
  </how-to-verify>
  <resume-signal>Reply with the confirmed 8 phase names (or "approved — use the reconstruction"). The mapping doc is written against these names.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Write the SaaS Readiness Audit and the Environment reference</name>
  <read_first>
    - docs/SAAS_READINESS_AUDIT.md (current state — verify contract)
    - docs/ENVIRONMENT.md (current state)
    - docs/DATABASE_SCHEMA.md (tone/style analog — the only existing docs/ file)
    - .planning/codebase/CONCERNS.md (the findings inventory the audit formalizes)
    - .planning/codebase/INTEGRATIONS.md (env-var inventory seeding ENVIRONMENT.md)
    - .planning/phases/01-audit-repo-hygiene-config-centralization/01-RESEARCH.md (Audit Findings Seed List — 15 verified items; Architecture Patterns: audit/changelog/ENVIRONMENT structures; Assumptions A3/A5)
    - .planning/phases/01-audit-repo-hygiene-config-centralization/01-PATTERNS.md (docs deliverables table)
  </read_first>
  <action>
    Write docs/SAAS_READINESS_AUDIT.md as a showcase artifact (D-01) with full candor (D-02), concise + evidence depth (~8-12 pages, D-03), in this locked section order (AUDIT-01): Executive Summary → Stack → Product Purpose → User Flows → Weaknesses by Area (~1 paragraph each) → Risk-Ranked Findings table → Roadmap → Safe Execution Plan → Definition of Done. The findings table columns are Severity / Finding / File:Line / Impact / Fix / Status, ordered Critical→Low (D-04). Populate it from the RESEARCH Audit Findings Seed List (15 verified items) — set Status to "Fixed in Phase 1" for this phase's fixes (admin-email centralization, hardcoded API key, /api/health disclosure, screenshot.js/archive), and "Fixed in Phase N" or "Open" for later/unfixed items. MUST include the two D-05 items: Firestore backup posture (Status: Open — unverified) and the D-07 /api/health traffic-check result (from plan 01-02's checkpoint). Also include the REACT_APP_DEV_BYPASS finding (RESEARCH #4 — build-time client auth-gate skip; recommend confirming unset in Vercel) and note the Firebase-API-key finding is process-hygiene not secret-exposure (the key is a public identifier — RESEARCH Assumption A3: recommend verifying key API restrictions). Do NOT write the admin email literal into the doc — refer to it as ADMIN_EMAIL (RESEARCH Anti-Patterns). Link to docs/ENVIRONMENT.md for env-var detail. Match the DATABASE_SCHEMA.md style (# title with "REMS -" prefix, ## Overview, --- dividers, concrete file/function references).

    Write docs/ENVIRONMENT.md (D-10, AUDIT-04) as a table with columns Variable | Purpose | Consumed by | Scope (build-time client / runtime server) | Required? — names only, never values. MUST document all 7: FIREBASE_SERVICE_ACCOUNT, RESEND_API_KEY, EMAIL_FROM, LEAD_INTAKE_KEY, FIREBASE_API_KEY (new, optional — fallback covers unset per D-11), REACT_APP_DEV_BYPASS (build-time client auth-gate skip — flag as security-relevant), REACT_APP_CRM_EMAIL_WEBHOOK_URL. Note that later phases append (Sentry DSN in Phase 3, Cloudinary secret in Phase 4).
  </action>
  <verify>
    <automated>test -f docs/SAAS_READINESS_AUDIT.md && grep -q "Definition of Done" docs/SAAS_READINESS_AUDIT.md && grep -qi "Status" docs/SAAS_READINESS_AUDIT.md && for v in FIREBASE_SERVICE_ACCOUNT RESEND_API_KEY EMAIL_FROM LEAD_INTAKE_KEY FIREBASE_API_KEY REACT_APP_DEV_BYPASS REACT_APP_CRM_EMAIL_WEBHOOK_URL; do grep -q "$v" docs/ENVIRONMENT.md || { echo "MISSING $v"; exit 1; }; done && echo DOCS_OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/SAAS_READINESS_AUDIT.md contains all nine required sections in order, ending with "Definition of Done"
    - The findings table includes a Status column and rows for: admin-email centralization, hardcoded Firebase API key, /api/health disclosure, screenshot.js/archive, Firestore backup posture (Open), the D-07 traffic result, and REACT_APP_DEV_BYPASS
    - The admin email literal does not appear anywhere in docs/SAAS_READINESS_AUDIT.md
    - docs/ENVIRONMENT.md documents all 7 variables by name with purpose, consumer, scope, and required flag — no values present
  </acceptance_criteria>
  <done>The audit reads as a senior team's assessment with a living Status column; ENVIRONMENT.md documents all 7 env vars by name only.</done>
</task>

<task type="auto">
  <name>Task 2: Write the 8-phase Upgrade Plan and the running Changelog</name>
  <read_first>
    - docs/SAAS_UPGRADE_PLAN.md (current state — verify contract)
    - docs/SAAS_UPGRADE_CHANGELOG.md (current state)
    - .planning/ROADMAP.md (the 8-phase execution order + per-phase goals/criteria to map onto)
    - .planning/phases/01-audit-repo-hygiene-config-centralization/01-RESEARCH.md (changelog structure; Summary of Phase 1 changes/commands)
    - .planning/phases/01-audit-repo-hygiene-config-centralization/01-PATTERNS.md (docs deliverables table)
  </read_first>
  <action>
    Write docs/SAAS_UPGRADE_PLAN.md (AUDIT-02) mapping the user-confirmed 8 brief phase names (from the checkpoint) onto this roadmap's research-backed execution order. For each phase provide: goal, files affected, tasks, risks, acceptance criteria, and verification commands — sourced from the corresponding ROADMAP.md phase details. Make the brief-name → roadmap-phase mapping explicit (the mapping is a roadmap-level decision already made — CONTEXT specifics — not open for redesign).

    Write docs/SAAS_UPGRADE_CHANGELOG.md (AUDIT-03, D-04) with a `## Phase 1 — <name> (date)` section containing What / Why / Files / Commands / Results / Risks subsections. Record Phase 1's actual work: config centralization (src/config.js, api/_lib/config.js, consumer repointing), HYG-05 API-key env-var move, check-constants CI proof, archive + screenshot.js removal + puppeteer uninstall, api/health.js gating, and the D-07 traffic-check result. Commands should list what was actually run (npm run check:constants, npm run test:ci, npm run build, git rm screenshot.js, rm -rf of the archive). Note in both docs that the changelog and the audit's Status column are standing tasks maintained by every subsequent phase.
  </action>
  <verify>
    <automated>test -f docs/SAAS_UPGRADE_PLAN.md && test -f docs/SAAS_UPGRADE_CHANGELOG.md && grep -qi "Phase 1" docs/SAAS_UPGRADE_CHANGELOG.md && grep -qi "verification\|acceptance" docs/SAAS_UPGRADE_PLAN.md && echo PLAN_CHANGELOG_OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/SAAS_UPGRADE_PLAN.md maps 8 named phases onto the roadmap, each with goal/files/tasks/risks/acceptance criteria/verification commands
    - The brief-name → roadmap-phase mapping is explicit in the plan doc
    - docs/SAAS_UPGRADE_CHANGELOG.md has a Phase 1 section with What/Why/Files/Commands/Results/Risks
    - The changelog Phase 1 entry references the actual commands run and the D-07 traffic result
  </acceptance_criteria>
  <done>The upgrade plan maps the confirmed 8 brief phases with per-phase detail; the changelog records Phase 1's finished state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs → public reviewers | The audit is a public showcase artifact; it must name findings with candor but never leak secret values (only variable names, no values — D-02, D-10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-D1 | Information Disclosure | docs/ENVIRONMENT.md + audit | medium | mitigate | Env vars documented by NAME only, never value (D-10); no secret ever written to a doc; the Firebase web key referenced is a public identifier |
| T-01-D2 | Information Disclosure | REACT_APP_DEV_BYPASS documented finding | low | mitigate | Documenting the flag is intentional (audit candor); paired with a recommendation to confirm it is unset in Vercel production; Firestore rules enforce data access regardless |
| T-01-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs NO packages (prose docs only); no supply-chain surface |
</threat_model>

<verification>
- All four docs exist; audit ends in Definition of Done and carries a Status column
- All 7 env vars present in ENVIRONMENT.md by name; no values anywhere
- Changelog has a Phase 1 entry with the six subsections
- Upgrade plan maps the confirmed 8 phase names with per-phase verification commands
- Admin email literal absent from all four docs
</verification>

<success_criteria>
AUDIT-01..04 satisfied: a reviewer can read a complete, candid, evidence-backed audit (living Status column), an 8-phase mapped upgrade plan, a Phase 1 changelog entry, and a name-only environment reference — recording the finished state of Phase 1.
</success_criteria>

<output>
Create `.planning/phases/01-audit-repo-hygiene-config-centralization/01-03-SUMMARY.md` when done.
</output>
