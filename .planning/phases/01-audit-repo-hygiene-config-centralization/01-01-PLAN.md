---
phase: 01-audit-repo-hygiene-config-centralization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.js
  - api/_lib/config.js
  - src/firebase.js
  - src/utils/helpers.js
  - src/components/NewDealPage.js
  - src/components/CRMMessagesPage.js
  - src/components/DealsDashboard.js
  - src/components/CRMEmailInboxPage.js
  - src/utils/permissions.test.js
  - api/send-email.js
  - api/lead-intake.js
  - scripts/check-constants.js
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [HYG-04, HYG-05]
must_haves:
  truths:
    - "The admin email literal appears in tracked code files only in src/config.js, api/_lib/config.js, and firestore.rules (HYG-04)"
    - "ROLES and SELF_SERVICE_ROLES are defined once (src/config.js) and imported everywhere they are used"
    - "api/send-email.js obtains the Firebase web API key from api/_lib/config.js, which reads process.env.FIREBASE_API_KEY with a checked-in fallback (HYG-05)"
    - "npm run check:constants exits non-zero when the admin email literal is added to any tracked code file outside the three allowed locations"
    - "CI runs check:constants on every push/PR to main, before lint/test/build"
    - "The existing Jest suite (including src/utils/permissions.test.js) stays green after the refactor"
  artifacts:
    - src/config.js
    - api/_lib/config.js
    - scripts/check-constants.js
  key_links:
    - "src/firebase.js, src/utils/helpers.js, and the four component consumers import ADMIN_EMAIL from src/config.js"
    - "api/send-email.js and api/lead-intake.js require FIREBASE_API_KEY / ADMIN_EMAIL from api/_lib/config.js"
    - "scripts/check-constants.js reads ADMIN_EMAIL via require('../api/_lib/config') so it never embeds the literal"
    - ".github/workflows/ci.yml has a 'Check constants' step running npm run check:constants between Install dependencies and Lint"
---

<objective>
Centralize the admin email and shared role constants into one client module (src/config.js) and one server module (api/_lib/config.js), repoint every consumer to import from them, move the hardcoded Firebase web API key in api/send-email.js to an env-var-with-fallback read, and add a CI-enforced grep proof (scripts/check-constants.js + npm script + ci.yml step) that fails if the admin email literal ever reappears outside the three allowed locations.

Purpose: Kills the two-sources-of-truth bug (admin email in 10 code locations, API key hardcoded) and makes the fix self-enforcing so it cannot silently regress — the CI check is the deliverable that keeps HYG-04 done. Implements HYG-04 (config centralization + CI grep proof) and HYG-05 (env-var API key), honoring D-09, D-11, D-12.
Output: src/config.js, api/_lib/config.js, scripts/check-constants.js (new); repointed consumers; check:constants npm script; ci.yml step.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audit-repo-hygiene-config-centralization/01-CONTEXT.md
@.planning/phases/01-audit-repo-hygiene-config-centralization/01-RESEARCH.md
@.planning/phases/01-audit-repo-hygiene-config-centralization/01-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (new symbols — exclude from drift verification)

- **src/config.js** (NEW, ESM): `export const ADMIN_EMAIL`, `export const ROLES`, `export const SELF_SERVICE_ROLES`
- **api/_lib/config.js** (existing/mirrored, CommonJS): `module.exports = { ADMIN_EMAIL, FIREBASE_API_KEY, CLOUDINARY_CLOUD_NAME }`
- **scripts/check-constants.js** (NEW, CommonJS): standalone Node script, no exports
- **package.json**: new `check:constants` script key
- **.github/workflows/ci.yml**: new `Check constants` step
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Create the client and server config modules</name>
  <read_first>
    - src/config.js (current state — may already exist from a prior run; verify contract)
    - api/_lib/config.js (current state)
    - src/utils/cloudinary.js (ESM named-export constants analog)
    - api/_lib/firebaseAdmin.js (CommonJS module.exports analog for api/_lib/)
    - src/firebase.js (lines 25-29: current ADMIN_EMAIL + SELF_SERVICE_ROLES definitions being centralized)
    - .planning/phases/01-audit-repo-hygiene-config-centralization/01-PATTERNS.md (sections on src/config.js and api/_lib/config.js)
  </read_first>
  <action>
    Ensure src/config.js exists as an ESM module exporting three named UPPER_SNAKE_CASE constants: ADMIN_EMAIL (the current admin email string, unchanged), ROLES (the four-element array admin/agent/buyer/seller), and SELF_SERVICE_ROLES (agent/buyer/seller — carry over the src/firebase.js:27-28 comment documenting that admin is never self-service and Firestore rules reject role:'admin' on create). This module MUST NOT import from src/firebase.js or any other module — it is pure constants with zero dependencies (so permissions.test.js's jest.mock('../firebase') stays sufficient — per D-09, PATTERNS Pitfall 2 reference).

    Ensure api/_lib/config.js exists as a CommonJS module exporting ADMIN_EMAIL (mirrored literal — the ESM/CJS split means one file cannot serve both, per D-09/D-11) and FIREBASE_API_KEY read as `process.env.FIREBASE_API_KEY || <checked-in public web key fallback>` (D-11: zero deploy risk, the key is a public identifier per Firebase docs, user sets the Vercel var at leisure). Use the header-comment convention from api/_lib/firebaseAdmin.js. Keep the existing CLOUDINARY_CLOUD_NAME export if present (do not remove pre-existing exports). Do NOT fold src/utils/cloudinary.js config into these modules (D-09 — Cloudinary stays its own source of truth).
  </action>
  <verify>
    <automated>node -e "const c=require('./api/_lib/config'); if(!c.ADMIN_EMAIL||!c.FIREBASE_API_KEY){process.exit(1)}" && grep -qE "export const ADMIN_EMAIL|export const ROLES|export const SELF_SERVICE_ROLES" src/config.js && grep -c "export const" src/config.js</automated>
  </verify>
  <acceptance_criteria>
    - src/config.js exports ADMIN_EMAIL, ROLES, and SELF_SERVICE_ROLES as ESM named exports
    - src/config.js contains no import statement (zero dependencies)
    - api/_lib/config.js exports ADMIN_EMAIL and FIREBASE_API_KEY via module.exports
    - api/_lib/config.js FIREBASE_API_KEY uses the `process.env.FIREBASE_API_KEY || fallback` shape
    - `node -e "require('./api/_lib/config')"` loads without error and both keys are truthy
  </acceptance_criteria>
  <done>Both config modules exist with the specified exports; the server module loads under plain Node; the client module is dependency-free.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint all client-side consumers to import from src/config.js</name>
  <read_first>
    - src/config.js (the module now being imported)
    - src/firebase.js (lines 1-4 import block, 25, 29)
    - src/utils/helpers.js (lines 1-4, 164)
    - src/components/NewDealPage.js (line 35)
    - src/components/CRMMessagesPage.js (line 143)
    - src/components/DealsDashboard.js (line 61)
    - src/components/CRMEmailInboxPage.js (lines 73, 86, 264)
    - src/utils/permissions.test.js (lines 1-9)
    - .planning/phases/01-audit-repo-hygiene-config-centralization/01-PATTERNS.md (Component consumers table + permissions.test.js section)
  </read_first>
  <action>
    Perform the behavior-preserving literal swap (PATTERNS Pattern 2) across all client consumers — byte-identical apart from replacing the admin email literal with the imported ADMIN_EMAIL constant, and replacing the local ROLES/SELF_SERVICE_ROLES definitions with imports. Preserve each line's exact optional-chaining (some files use `currentUser.email`, others `currentUser?.email` — do not normalize).

    - src/firebase.js: add `import { ADMIN_EMAIL, SELF_SERVICE_ROLES } from './config';` to the import block; delete the local `const ADMIN_EMAIL` (line 25) and local `const SELF_SERVICE_ROLES` (line 29). Leave firebaseConfig.apiKey (line 7, client web config) untouched — out of scope.
    - src/utils/helpers.js: replace the local `const ADMIN_EMAIL` (line 4) with `import { ADMIN_EMAIL } from '../config';`; drop or update the stale "change this constant" comment to point at src/config.js; change the inline literal in `isAdmin()` (line 164) to `=== ADMIN_EMAIL`. Do NOT merge isAdmin into isAdminUser (helper consolidation is out of scope — D-09).
    - src/components/NewDealPage.js, CRMMessagesPage.js, DealsDashboard.js, CRMEmailInboxPage.js: add `import { ADMIN_EMAIL } from '../config';` alongside the existing firebase import, and swap each admin-email literal (NewDealPage:35, CRMMessagesPage:143, DealsDashboard:61, CRMEmailInboxPage:73/86/264) for ADMIN_EMAIL. CRMEmailInboxPage:73 is a sample-data `from:` fallback (not an admin check) — the swap is still behavior-identical.
    - src/utils/permissions.test.js: add `import { ADMIN_EMAIL } from '../config';` and change the admin fixture (line 9) to `email: ADMIN_EMAIL` so the "admin email counts even without the admin role" test keeps tracking the real constant (D-09, PATTERNS Pitfall 2). Keep the existing `jest.mock('../firebase', ...)` — src/config.js has no firebase dependency, so the mock stays sufficient.
  </action>
  <verify>
    <automated>npm run test:ci 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:ci` passes with no failures, including src/utils/permissions.test.js
    - No admin email literal remains in any src/ file except src/config.js (verified in Task 3 by check:constants)
    - src/firebase.js no longer defines local ADMIN_EMAIL or SELF_SERVICE_ROLES constants; both come from ./config
    - permissions.test.js imports ADMIN_EMAIL from ../config and the admin fixture uses email: ADMIN_EMAIL
    - Optional-chaining on each swapped line is unchanged from before the edit
  </acceptance_criteria>
  <done>All eight client consumers plus the test fixture import ADMIN_EMAIL (and firebase.js imports SELF_SERVICE_ROLES) from src/config.js; the Jest suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: Repoint server consumers (HYG-05), add the check-constants proof and CI step</name>
  <read_first>
    - api/send-email.js (lines 1-5 header + key definition, line 28 lookup URL)
    - api/lead-intake.js (lines 5-7)
    - api/_lib/config.js (the module now being required)
    - scripts/check-constants.js (current state — verify contract)
    - package.json (scripts block)
    - .github/workflows/ci.yml (Install dependencies / Lint step region)
    - .planning/phases/01-audit-repo-hygiene-config-centralization/01-RESEARCH.md (Code Examples: check-constants.js sketch; Pitfalls 3, 6, 7)
    - .planning/phases/01-audit-repo-hygiene-config-centralization/01-PATTERNS.md (send-email/lead-intake swap, scripts/check-constants.js, package.json, ci.yml sections)
  </read_first>
  <action>
    Server literal swaps (HYG-05, D-11):
    - api/send-email.js: replace the hardcoded key definition (lines 4-5) with `const { FIREBASE_API_KEY } = require('./_lib/config');`. The lookup URL at line 28 already interpolates FIREBASE_API_KEY — leave it untouched.
    - api/lead-intake.js: replace the local `const ADMIN_EMAIL` (line 7) with `const { ADMIN_EMAIL } = require('./_lib/config');`, matching the existing require style at line 5. The consumer at line 44 (getUserByEmail(ADMIN_EMAIL)) is untouched.

    check-constants proof (HYG-04, D-12) — ensure scripts/check-constants.js exists as a plain-Node CommonJS script following the RESEARCH Code Examples sketch:
    - Read ADMIN_EMAIL via `require('../api/_lib/config')` — the script MUST NOT embed the email literal (so it needs no allowlist entry — RESEARCH Anti-Patterns).
    - Enumerate files with `execSync('git ls-files src api scripts public .github firestore.rules vercel.json package.json')` — never an fs walk, never shell grep (BSD vs GNU divergence — RESEARCH Pitfall 3).
    - Allowlist exactly three paths: src/config.js, api/_lib/config.js, firestore.rules.
    - Exit 1 listing violating paths on stderr; exit 0 with a one-line OK message.
    - package.json: add `"check:constants": "node scripts/check-constants.js"` to the scripts block (terse single-command style, no && chains).
    - .github/workflows/ci.yml: ensure a `Check constants` step runs `npm run check:constants` immediately after `Install dependencies` and before `Lint` (fastest-fail first; preserves lint -> test -> build order). Do NOT touch the Build step's deliberate `CI: false` (RESEARCH Pitfall 7).

    Then run the sabotage test once to prove the gate bites: temporarily add the admin email literal to any tracked component, confirm `npm run check:constants` exits 1 and names that file, then revert the sabotage.
  </action>
  <verify>
    <automated>npm run check:constants && test $(grep -c "AIzaSy" api/send-email.js) -eq 0 && node -e "require('./api/lead-intake.js')" 2>/dev/null; grep -q "check:constants" package.json && grep -q "check:constants" .github/workflows/ci.yml && echo GATE_WIRED</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check:constants` exits 0 on the clean tree and prints a one-line OK
    - Adding the admin email literal to a tracked code file makes `npm run check:constants` exit 1 and print that file path (sabotage test performed and reverted)
    - `grep -c "AIzaSy" api/send-email.js` returns 0 (no hardcoded key literal remains in that file)
    - scripts/check-constants.js contains `require('../api/_lib/config')` and does not contain the admin email literal
    - package.json scripts block contains the check:constants entry
    - .github/workflows/ci.yml contains a step running npm run check:constants positioned before the Lint step
  </acceptance_criteria>
  <done>send-email and lead-intake read from api/_lib/config.js; check-constants.js passes clean, fails on a planted literal, and runs in CI before lint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build/deploy → repo | The admin email and API key are compile-time/deploy-time constants; a leaked or duplicated literal is a config-integrity risk, not a runtime auth bypass |
| CI → main | check:constants runs in CI before merge; a green main with a partial migration would defeat the deliverable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-C1 | Tampering | Config migration (10 consumers) | medium | mitigate | check:constants CI gate makes a partial migration (2 sources of truth) fail the build — enforcement lands in the same phase as the refactor (RESEARCH Pitfall 6) |
| T-01-C2 | Information Disclosure | api/send-email.js API key | low | accept | Firebase web API key is a public identifier, not a secret [CITED: firebase.google.com/docs/projects/api-keys]; env-var-with-fallback (D-11) is process hygiene, and the checked-in fallback is compliant |
| T-01-C3 | Tampering | check-constants string-construction bypass (`'x'+'@y'`) | low | accept | Out of scope for a literal check; all current occurrences are plain literals (verified); mitigated by code review |
| T-01-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs NO packages (RESEARCH Package Legitimacy Audit: no rows) — no supply-chain surface |
</threat_model>

<verification>
- `npm run test:ci` green (permissions.test.js tracks the real ADMIN_EMAIL constant)
- `npm run check:constants` exits 0 clean; exits 1 on a planted literal (sabotage test)
- `npm run lint && npm run build` succeed
- No admin email literal in tracked code outside src/config.js, api/_lib/config.js, firestore.rules
- No `AIzaSy` literal remains in api/send-email.js
</verification>

<success_criteria>
HYG-04 and HYG-05 satisfied: admin email + roles single-sourced in src/config.js (client) and api/_lib/config.js (server); Firebase API key read from env with fallback; CI grep proof green and enforced before lint on every push/PR to main.
</success_criteria>

<output>
Create `.planning/phases/01-audit-repo-hygiene-config-centralization/01-01-SUMMARY.md` when done.
</output>
